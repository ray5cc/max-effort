import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Java 核心面试题","description":"","frontmatter":{},"headers":[],"relativePath":"02-面试指南/02-后端面试/01-Java核心面试题.md","filePath":"02-面试指南/02-后端面试/01-Java核心面试题.md","lastUpdated":1774066204000}');
const _sfc_main = { name: "02-面试指南/02-后端面试/01-Java核心面试题.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="java-核心面试题" tabindex="-1">Java 核心面试题 <a class="header-anchor" href="#java-核心面试题" aria-label="Permalink to &quot;Java 核心面试题&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>基于 OpenJDK HotSpot 源码的分层面试题，涵盖 JVM 内存模型、垃圾收集器、类加载机制、并发编程底层原理与 Java 内存模型（JMM）。</p></blockquote><h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to &quot;相关链接&quot;">&amp;ZeroWidthSpace;</a></h2><ul><li>对应技术资料：<a href="./../../01-技术资料/02-后端/01-Java核心与JVM.html">Java 核心与 JVM</a></li></ul><h2 id="🔥-高频考点速记" tabindex="-1">🔥 高频考点速记 <a class="header-anchor" href="#🔥-高频考点速记" aria-label="Permalink to &quot;🔥 高频考点速记&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>面试中最常被问到的核心知识点，按出现频率排序。建议优先掌握前 5 项。</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>考点</th><th>核心要点（一句话）</th><th>出题概率</th></tr></thead><tbody><tr><td>1</td><td>JVM 内存模型</td><td>堆(Young/Old分区) + 方法区(Metaspace) + 栈 + PC</td><td>★★★★★</td></tr><tr><td>2</td><td>GC 算法与收集器</td><td>G1(Region化+暂停目标) 是默认，ZGC(&lt;1ms) 是未来</td><td>★★★★★</td></tr><tr><td>3</td><td>synchronized 锁升级</td><td>无锁→偏向锁→轻量级锁(CAS)→重量级锁(Monitor)</td><td>★★★★★</td></tr><tr><td>4</td><td>volatile 与 JMM</td><td>可见性+有序性(内存屏障)，不保证原子性</td><td>★★★★☆</td></tr><tr><td>5</td><td>HashMap 原理</td><td>数组+链表+红黑树，扩容 rehash，线程不安全</td><td>★★★★★</td></tr><tr><td>6</td><td>类加载机制</td><td>双亲委派：Bootstrap→Extension→App，确保类唯一性</td><td>★★★★☆</td></tr><tr><td>7</td><td>AQS 框架</td><td>state + CLH 队列，ReentrantLock/Semaphore 的基石</td><td>★★★☆☆</td></tr><tr><td>8</td><td>线程池</td><td>核心线程→队列→最大线程→拒绝策略，生产禁用 Executors</td><td>★★★★☆</td></tr><tr><td>9</td><td>ConcurrentHashMap</td><td>JDK 8: CAS+synchronized 分段锁，JDK 7: Segment 锁</td><td>★★★★☆</td></tr><tr><td>10</td><td>OOM 排查</td><td>jmap dump → MAT/VisualVM 分析 → 大对象/泄漏定位</td><td>★★★★☆</td></tr></tbody></table><hr><h2 id="题目列表" tabindex="-1">题目列表 <a class="header-anchor" href="#题目列表" aria-label="Permalink to &quot;题目列表&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="⭐-基础题" tabindex="-1">⭐ 基础题 <a class="header-anchor" href="#⭐-基础题" aria-label="Permalink to &quot;⭐ 基础题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q1. JVM 运行时数据区分为哪几个区域？哪些是线程私有的，哪些是线程共享的？</strong></p><p><strong>参考答案：</strong></p><p><strong>线程共享（进程级别）：</strong></p><ul><li><strong>堆（Heap）</strong>：存储所有对象实例和数组，GC 的主要工作区域</li><li><strong>方法区（Method Area）/ Metaspace（JDK 8+）</strong>：存储类信息、运行时常量池、静态变量、JIT 编译代码</li></ul><p><strong>线程私有（每线程独立）：</strong></p><ul><li><strong>虚拟机栈（VM Stack）</strong>：每个方法调用创建一个栈帧（局部变量表、操作数栈、动态链接、返回地址）</li><li><strong>本地方法栈（Native Method Stack）</strong>：执行 native 方法使用</li><li><strong>程序计数器（Program Counter）</strong>：当前执行的字节码行号，唯一不会 OOM 的区域（native 方法为 undefined）</li></ul><p><strong>JDK 8 的重要变化：</strong> 永久代（PermGen）→ Metaspace，后者使用本地内存（Native Memory），不受 <code>-Xmx</code> 限制，但可用 <code>-XX:MaxMetaspaceSize</code> 限制。</p><hr><p><strong>Q2. Java 对象在堆中的分配流程是什么？</strong></p><p><strong>参考答案：</strong></p><ol><li><p><strong>TLAB（Thread-Local Allocation Buffer）优先</strong>：每个线程在 Eden 区预分配一块独占缓冲区，对象分配直接在 TLAB 内 bump pointer，无需锁（CAS），是最快路径</p></li><li><p><strong>TLAB 不够</strong>：TLAB 剩余空间不足时，申请新的 TLAB 或直接在 Eden 区上 CAS 分配</p></li><li><p><strong>Eden 满</strong>：触发 Minor GC</p></li><li><p><strong>大对象</strong>：超过 <code>-XX:PretenureSizeThreshold</code> 的对象直接分配到老年代，避免在 Young Gen 来回复制</p></li><li><p><strong>老年代不够</strong>：Full GC → 仍不够 → <code>OutOfMemoryError: Java heap space</code></p></li></ol><p><strong>动态年龄晋升：</strong> 若 Survivor 区中某年龄段对象的总大小超过 Survivor 空间的一半，该年龄及以上的对象直接晋升（不必等到 MaxTenuringThreshold=15）。</p><hr><p><strong>Q3. Minor GC 和 Full GC 的区别是什么？什么情况会触发 Full GC？</strong></p><p><strong>参考答案：</strong></p><table tabindex="0"><thead><tr><th></th><th>Minor GC</th><th>Full GC</th></tr></thead><tbody><tr><td>回收区域</td><td>Young Generation（Eden + Survivor）</td><td>整个堆 + Metaspace</td></tr><tr><td>触发条件</td><td>Eden 区空间不足</td><td>多种情况（见下）</td></tr><tr><td>STW 时间</td><td>短（Young Gen 通常较小）</td><td>长</td></tr><tr><td>频率</td><td>频繁</td><td>少</td></tr></tbody></table><p><strong>触发 Full GC 的常见原因：</strong></p><ol><li>老年代空间不足（对象晋升失败 / 大对象分配失败）</li><li>Minor GC 后晋升到老年代的对象大于老年代剩余空间（空间担保失败）</li><li>显式调用 <code>System.gc()</code>（建议 <code>-XX:+DisableExplicitGC</code>）</li><li>Metaspace 空间不足</li><li>CMS GC promotion failed / concurrent mode failure</li></ol><hr><p><strong>Q4. 什么是 GC Roots？常见的 GC Roots 有哪些？</strong></p><p><strong>参考答案：</strong></p><p>GC Roots 是<strong>可达性分析</strong>的起始点——从 GC Roots 出发，能被引用到的对象都是存活对象，不可达的对象是垃圾。</p><p><strong>常见 GC Roots：</strong></p><ol><li>虚拟机栈中的局部变量（各线程的栈帧中正在使用的对象引用）</li><li>方法区中的静态变量引用的对象</li><li>方法区中常量引用的对象（如字符串常量池中的引用）</li><li>本地方法栈中 JNI（native 方法）引用的对象</li><li>JVM 内部引用（如基本数据类型对应的 Class 对象、常驻异常对象 <code>NullPointerException</code> 等）</li><li>同步锁（synchronized）持有的对象</li><li>被 JVM 内部使用的 JMX Bean、JVMTI 中注册的回调、本地代码缓存等</li></ol><hr><p><strong>Q5. 解释 Java 中的 <code>volatile</code> 关键字，它能解决什么问题，不能解决什么？</strong></p><p><strong>参考答案：</strong></p><p><strong><code>volatile</code> 保证：</strong></p><ol><li><p><strong>可见性</strong>：对 volatile 变量的写立即刷新到主内存，其他线程的读从主内存获取最新值（通过内存屏障实现）</p></li><li><p><strong>有序性（禁止重排序）</strong>：volatile 写前插入 <code>StoreStore</code> 屏障，写后插入 <code>StoreLoad</code> 屏障；volatile 读后插入 <code>LoadLoad</code> + <code>LoadStore</code> 屏障</p></li></ol><p><strong><code>volatile</code> 不保证：</strong></p><ul><li><strong>原子性</strong>：<code>i++</code> 不是原子操作（读取→加1→写回三步），多线程并发执行仍有数据竞争<div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> i </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 线程A和线程B同时执行 i++，结果可能不是 2</span></span></code></pre></div></li></ul><p><strong>适用场景：</strong></p><ul><li>状态标志（<code>volatile boolean stopped = false</code>）</li><li>DCL（双重检查锁）中的 <code>volatile instance</code></li><li>轻量级的读多写少场景（写操作本身已是原子的，如 volatile long 赋值）</li></ul><hr><p><strong>Q6. <code>synchronized</code> 和 <code>ReentrantLock</code> 的区别是什么？各自适用场景？</strong></p><p><strong>参考答案：</strong></p><table tabindex="0"><thead><tr><th>特性</th><th>synchronized</th><th>ReentrantLock</th></tr></thead><tbody><tr><td>实现层次</td><td>JVM 字节码（monitorenter/monitorexit）</td><td>Java API（AQS）</td></tr><tr><td>锁释放</td><td>自动（代码块结束/异常时）</td><td>必须手动 <code>unlock()</code>（finally 块）</td></tr><tr><td>可中断</td><td>不支持</td><td><code>lockInterruptibly()</code> 支持</td></tr><tr><td>超时尝试</td><td>不支持</td><td><code>tryLock(time, unit)</code> 支持</td></tr><tr><td>公平性</td><td>非公平（性能更好）</td><td>可选公平/非公平</td></tr><tr><td>多 Condition</td><td><code>wait()/notify()</code>（一个等待队列）</td><td><code>newCondition()</code> 可创建多个等待队列</td></tr><tr><td>性能（JDK 8+）</td><td>相当（锁升级优化）</td><td>相当</td></tr></tbody></table><p><strong>选择建议：</strong></p><ul><li>简单场景：优先 <code>synchronized</code>（代码简洁，不会忘记 unlock）</li><li>需要超时、可中断、公平锁、多个 Condition：选 <code>ReentrantLock</code></li></ul><hr><h3 id="⭐⭐-进阶题" tabindex="-1">⭐⭐ 进阶题 <a class="header-anchor" href="#⭐⭐-进阶题" aria-label="Permalink to &quot;⭐⭐ 进阶题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q7. 描述 G1 收集器的工作原理，它如何实现可预测停顿时间？</strong></p><p><strong>参考答案：</strong></p><p><strong>G1 Region 化内存管理：</strong></p><p>G1 将堆划分为大小相等的 Region（1-32MB，通常 2048 个），每个 Region 可以动态扮演 Eden/Survivor/Old/Humongous 角色，打破了传统分代的物理边界。</p><p><strong>可预测停顿的实现：</strong></p><ol><li><strong>Region 粒度回收</strong>：G1 每次 GC 选择若干个 Region（而非整个 Young/Old 区）进行回收</li><li><strong>Garbage First 策略</strong>：优先回收 <strong>垃圾比例最高</strong>的 Region（单位时间内回收效益最大）</li><li><strong>停顿预测模型</strong>：G1 基于历史数据（每个 Region 的回收时间）建立预测模型，在 <code>-XX:MaxGCPauseMillis</code>（默认 200ms）限制内选择尽量多的 Region</li><li><strong>Young GC 控制</strong>：通过动态调整 Eden Region 数量，使 Young GC 停顿不超过目标</li></ol><p><strong>RSet（Remembered Set）：</strong> 每个 Region 维护一个 RSet，记录其他 Region 对本 Region 的引用。避免 GC 时扫描整个堆来找跨 Region 引用，大幅减少扫描范围。</p><p><strong>触发 Mixed GC 的条件：</strong> Old Region 占堆的比例超过 <code>InitiatingHeapOccupancyPercent</code>（默认 45%），开始混合回收 Young + 部分 Old Region。</p><hr><p><strong>Q8. 解释 AQS（AbstractQueuedSynchronizer）的核心机制。</strong></p><p><strong>参考答案：</strong></p><p>AQS 是 <code>java.util.concurrent</code> 包中大多数同步器的共同框架，核心是一个<strong>volatile int state + CLH 变体等待队列</strong>。</p><p><strong>核心数据结构：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// state: 同步状态（不同子类含义不同）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ReentrantLock: 0=未锁，&gt;=1=锁定重入次数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// Semaphore: 剩余许可数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// CountDownLatch: 剩余计数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> state;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// CLH 变体等待队列（Node 双向链表）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> transient</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Node head;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> transient</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Node tail;</span></span></code></pre></div><p><strong>独占模式获取锁（<code>lock()</code> → <code>acquire(1)</code>）流程：</strong></p><ol><li><code>tryAcquire(1)</code>：子类尝试获取（ReentrantLock 中 CAS state 0→1）</li><li>成功 → 直接返回</li><li>失败 → <code>addWaiter(Node.EXCLUSIVE)</code> 将当前线程加入队列尾部（CAS 操作）</li><li><code>acquireQueued(node, 1)</code> 自旋： <ul><li>如果前驱是头节点：再次 <code>tryAcquire()</code>（防止前驱刚释放锁）</li><li>成功 → 设自己为头节点，返回</li><li>失败 → 检查是否应该 park：前驱状态为 <code>SIGNAL</code> → <code>LockSupport.park(this)</code> 阻塞</li></ul></li></ol><p><strong>释放锁（<code>unlock()</code> → <code>release(1)</code>）流程：</strong></p><ol><li><code>tryRelease(1)</code>：state 减 1（重入次数递减）</li><li>state == 0：<code>unparkSuccessor(head)</code> 唤醒头节点的后继节点（<code>LockSupport.unpark(nextThread)</code>）</li></ol><p><strong>公平锁 vs 非公平锁：</strong> 非公平锁在 <code>tryAcquire</code> 时不检查队列是否有等待者，直接 CAS 竞争（可能插队）；公平锁先检查队列为空才 CAS。非公平锁吞吐量更高，但可能饥饿。</p><hr><p><strong>Q9. CMS 收集器有哪些缺陷？G1 如何解决了这些问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>CMS 的三大缺陷：</strong></p><ol><li><p><strong>内存碎片：</strong> CMS 使用标记-清除算法，不移动对象，久而久之产生大量内存碎片。当需要分配大对象时，碎片化的老年代找不到连续空间，被迫触发 Serial Old（单线程 STW Full GC）。</p></li><li><p><strong>浮动垃圾（Floating Garbage）：</strong> 并发标记阶段（CMS 不停用户线程），新产生的垃圾对象无法在本轮 GC 中回收，必须等到下轮 GC——这些就是&quot;浮动垃圾&quot;。因此 CMS 必须在老年代使用率达到约 92%（<code>CMSInitiatingOccupancyFraction</code>）时就触发 GC，不能等满再 GC。</p></li><li><p><strong>Concurrent Mode Failure：</strong> 若 CMS 并发清除阶段，老年代空间不足（浮动垃圾 + 新晋升对象），退化为 Serial Old Full GC——这是最严重的性能灾难，可能导致几秒到几十秒的停顿。</p></li></ol><p><strong>G1 的解决方案：</strong></p><ol><li><strong>内存碎片：</strong> G1 使用标记-复制算法回收 Region，存活对象被复制到新 Region，无碎片</li><li><strong>浮动垃圾：</strong> G1 的 Remembered Set 和 SATB（Snapshot-At-The-Beginning）写屏障准确跟踪对象变化，浮动垃圾问题大幅减轻</li><li><strong>停顿预测：</strong> G1 通过停顿模型控制每次 GC 的 Region 数量，规避 Full GC（但 Evacuation Failure 时仍可能触发）</li></ol><hr><p><strong>Q10. 什么是类加载的双亲委派机制？为什么这样设计？如何打破双亲委派？</strong></p><p><strong>参考答案：</strong></p><p><strong>双亲委派工作流程：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ClassLoader.loadClass() 核心逻辑：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Class&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt; c </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> findLoadedClass</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(name);  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 已加载则直接返回</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (c </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    try</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        c </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> parent.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">loadClass</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(name);  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 委托父类加载器</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">catch</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (ClassNotFoundException </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (c </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) c </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> findClass</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(name);  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 父加载器找不到，自己加载</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>设计目的：</strong></p><ol><li><strong>防止核心类被替换</strong>：<code>java.lang.Object</code> 始终由 Bootstrap ClassLoader 加载，自定义的同名类无法替代</li><li><strong>类的唯一性</strong>：同一路径的类只被加载一次（同一类加载器），避免重复加载</li></ol><p><strong>打破双亲委派的合法场景：</strong></p><ol><li><p><strong>SPI（Service Provider Interface）</strong>：<code>java.util.ServiceLoader</code> 需要 Bootstrap ClassLoader 加载的接口，由 AppClassLoader 加载实现类。通过 <strong>Thread Context ClassLoader</strong> 打破：父加载器加载接口，委托给线程上下文类加载器加载实现</p></li><li><p><strong>OSGi / Tomcat</strong>：每个模块/WebApp 有独立类加载器，实现类隔离（不同 WebApp 的同名类互不干扰）。Tomcat 的类加载优先从 WebApp 自己的 <code>/WEB-INF/classes</code> 和 <code>/WEB-INF/lib</code> 加载（先子后父，逆双亲委派）</p></li><li><p><strong>热部署/热更新</strong>：丢弃旧类加载器（JVM 会 GC 掉），创建新类加载器重新加载更新后的类文件</p></li></ol><hr><p><strong>Q11. 解释 Java 内存模型中的 happens-before 关系，并举例说明。</strong></p><p><strong>参考答案：</strong></p><p><strong>happens-before 定义：</strong> 如果操作 A happens-before 操作 B，则 A 的执行结果对 B 可见，且 A 的执行顺序在 B 之前（从可见性和有序性角度）。</p><p><strong>8 条天然 happens-before 规则：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 1. 程序顺序规则：单线程内顺序执行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> a </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// A</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> b </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> a;    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// B — A happens-before B，所以 b 一定是 1</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 2. volatile 规则</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> x </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 线程1: x = 1 (写)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 线程2: int y = x (读，如果读到 y=1，则写 happens-before 读)</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 3. Monitor 锁规则</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">synchronized</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(obj) { count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; }  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// T1 的 unlock</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">synchronized</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(obj) { read count; } </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// T2 的 lock（在 T1 unlock 后）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// T1 的 unlock happens-before T2 的 lock</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 4. 线程启动规则</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// main 线程中设置的变量，t.start() 之后，t 线程能看到这些变量的值</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> x </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 42</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Thread t </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Thread</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> System.out.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">println</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(x)); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 一定打印 42</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">t.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">start</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 5. 线程终止规则</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">t.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">join</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// join() 返回后，t 线程中的所有操作对当前线程可见</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 6. 传递性：A hb B, B hb C → A hb C</span></span></code></pre></div><p><strong>实际应用 — DCL 必须加 volatile 的原因：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">instance </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Singleton</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 底层三步：1.分配内存 2.初始化对象 3.引用赋值</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// JIT 可能重排序为：1.分配内存 3.引用赋值 2.初始化对象</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 若线程 B 在步骤 3 完成后检查 instance != null，以为已初始化</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 实际上步骤 2 还未完成 → 使用了未初始化的对象</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// volatile 保证步骤 3 在步骤 2 之后（StoreStore 屏障）</span></span></code></pre></div><hr><p><strong>Q12. ZGC 相对于 G1 的核心优势是什么？它是如何实现亚毫秒停顿的？</strong></p><p><strong>参考答案：</strong></p><p><strong>ZGC 的设计目标：</strong> 任意大小堆（TB 级）上保持最大停顿时间 &lt; 10ms（通常 &lt; 1ms）。</p><p><strong>核心技术：</strong></p><p><strong>1. 染色指针（Colored Pointers）：</strong> 64 位指针的高位用于存储 GC 元数据（标记位、重定位信息），无需额外内存存储对象状态。ZGC 最多支持 4TB 堆（42位地址）。</p><p><strong>2. 读屏障（Load Barrier）vs 写屏障：</strong> G1 使用写屏障维护 RSet，每次写操作都有额外开销。ZGC 只使用读屏障（<code>load barrier</code>），每次读取对象引用时 JIT 插入一个轻量检查，代价更小，且能实现&quot;自愈&quot;（读到过期指针时自动更新）。</p><p><strong>3. 完全并发的 Mark 和 Relocate：</strong></p><ul><li>G1 的 Remark 和对象复制（Evacuation）需要 STW</li><li>ZGC 几乎所有阶段都是并发的，只有 3 个极短暂的 STW 阶段（各 &lt; 1ms）：初始标记、再标记、初始重定位</li></ul><p><strong>4. Region 大小可变：</strong> ZGC 有小/中/大三种 Region，更灵活地管理不同大小的对象。</p><p><strong>vs G1 的劣势：</strong> ZGC 的 CPU 开销（读屏障）比 G1 高约 5-10%；吞吐量略低于 G1（尤其批处理场景）；JDK 11 可用但 15 才生产就绪。</p><hr><h3 id="⭐⭐⭐-高级题" tabindex="-1">⭐⭐⭐ 高级题 <a class="header-anchor" href="#⭐⭐⭐-高级题" aria-label="Permalink to &quot;⭐⭐⭐ 高级题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q13. 分析以下代码存在的并发问题，并给出修复方案：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> class</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Counter</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count; }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>参考答案：</strong></p><p><strong>并发问题分析：</strong></p><p><code>count++</code> 不是原子操作，字节码层面分三步：</p><ol><li><code>getfield</code>（读取 count 当前值到操作数栈）</li><li><code>iconst_1</code> + <code>iadd</code>（加 1）</li><li><code>putfield</code>（写回）</li></ol><p>两个线程同时执行时，可能同时读到相同的值，然后都写回相同的结果，导致一次 increment 实际上只加了 1（丢失更新）。</p><p><strong>修复方案（4种）：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 方案1：synchronized 方法</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> synchronized</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 方案2：AtomicInteger（推荐，无锁，基于 CAS）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> AtomicInteger count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AtomicInteger</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { count.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">incrementAndGet</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 方案3：LongAdder（高并发写推荐，减少 CAS 竞争）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> LongAdder count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> LongAdder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { count.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> long</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sum</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 方案4：volatile + synchronized（如果 get() 不需要强一致）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> volatile</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> synchronized</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> increment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count; } </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// volatile 保证可见性</span></span></code></pre></div><p><strong>选择建议：</strong> 读多写少 → <code>AtomicInteger</code>；写多 → <code>LongAdder</code>（内部分桶，减少竞争）；复杂逻辑 → <code>synchronized</code>。</p><hr><p><strong>Q14. 描述 CMS 垃圾收集器发生 &quot;Concurrent Mode Failure&quot; 的原因和解决方案。</strong></p><p><strong>参考答案：</strong></p><p><strong>Concurrent Mode Failure 触发条件：</strong></p><p>CMS 并发清除阶段（不 STW，与应用线程并行）期间，应用线程仍在运行，可能：</p><ol><li>将新对象晋升到老年代（Young GC 触发）</li><li>大对象直接在老年代分配</li></ol><p>如果老年代此时剩余空间不足以容纳这些新晋升/分配的对象 → <strong>Concurrent Mode Failure</strong> → 退化为 <strong>Serial Old</strong>（单线程 Full GC，STW），停顿时间可能长达数秒甚至数十秒。</p><p><strong>根本原因：</strong> <code>CMSInitiatingOccupancyFraction</code> 阈值设置过高，CMS 启动太晚，来不及在老年代满之前完成并发清除。</p><p><strong>解决方案：</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 降低 CMS 触发阈值（越早触发，留给并发清除的时间越多）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:CMSInitiatingOccupancyFraction</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">=70</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # 默认 92，降到 70-80</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:+UseCMSInitiatingOccupancyOnly</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # 使用指定阈值，不自适应调整</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 增大老年代（减少晋升压力）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-Xmx8g</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -XX:NewRatio=2</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # Old:Young = 2:1</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3. 减少对象晋升速度（增大 Survivor 区）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:SurvivorRatio</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">=6</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # Eden:Survivor = 6:1（默认 8）</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 4. 根本解决：升级到 G1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:+UseG1GC</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -XX:MaxGCPauseMillis=200</span></span></code></pre></div><hr><p><strong>Q15. 解释 synchronized 锁升级的全过程（偏向锁 → 轻量级锁 → 重量级锁）。</strong></p><p><strong>参考答案：</strong></p><p><strong>HotSpot 对象头 Mark Word（64位）：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>无锁:   [hashCode(31)|0|分代年龄(4)|偏向位(0)|01]</span></span>
<span class="line"><span>偏向锁: [线程ID(54)|Epoch(2)|年龄(4)|偏向位(1)|01]</span></span>
<span class="line"><span>轻量级: [Lock Record 指针(62)                   |00]</span></span>
<span class="line"><span>重量级: [Monitor 指针(62)                        |10]</span></span></code></pre></div><p><strong>升级过程：</strong></p><p><strong>无锁 → 偏向锁：</strong></p><ul><li>JVM 启动 4 秒后（<code>BiasedLockingStartupDelay</code>）开始偏向</li><li>第一个线程获取锁时，CAS 将线程 ID 写入 Mark Word（获得偏向）</li><li>该线程再次进入：只需比较 Mark Word 中的线程 ID，无 CAS（最快路径）</li></ul><p><strong>偏向锁 → 轻量级锁（撤销偏向）：</strong></p><ul><li>另一个线程尝试获取同一偏向锁 → 需要 STW（安全点）撤销偏向</li><li>撤销时：原持有线程仍在使用 → 升级为轻量级锁；否则设为无锁</li></ul><p><strong>轻量级锁：</strong></p><ul><li>线程在栈帧创建 Lock Record，CAS 将 Mark Word 中的内容替换为 Lock Record 指针</li><li>成功 → 获得轻量级锁；失败（竞争）→ 自旋等待</li><li>自旋若干次（自适应自旋，默认 10 次）仍失败 → 膨胀为重量级锁</li></ul><p><strong>重量级锁：</strong></p><ul><li>在堆中创建 Monitor 对象（内含 OS 互斥量 mutex）</li><li>未获得锁的线程进入 EntryList（阻塞，不消耗 CPU）</li><li>释放时唤醒 EntryList 中的线程</li></ul><p><strong>JDK 15 起默认禁用偏向锁</strong>（偏向锁撤销需要 STW，在高并发场景下 STW 代价超过偏向收益）。</p><hr><p><strong>Q16. 什么是 ThreadLocal？它如何工作？如何避免内存泄漏？</strong></p><p><strong>参考答案：</strong></p><p><strong>ThreadLocal 作用：</strong> 为每个线程提供独立的变量副本，线程间互不干扰（无需同步）。</p><p><strong>实现原理：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ThreadLocal.set(value) 内部实现：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(T value) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Thread t </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Thread.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">currentThread</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    ThreadLocalMap map </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> t.threadLocals;  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 每个 Thread 对象内的 Map</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (map </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        map.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">this</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, value);             </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// key = ThreadLocal 实例（弱引用）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    else</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">        createMap</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(t, value);              </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// value = 存储的值（强引用）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>关键设计：</strong> 数据存储在 <code>Thread.threadLocals</code>（<code>ThreadLocalMap</code>）中，而不是 <code>ThreadLocal</code> 对象中。<code>ThreadLocalMap</code> 的 key 是 <code>ThreadLocal</code> 对象的<strong>弱引用</strong>。</p><p><strong>内存泄漏风险：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread → ThreadLocalMap → Entry[key(弱引用)=ThreadLocal, value(强引用)=T]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>当 ThreadLocal 对象被 GC 回收（弱引用被清除）后：</span></span>
<span class="line"><span>  key = null（已被 GC）</span></span>
<span class="line"><span>  value = T（仍然存活！因为强引用）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>如果 Thread 不死（线程池的核心线程），value 就永远不会被回收 → 内存泄漏</span></span></code></pre></div><p><strong>正确使用姿势：</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> static</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> final</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ThreadLocal&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">Connection</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt; conn </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ThreadLocal.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">withInitial</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(...);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">try</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    conn.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">getConnection</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">());</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    doWork</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">} </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">finally</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    conn.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">remove</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// !! 必须手动 remove，否则线程池场景下内存泄漏</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>最佳实践：</strong> 在 <code>try-finally</code> 中保证 <code>ThreadLocal.remove()</code> 被调用，尤其是在线程池中（线程复用，不 remove 上一次的 value 会泄漏到下次请求）。</p><hr><p><strong>Q17. 如何排查 Java 应用的内存溢出（OOM）问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>步骤 1：确认 OOM 类型</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OutOfMemoryError: Java heap space     → 堆空间不足（最常见）</span></span>
<span class="line"><span>OutOfMemoryError: Metaspace           → 方法区/元空间不足（类加载太多）</span></span>
<span class="line"><span>OutOfMemoryError: GC overhead exceeded → GC 时间占比 &gt; 98%（对象创建太快）</span></span>
<span class="line"><span>OutOfMemoryError: Direct buffer memory → NIO Direct Buffer 用完</span></span>
<span class="line"><span>StackOverflowError                     → 线程栈溢出（递归太深）</span></span></code></pre></div><p><strong>步骤 2：采集 Heap Dump</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式1：OOM 时自动 dump</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:+HeapDumpOnOutOfMemoryError</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -XX:HeapDumpPath=/tmp/heapdump.hprof</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式2：手动 dump（进程仍在运行时）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">jmap</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -dump:format=b,file=/tmp/heapdump.hprof</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式3：jcmd</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">jcmd</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> VM.heap_dump</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /tmp/heapdump.hprof</span></span></code></pre></div><p><strong>步骤 3：分析 Heap Dump</strong></p><p>工具：MAT（Eclipse Memory Analyzer）、VisualVM、JProfiler</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>在 MAT 中：</span></span>
<span class="line"><span>1. 打开 Heap Dump → Leak Suspects Report（自动找内存泄漏嫌疑）</span></span>
<span class="line"><span>2. 查看 Dominator Tree（占用最多内存的对象树）</span></span>
<span class="line"><span>3. 查看 Histogram（各类实例数量和内存占用）</span></span>
<span class="line"><span>4. 找到占用最大的对象，追踪其 GC Root 引用链（Paths to GC Roots）</span></span></code></pre></div><p><strong>常见内存泄漏模式：</strong></p><ul><li>静态 <code>Map/List</code> 持续增长，未设大小限制</li><li><code>ThreadLocal</code> 未 remove</li><li>监听器/回调未注销（Observer 模式）</li><li>Connection/Stream 未关闭（try-with-resources）</li><li>内部类持有外部类引用（匿名类/Lambda）</li></ul><hr><p><strong>Q18. 什么是 JIT 即时编译？JVM 是如何决定什么代码值得 JIT 编译的？</strong></p><p><strong>参考答案：</strong></p><p><strong>JIT（Just-In-Time Compilation）：</strong> JVM 启动时解释执行字节码（慢），运行过程中识别热点代码并编译为机器码（快），后续直接执行机器码。</p><p><strong>热点探测：</strong></p><p>HotSpot JVM 通过两种计数器确定热点代码：</p><ol><li><strong>方法调用计数器</strong>（<code>invocation_counter</code>）：方法被调用的次数</li><li><strong>循环回边计数器</strong>（<code>backedge_counter</code>）：方法体内循环被执行的次数（用于 OSR，On-Stack Replacement）</li></ol><p>阈值由 <code>-XX:CompileThreshold</code> 控制（Client 模式 1500，Server 模式 10000）。计数器有&quot;热度衰减&quot;机制：超过一段时间（<code>-XX:CounterHalfLifeTime</code>）减半，避免过去热的代码被永久 JIT。</p><p><strong>编译层次（Tiered Compilation，JDK 7+ 默认）：</strong></p><table tabindex="0"><thead><tr><th>层次</th><th>说明</th></tr></thead><tbody><tr><td>Level 0</td><td>解释执行</td></tr><tr><td>Level 1</td><td>C1 简单编译（无 profiling）</td></tr><tr><td>Level 2</td><td>C1 编译（有限 profiling）</td></tr><tr><td>Level 3</td><td>C1 完整编译（完整 profiling）</td></tr><tr><td>Level 4</td><td>C2 优化编译（激进优化）</td></tr></tbody></table><p><strong>JIT 的关键优化：</strong></p><ul><li>内联（Inlining）：将小方法直接嵌入调用处（消除方法调用开销）</li><li>逃逸分析（Escape Analysis）：未逃逸的对象分配在栈上（无 GC 压力）</li><li>锁消除（Lock Elimination）：未逃逸的同步块消除锁操作</li><li>循环展开（Loop Unrolling）：减少循环分支判断</li></ul><hr><p><strong>Q19. 如何分析和解决 Java 应用的 CPU 使用率过高问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>步骤 1：找到 CPU 占用最高的线程</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Linux: 找到进程总 CPU</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">top</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> grep</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> java</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 找到进程内 CPU 最高的线程（TID）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">top</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # H=显示线程, p=指定进程</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 或使用 ps</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ps</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -mp</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -o</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> THREAD,tid,time</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sort</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -rn</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> head</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -20</span></span></code></pre></div><p><strong>步骤 2：将线程 TID 转换为 16 进制</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">printf</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;%x\\n&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ti</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 十进制 TID → 16 进制（用于 jstack 中查找）</span></span></code></pre></div><p><strong>步骤 3：获取线程栈信息</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">jstack</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -l</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /tmp/thread_dump.txt</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 在 thread_dump.txt 中搜索上一步得到的 16 进制 TID（如 nid=0x1234）</span></span></code></pre></div><p><strong>步骤 4：分析栈帧</strong></p><p><strong>常见 CPU 高场景：</strong></p><table tabindex="0"><thead><tr><th>栈顶帧</th><th>可能原因</th></tr></thead><tbody><tr><td><code>java.util.HashMap.put</code></td><td>死循环（多线程并发修改 HashMap，JDK 7 resize 死循环）</td></tr><tr><td>GC 线程 CPU 高</td><td>内存不足、GC 频繁</td></tr><tr><td><code>sun.nio.ch.EPoll*</code></td><td>epoll 空轮询 Bug（旧版 Netty/JDK 问题）</td></tr><tr><td>业务逻辑循环</td><td>死循环 Bug 或正常高计算</td></tr><tr><td><code>String.intern()</code></td><td>大量字符串 intern 导致常量池竞争</td></tr></tbody></table><p><strong>步骤 5：使用 async-profiler 采样分析</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 采样 30 秒，生成火焰图</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./profiler.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 30</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /tmp/flamegraph.svg</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 火焰图中宽度最大的帧 = CPU 热点</span></span></code></pre></div><hr><p><strong>Q20. 什么是逃逸分析？它如何影响对象分配策略？</strong></p><p><strong>参考答案：</strong></p><p><strong>逃逸分析（Escape Analysis）：</strong> JIT 编译器分析一个对象是否&quot;逃逸&quot;出其创建的方法或线程范围。</p><p><strong>三种逃逸状态：</strong></p><ol><li><strong>无逃逸（No Escape）：</strong> 对象只在方法内部使用，方法返回后对象不可达</li><li><strong>方法逃逸（Method Escape）：</strong> 对象被作为返回值或参数传递到其他方法</li><li><strong>线程逃逸（Thread Escape）：</strong> 对象被赋值给全局变量或可被其他线程访问的字段</li></ol><p><strong>逃逸分析的优化：</strong></p><p><strong>1. 栈上分配（Stack Allocation）：</strong> 无逃逸对象分配在线程栈上（而非堆），方法返回时自动回收，无 GC 压力</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> foo</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // Point 未逃逸，可能被 JIT 分配在栈上</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Point p </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Point</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    System.out.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">println</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(p.x </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> p.y);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>2. 标量替换（Scalar Replacement）：</strong> 无逃逸对象被分解为各个字段（标量），直接存储在寄存器/栈上，对象本身不在堆上创建</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Point p </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Point</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// JIT 可能优化为：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> p_x </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> p_y </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><strong>3. 锁消除（Lock Elimination）：</strong> 无线程逃逸的 synchronized 块中的锁被消除</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> String </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">concat</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(String s1, String s2) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // StringBuffer 未逃逸，JIT 会消除其内部 synchronized</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    StringBuffer sb </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> StringBuffer</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    sb.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">append</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(s1).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">append</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(s2);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> sb.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">toString</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>启用参数：</strong> JDK 8+ 默认开启（<code>-XX:+DoEscapeAnalysis</code>，<code>-XX:+EliminateLocks</code>）。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("02-面试指南/02-后端面试/01-Java核心面试题.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _01Java_____ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _01Java_____ as default
};
