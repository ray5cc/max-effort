import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"PostgreSQL 面试题","description":"","frontmatter":{},"headers":[],"relativePath":"02-面试指南/03-数据库面试/03-PostgreSQL面试题.md","filePath":"02-面试指南/03-数据库面试/03-PostgreSQL面试题.md","lastUpdated":1774066204000}');
const _sfc_main = { name: "02-面试指南/03-数据库面试/03-PostgreSQL面试题.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="postgresql-面试题" tabindex="-1">PostgreSQL 面试题 <a class="header-anchor" href="#postgresql-面试题" aria-label="Permalink to &quot;PostgreSQL 面试题&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>基于 PostgreSQL 源码（<code>postgres/postgres</code>）的分层面试题，涵盖进程架构、存储引擎、MVCC、WAL、查询执行器与 VACUUM 核心机制。</p></blockquote><h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to &quot;相关链接&quot;">&amp;ZeroWidthSpace;</a></h2><ul><li>对应技术资料：<a href="./../../01-技术资料/03-数据库/03-PostgreSQL核心技术.html">PostgreSQL 核心技术</a></li></ul><h2 id="题目列表" tabindex="-1">题目列表 <a class="header-anchor" href="#题目列表" aria-label="Permalink to &quot;题目列表&quot;">&amp;ZeroWidthSpace;</a></h2><hr><h2 id="🔥-高频考点速记" tabindex="-1">🔥 高频考点速记 <a class="header-anchor" href="#🔥-高频考点速记" aria-label="Permalink to &quot;🔥 高频考点速记&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>面试中最常被问到的核心知识点，按出现频率排序。建议优先掌握前 5 项。</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>考点</th><th>核心要点（一句话）</th><th>出题概率</th></tr></thead><tbody><tr><td>1</td><td>MVCC 与可见性判断</td><td>xmin/xmax + 快照比较，读写不阻塞</td><td>★★★★★</td></tr><tr><td>2</td><td>WAL 与持久性</td><td>Write-Ahead Logging，先写日志再写数据</td><td>★★★★★</td></tr><tr><td>3</td><td>B-tree 索引实现</td><td>Lehman &amp; Yao 并发安全，High Key 优化</td><td>★★★★☆</td></tr><tr><td>4</td><td>VACUUM 与膨胀</td><td>清理死元组，autovacuum 调优，VACUUM FULL 重建</td><td>★★★★★</td></tr><tr><td>5</td><td>事务隔离级别</td><td>RC/RR/Serializable，SSI 代替 2PL</td><td>★★★★☆</td></tr><tr><td>6</td><td>执行计划分析</td><td>EXPLAIN ANALYZE，Sequential vs Index Scan 决策</td><td>★★★★☆</td></tr><tr><td>7</td><td>流式复制</td><td>WAL Sender/Receiver，同步/异步模式</td><td>★★★☆☆</td></tr><tr><td>8</td><td>堆表页面布局</td><td>8KB page，ItemId 数组+元组数据</td><td>★★★☆☆</td></tr><tr><td>9</td><td>HOT 更新</td><td>同页面更新免索引更新，减少膨胀</td><td>★★★☆☆</td></tr><tr><td>10</td><td>XID Wraparound</td><td>42亿事务ID上限，aggressive VACUUM 防范</td><td>★★★☆☆</td></tr></tbody></table><hr><h3 id="⭐-基础题" tabindex="-1">⭐ 基础题 <a class="header-anchor" href="#⭐-基础题" aria-label="Permalink to &quot;⭐ 基础题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q1. PostgreSQL 采用什么进程模型？与 MySQL 的线程模型相比有何优缺点？</strong></p><p><strong>参考答案：</strong></p><p>PostgreSQL 采用<strong>多进程模型</strong>：每个客户端连接对应一个独立的 Backend 进程（由 Postmaster fork 创建）。</p><p><strong>优点：</strong></p><ul><li>进程间天然内存隔离，一个 Backend 崩溃不会影响其他连接或主进程</li><li>利用 OS 进程管理机制，简化了崩溃恢复逻辑</li><li>安全性更好：进程间无法共享内存地址空间</li></ul><p><strong>缺点：</strong></p><ul><li>每个连接开销较大（fork 代价、进程上下文切换）</li><li>默认最大连接数（<code>max_connections</code>）通常设为 100-500，超高并发场景需要 PgBouncer 等连接池</li><li>每个进程独占自己的 CPU 缓存，相比线程模型有更多缓存 miss</li></ul><p>MySQL 使用一连接一线程模型，线程共享内存空间，创建代价低，但线程崩溃可能影响整个进程。</p><p><strong>关键知识点：</strong> <code>src/backend/postmaster/postmaster.c</code> 的 <code>BackendStartup</code> → <code>fork_process()</code> → <code>BackendRun()</code>。</p><hr><p><strong>Q2. 解释 PostgreSQL 中什么是 WAL，它解决了什么问题？</strong></p><p><strong>参考答案：</strong></p><p>WAL（Write-Ahead Logging，预写日志）是 PostgreSQL 实现 ACID 中<strong>持久性（Durability）<strong>和</strong>崩溃恢复</strong>的核心机制。</p><p><strong>核心原则：</strong> 任何数据页修改写入磁盘之前，必须先将对应的 WAL 记录持久化到 WAL 文件。</p><p><strong>解决的问题：</strong></p><ol><li><strong>崩溃恢复</strong>：崩溃后重启时，从最近 Checkpoint 位置重放 WAL 记录，将数据库恢复到崩溃前的一致状态</li><li><strong>性能</strong>：WAL 是顺序追加写（比随机 I/O 高效得多），数据页可以延迟异步刷盘</li><li><strong>流式复制</strong>：主库将 WAL 流式传输给备库，备库通过重放 WAL 保持与主库同步</li></ol><p><strong>LSN（Log Sequence Number）</strong>：WAL 的字节偏移量，用于标识 WAL 位置，每个数据页在其头部存储最后修改时的 LSN。</p><hr><p><strong>Q3. PostgreSQL 的 B-tree 索引与 Hash 索引的区别是什么？各自适用什么场景？</strong></p><p><strong>参考答案：</strong></p><table tabindex="0"><thead><tr><th></th><th>B-tree</th><th>Hash</th></tr></thead><tbody><tr><td>支持操作</td><td><code>=</code>, <code>&lt;</code>, <code>&gt;</code>, <code>BETWEEN</code>, <code>LIKE &#39;x%&#39;</code>, <code>ORDER BY</code></td><td>仅 <code>=</code></td></tr><tr><td>结构</td><td>平衡树（O(log n) 查找）</td><td>哈希表（O(1) 等值查找）</td></tr><tr><td>范围查询</td><td>✓（叶子节点双向链表）</td><td>✗</td></tr><tr><td>排序支持</td><td>✓（索引有序）</td><td>✗</td></tr><tr><td>大小</td><td>中等</td><td>相对小</td></tr><tr><td>WAL 持久化</td><td>✓</td><td>✓（PG 10+，之前不记录 WAL）</td></tr></tbody></table><p><strong>适用场景：</strong></p><ul><li>B-tree：99% 的场景默认选择，支持最广泛的操作符</li><li>Hash：仅纯等值查询的大表，且不需要排序或范围（实际很少使用，B-tree 一般够用）</li></ul><hr><p><strong>Q4. 什么是 VACUUM？为什么 PostgreSQL 需要定期执行 VACUUM？</strong></p><p><strong>参考答案：</strong></p><p><strong>背景：</strong> PostgreSQL 的 MVCC 实现通过保留旧版本 tuple（而非原地修改）来支持多版本并发读。UPDATE/DELETE 不会立即删除旧数据，而是将其标记为死元组（dead tuple）。</p><p><strong>VACUUM 的核心工作：</strong></p><ol><li><strong>回收死元组</strong>：将 <code>t_xmax</code> 已提交的死 tuple 标记为可复用，释放空间给后续插入</li><li><strong>更新 Visibility Map（VM）</strong>：标记页面内所有 tuple 对所有事务可见，供 Index Only Scan 优化</li><li><strong>更新 Free Space Map（FSM）</strong>：记录各页剩余空间，供 INSERT 快速定位合适页面</li><li><strong>冻结旧 XID</strong>：防止 XID Wraparound 问题（32位 XID 约42亿上限）</li></ol><p><strong>不执行 VACUUM 的后果：</strong></p><ul><li>表空间不断膨胀（bloat）</li><li>查询需要扫描大量死元组，性能下降</li><li>XID 年龄达到阈值时数据库拒绝连接，强制进入维护模式</li></ul><hr><p><strong>Q5. 解释 PostgreSQL 的事务隔离级别以及默认级别是什么？</strong></p><p><strong>参考答案：</strong></p><p>PostgreSQL 支持 4 种隔离级别（READ UNCOMMITTED 实现等同 READ COMMITTED）：</p><table tabindex="0"><thead><tr><th>隔离级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th><th>实现方式</th></tr></thead><tbody><tr><td>READ COMMITTED（<strong>默认</strong>）</td><td>否</td><td>可能</td><td>可能</td><td>每条语句获取新快照</td></tr><tr><td>REPEATABLE READ</td><td>否</td><td>否</td><td>否*</td><td>事务开始时获取快照</td></tr><tr><td>SERIALIZABLE</td><td>否</td><td>否</td><td>否</td><td>SSI + 谓词锁</td></tr></tbody></table><p>*PostgreSQL 的 REPEATABLE READ 通过 MVCC 快照天然避免幻读（不同于 MySQL 用 Gap Lock）。</p><p><strong>默认级别 READ COMMITTED</strong> 适合大多数 OLTP 场景：读写不互相阻塞，并发性好。</p><p><strong>SERIALIZABLE</strong> 使用 SSI（Serializable Snapshot Isolation），通过检测事务间的 rw-anti-dependency 环来发现序列化冲突，不会产生大量锁。</p><hr><p><strong>Q6. PostgreSQL 中 <code>pg_stat_activity</code> 视图能告诉我们什么？</strong></p><p><strong>参考答案：</strong></p><p><code>pg_stat_activity</code> 是 DBA 最常用的监控视图，每行代表一个活跃连接/后台进程：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pid, usename, application_name, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">state</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, wait_event_type, wait_event, query</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_activity</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> state</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;idle&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><strong>关键字段：</strong></p><ul><li><code>pid</code>：Backend 进程 ID</li><li><code>state</code>：<code>active</code>（正在执行）/ <code>idle</code>（空闲）/ <code>idle in transaction</code>（事务中等待）/ <code>idle in transaction (aborted)</code></li><li><code>wait_event_type/wait_event</code>：等待原因（Lock/BufferPin/IO/Client 等）</li><li><code>query</code>：当前/最近执行的 SQL</li><li><code>query_start</code>：查询开始时间（可计算执行时长）</li><li><code>xact_start</code>：事务开始时间（<code>idle in transaction</code> + 时间长 → 需要关注）</li></ul><hr><h3 id="⭐⭐-进阶题" tabindex="-1">⭐⭐ 进阶题 <a class="header-anchor" href="#⭐⭐-进阶题" aria-label="Permalink to &quot;⭐⭐ 进阶题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q7. 解释 HOT（Heap Only Tuple）更新优化的原理和条件。</strong></p><p><strong>参考答案：</strong></p><p><strong>问题背景：</strong> 普通 UPDATE 产生新版本 tuple 时，会更新所有关联索引（即使被更新的列不在索引中）。高频 UPDATE 导致索引膨胀，I/O 增加。</p><p><strong>HOT 条件（两个条件必须同时满足）：</strong></p><ol><li>被更新的列<strong>不在任何索引</strong>中</li><li>新 tuple 与旧 tuple 在<strong>同一个数据页</strong>中（页内有足够空闲空间）</li></ol><p><strong>HOT 的工作原理：</strong></p><ul><li>新 tuple 在旧 tuple 的 <code>t_ctid</code> 字段中建立指针链（旧→新）</li><li>旧 tuple 的 <code>t_infomask2</code> 设置 <code>HEAP_HOT_UPDATED</code> 标志</li><li>新 tuple 设置 <code>HEAP_ONLY_TUPLE</code> 标志，表示它没有独立的索引条目</li><li>索引条目<strong>不变</strong>，扫描时跟随 <code>t_ctid</code> 链找到最新版本</li></ul><p><strong>收益：</strong> 索引条目数量保持不变，避免索引膨胀，减少索引页 I/O。</p><p><strong>VACUUM 的作用：</strong> <code>heap_page_prune()</code> 函数定期清理 HOT 链中已死亡的旧版本，维护链的紧凑性。</p><hr><p><strong>Q8. 描述 PostgreSQL MVCC 中可见性判断的完整流程。</strong></p><p><strong>参考答案：</strong></p><p>可见性判断的核心函数是 <code>HeapTupleSatisfiesMVCC</code>（<code>src/backend/access/heap/heapam_visibility.c</code>）。</p><p><strong>判断流程：</strong></p><p><strong>步骤 1：检查 <code>t_xmin</code>（谁插入了这个版本）</strong></p><ul><li><code>t_xmin == InvalidXID</code>：tuple 未完成插入 → 不可见</li><li><code>t_xmin</code> 的事务已中止（查 pg_clog）→ 不可见</li><li><code>t_xmin</code> &gt; 快照的 <code>xmax</code>（快照后才开始的事务）→ 不可见</li><li><code>t_xmin</code> 在快照的 <code>xip</code> 中（快照时仍活跃）→ 不可见</li><li>否则（快照前已提交）→ 通过 t_xmin 检查，继续检查 t_xmax</li></ul><p><strong>步骤 2：检查 <code>t_xmax</code>（谁删除/更新了这个版本）</strong></p><ul><li><code>t_xmax == 0</code>：未被删除 → <strong>可见</strong></li><li><code>t_xmax</code> 的事务已中止 → 删除被回滚 → <strong>可见</strong></li><li><code>t_xmax</code> 在快照的 <code>xip</code> 中（快照时仍活跃）→ 删除未提交 → <strong>可见</strong></li><li><code>t_xmax</code> &gt; 快照的 <code>xmax</code> → 快照后才删除 → <strong>可见</strong></li><li>否则（删除已提交且在快照前）→ <strong>不可见</strong></li></ul><p><strong>优化：</strong> <code>t_infomask</code> 缓存事务提交/中止状态（<code>HEAP_XMIN_COMMITTED</code>, <code>HEAP_XMAX_ABORTED</code> 等标志），避免重复查询 pg_clog（SLRU），显著减少 I/O。</p><hr><p><strong>Q9. PostgreSQL 如何实现流式复制？主库宕机后如何进行故障切换？</strong></p><p><strong>参考答案：</strong></p><p><strong>流式复制架构：</strong></p><ul><li>主库：WAL Sender 进程读取 WAL 流，通过 TCP 发送给备库</li><li>备库：WAL Receiver 进程接收 WAL 记录，写入本地 WAL 文件，startup 进程持续回放</li></ul><p><strong>WAL Sender 核心逻辑：</strong></p><ol><li>从 <code>pg_wal/</code> 目录读取待发送 WAL（当前位置到最新写入 LSN）</li><li>通过 <code>pq_sendmessage</code> 发送 <code>XLogData</code> 消息</li><li>接收备库的 <code>StandbyStatusUpdate</code>（汇报 <code>write_lsn / flush_lsn / apply_lsn</code>）</li></ol><p><strong>同步复制（<code>synchronous_commit = on</code>）：</strong> 主库在 COMMIT 时等待至少一个同步备库确认 <code>flush_lsn &gt;= commit_lsn</code> 后才返回成功。</p><p><strong>故障切换（Failover）流程：</strong></p><ol><li>检测主库故障（<code>pg_is_in_recovery()</code> + 连接超时）</li><li>确认备库已接收尽可能多的 WAL（选取 <code>apply_lsn</code> 最新的备库）</li><li>在备库执行 <code>pg_promote()</code> 或创建 <code>recovery.signal</code> 后重启</li><li>备库退出恢复模式，成为新主库</li><li>其他备库更新 <code>primary_conninfo</code> 指向新主库，执行 <code>pg_rewind</code> 对齐分叉点</li></ol><hr><p><strong>Q10. 解释 Checkpoint 的作用、触发条件和 <code>checkpoint_completion_target</code> 的意义。</strong></p><p><strong>参考答案：</strong></p><p><strong>Checkpoint 的作用：</strong></p><ul><li>将内存（shared_buffers）中所有脏页强制刷写到磁盘</li><li>在 WAL 中写入 checkpoint 记录，作为崩溃恢复的起始点</li><li>推进 <code>last_checkpoint_lsn</code>，允许旧 WAL 文件被回收/覆盖</li></ul><p><strong>触发条件（满足任一）：</strong></p><ul><li><code>checkpoint_timeout</code>（默认 5 分钟）时间到达</li><li>WAL 积累量超过 <code>max_wal_size</code>（默认 1GB）</li><li><code>CHECKPOINT</code> 命令手动触发</li><li><code>pg_ctl stop</code> / 关闭数据库时</li></ul><p><strong><code>checkpoint_completion_target</code>（默认 0.9）的意义：</strong></p><p>如果两次 checkpoint 间隔为 T 秒，PostgreSQL 的目标是在 <code>T * 0.9</code> 秒内<strong>均匀地</strong>完成本次 checkpoint 的所有脏页刷盘。这样可以避免在 checkpoint 触发时刻产生 I/O 突刺，将 I/O 压力平摊到整个 checkpoint 间隔期间。</p><p><strong>调优建议：</strong></p><ul><li>增大 <code>max_wal_size</code> 和 <code>checkpoint_timeout</code>，延长 checkpoint 间隔，减少 checkpoint 频率</li><li><code>checkpoint_completion_target = 0.9</code> 通常是好的默认值</li><li>监控 <code>pg_stat_bgwriter</code> 中的 <code>checkpoints_req</code>（触发量 checkpoint）vs <code>checkpoints_timed</code>（超时 checkpoint），前者多说明写入量大需增大 <code>max_wal_size</code></li></ul><hr><p><strong>Q11. 解释 PostgreSQL B-tree 索引中的 High Key 概念及其作用。</strong></p><p><strong>参考答案：</strong></p><p>PostgreSQL B-tree 实现来自 Lehman &amp; Yao 1981 年论文，<strong>High Key</strong> 是其核心设计之一。</p><p><strong>High Key 定义：</strong> 每个非最右叶子页面的<strong>第一个 line pointer</strong> 指向该页的 High Key——即该页所能存储的最大键值（或包含最大值的键）。</p><p><strong>High Key 的作用（支持并发安全的页面分裂）：</strong></p><p>在并发环境下，一个线程可能在另一个线程修改 B-tree 时进行查找：</p><ol><li>线程 A 正在读叶子页 P，找到目标 key 所在位置</li><li>线程 B 同时对页 P 进行分裂，将部分键值移到新页 Q，并更新 P 的 High Key</li></ol><p>读线程 A 在持有页 P 读锁后，通过比较目标 key 与 P 的 High Key：</p><ul><li>若 key &lt;= High Key：目标在本页或已被正确处理</li><li>若 key &gt; High Key：目标已移到右兄弟页 → 通过 <code>btpo_next</code> 指针<strong>向右移动</strong>（不需要从根重新开始）</li></ul><p>这种&quot;右移&quot;机制（rightlink）使得并发查找不需要持有全树锁，大幅提高并发性。</p><hr><p><strong>Q12. 什么是 SSI（Serializable Snapshot Isolation）？它与传统的基于锁的串行化有何不同？</strong></p><p><strong>参考答案：</strong></p><p><strong>SSI 原理（Michael J. Cahill 论文）：</strong></p><p>SSI 通过检测<strong>可串行化异常</strong>来实现真正的串行化隔离，而非用锁预防所有并发。</p><p>核心思想：任何非串行化的并发执行都可以表示为事务图中存在 <strong>rw-anti-dependency 环</strong>：</p><ul><li>T1 读了某数据 → T2 对该数据执行了写（T1 的快照无法看到 T2 的写）= <code>T1 -rw→ T2</code></li><li>T2 读了某数据 → T1 对该数据执行了写 = <code>T2 -rw→ T1</code></li><li>两者形成环 → 无法串行化</li></ul><p><strong>实现：</strong></p><ul><li>每个事务维护<strong>谓词锁（Predicate Lock）</strong>，记录已读取的数据范围</li><li>写操作时检测是否与其他事务的谓词锁冲突</li><li>检测到 rw-anti-dependency 环 → 中止代价小的事务（<code>ERROR: could not serialize access due to read/write dependencies</code>）</li></ul><p><strong>vs 基于锁的串行化（如 2PL）：</strong></p><table tabindex="0"><thead><tr><th></th><th>SSI</th><th>两阶段锁（2PL）</th></tr></thead><tbody><tr><td>并发性</td><td>高（大多数情况无锁）</td><td>低（读写相互阻塞）</td></tr><tr><td>死锁</td><td>可能（abort 代替）</td><td>可能</td></tr><tr><td>读阻塞写</td><td>否</td><td>是</td></tr><tr><td>实现复杂度</td><td>高</td><td>相对简单</td></tr></tbody></table><hr><h3 id="⭐⭐⭐-高级题" tabindex="-1">⭐⭐⭐ 高级题 <a class="header-anchor" href="#⭐⭐⭐-高级题" aria-label="Permalink to &quot;⭐⭐⭐ 高级题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q13. 描述 PostgreSQL 堆页面的内部布局，包括 <code>PageHeaderData</code>、ItemId 数组和 Tuple 数据的位置关系。</strong></p><p><strong>参考答案：</strong></p><p>PostgreSQL 每个 8KB 页面（block）的内存布局如下（从低地址到高地址）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>偏移 0:   PageHeaderData (24 bytes)</span></span>
<span class="line"><span>          - pd_lsn (8B): 最后修改此页的 WAL LSN</span></span>
<span class="line"><span>          - pd_checksum (2B): 页面校验和</span></span>
<span class="line"><span>          - pd_flags (2B): PD_HAS_FREE_LINES, PD_PAGE_FULL, PD_ALL_VISIBLE</span></span>
<span class="line"><span>          - pd_lower (2B): ItemId 区域末尾（新 ItemId 从这里向右增长）</span></span>
<span class="line"><span>          - pd_upper (2B): Tuple 区域开始（新 Tuple 从这里向左增长）</span></span>
<span class="line"><span>          - pd_special (2B): Special 区域开始（堆表=页尾，索引页另有用途）</span></span>
<span class="line"><span>          - pd_pagesize_version (2B): 页大小和格式版本</span></span>
<span class="line"><span>          - pd_prune_xid (4B): 用于判断是否可 prune 的最小 XID</span></span>
<span class="line"><span></span></span>
<span class="line"><span>偏移 24+: ItemId 数组（每个 4 bytes，向高地址增长）</span></span>
<span class="line"><span>          - lp_off (15bit): Tuple 在页内的偏移</span></span>
<span class="line"><span>          - lp_flags (2bit): LP_UNUSED/LP_NORMAL/LP_REDIRECT/LP_DEAD</span></span>
<span class="line"><span>          - lp_len (15bit): Tuple 长度</span></span>
<span class="line"><span></span></span>
<span class="line"><span>(空闲空间，pd_lower 到 pd_upper 之间)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>高地址:   Tuple 数据（向低地址增长）</span></span>
<span class="line"><span>          - 每个 Tuple = HeapTupleHeaderData + 用户数据</span></span>
<span class="line"><span>          - HeapTupleHeaderData 含 t_xmin/t_xmax/t_ctid/infomask</span></span>
<span class="line"><span></span></span>
<span class="line"><span>页尾:     Special 区域（堆表为空，B-tree 存 BTPageOpaqueData）</span></span></code></pre></div><p><strong>关键点：</strong></p><ul><li><code>FreeSpace = pd_upper - pd_lower</code></li><li>ItemId 是&quot;间接层&quot;，允许页内 Tuple 移动（HOT 链）而不影响索引</li><li><code>pd_all_visible</code> 位设置后，IndexOnlyScan 可跳过堆访问直接返回索引数据</li></ul><hr><p><strong>Q14. 什么是 XID Wraparound？PostgreSQL 如何防止它？</strong></p><p><strong>参考答案：</strong></p><p><strong>问题根源：</strong> PostgreSQL 的 XID 是 32 位无符号整数（约 42 亿），可见性比较通过模运算实现（每个 XID 只能&quot;看见&quot;比它小约 21 亿和&quot;看不见&quot;比它大约 21 亿的事务）。</p><p><strong>Wraparound 危险场景：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>假设某 tuple 的 t_xmin = 5（非常古老的事务）</span></span>
<span class="line"><span>当前 XID = 2,000,000,000：5 在&quot;过去&quot;，tuple 可见 ✓</span></span>
<span class="line"><span></span></span>
<span class="line"><span>当 XID 增长到 2,500,000,000：</span></span>
<span class="line"><span>  2,500,000,000 - 5 = 2,499,999,995 &gt; 2^31</span></span>
<span class="line"><span>  从模运算视角看，XID=5 现在在&quot;未来&quot;！</span></span>
<span class="line"><span>  → tuple 突然变为不可见！ → 数据丢失！</span></span></code></pre></div><p><strong>防范机制：VACUUM FREEZE</strong></p><p>VACUUM 将年龄超过 <code>vacuum_freeze_min_age</code>（默认 5000万）的 tuple 的 <code>t_xmin</code> 替换为特殊的 <code>FrozenTransactionId</code>（XID=2）。冻结后的 tuple 对所有现在和将来的事务<strong>永久可见</strong>，不参与 wraparound 计算。</p><p><strong>关键参数：</strong></p><ul><li><code>vacuum_freeze_min_age = 50,000,000</code>：低于此年龄不冻结（保留 MVCC 历史）</li><li><code>vacuum_freeze_table_age = 150,000,000</code>：表 relfrozenxid 年龄超过此值，强制全表冻结扫描</li><li><code>autovacuum_freeze_max_age = 200,000,000</code>：超过此值必须触发 autovacuum 冻结</li></ul><p><strong>应急查询：</strong></p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> datname, age(datfrozenxid) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> age,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">       2147483647</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> -</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> age(datfrozenxid) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> xids_remaining</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_database</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> age </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- age &gt; 1.5亿 开始告警，&gt; 2亿 强制维护</span></span></code></pre></div><hr><p><strong>Q15. 在什么情况下应该选择 BRIN 索引而不是 B-tree 索引？</strong></p><p><strong>参考答案：</strong></p><p><strong>BRIN（Block Range Index）</strong> 的工作原理：</p><ul><li>将表按<strong>块范围</strong>（默认 128 页）分组，每组只存储该范围内列值的 min/max</li><li>查询时，BRIN 找出 min/max 范围与查询条件重叠的块范围，只扫描这些块</li></ul><p><strong>选择 BRIN 的条件（同时满足）：</strong></p><ol><li><strong>表非常大</strong>（数亿行以上）且空间紧张，无法承受 B-tree 索引大小</li><li><strong>列值与物理存储顺序高度相关</strong>（即相关性 <code>correlation</code> 接近 1）</li><li>查询模式是<strong>范围查询</strong>，且允许较低的精度（BRIN 只能做粗粒度过滤）</li></ol><p><strong>典型场景：</strong></p><ul><li>日志表中的时间戳（按时间顺序插入）</li><li>IoT 传感器数据（按采集顺序插入）</li><li>批量导入的历史数据</li></ul><p><strong>BRIN vs B-tree 对比：</strong></p><table tabindex="0"><thead><tr><th></th><th>B-tree</th><th>BRIN</th></tr></thead><tbody><tr><td>索引大小</td><td>大（与行数成正比）</td><td>极小（与块数成正比）</td></tr><tr><td>查询精度</td><td>精确（直接找到行）</td><td>粗粒度（确定块范围）</td></tr><tr><td>插入代价</td><td>中等（需维护树结构）</td><td>极低（仅更新 min/max）</td></tr><tr><td>数据相关性要求</td><td>无</td><td>高（相关性差则退化为全表扫描）</td></tr></tbody></table><p><strong>查看列相关性：</strong></p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> attname, correlation</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stats</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tablename </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;your_table&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> abs</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(correlation) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- correlation 越接近 ±1，BRIN 效果越好</span></span></code></pre></div><hr><p><strong>Q16. 解释 <code>pg_stat_statements</code> 扩展的工作原理以及如何用它进行查询优化？</strong></p><p><strong>参考答案：</strong></p><p><code>pg_stat_statements</code> 通过<strong>钩子（Hook）机制</strong>拦截所有 SQL 执行，统计每类查询的执行次数、总时间、行数等。</p><p><strong>工作原理：</strong></p><ol><li>通过 <code>ExecutorFinish_hook</code> 在每次查询执行结束时收集统计数据</li><li>对 SQL 文本进行<strong>归一化</strong>处理（将常量替换为 <code>$1</code>, <code>$2</code> 等参数占位符），同类 SQL 聚合统计</li><li>数据存储在共享内存中（<code>pg_stat_statements.max</code> 条），进程间共享</li></ol><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 安装</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">CREATE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> EXTENSION pg_stat_statements;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 查找最耗时的查询（总执行时间）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> query,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       calls,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       total_exec_time </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1000</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> total_sec,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       mean_exec_time </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> avg_ms,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">       rows</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> /</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> calls </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> avg_rows,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       stddev_exec_time </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stddev_ms   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 标准差大 = 执行时间不稳定</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_statements</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> total_exec_time </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">LIMIT</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 20</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 查找 I/O 密集型查询</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> query, calls,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       shared_blks_hit,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       shared_blks_read,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       shared_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (shared_blks_hit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> shared_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">001</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> miss_rate</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_statements</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> shared_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1000</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> shared_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 重置统计</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_statements_reset();</span></span></code></pre></div><p><strong>优化步骤：</strong></p><ol><li>找出 <code>total_exec_time</code> 最高的 SQL</li><li>用 <code>EXPLAIN (ANALYZE, BUFFERS)</code> 分析执行计划</li><li>检查是否有 Seq Scan（考虑添加索引）</li><li>检查 <code>rows</code> 估算是否准确（不准确则需 <code>ANALYZE</code> 更新统计信息）</li><li><code>stddev_exec_time</code> 大的查询排查锁等待或 I/O 抖动</li></ol><hr><p><strong>Q17. PostgreSQL 表级锁的 8 种模式是什么？哪些锁会阻塞 SELECT 查询？</strong></p><p><strong>参考答案：</strong></p><p>PostgreSQL 表级锁从弱到强：</p><ol><li><strong>ACCESS SHARE</strong> — <code>SELECT</code>（只读）获取，与 ACCESS EXCLUSIVE 冲突</li><li><strong>ROW SHARE</strong> — <code>SELECT FOR UPDATE/SHARE</code> 获取，与 EXCLUSIVE 和 ACCESS EXCLUSIVE 冲突</li><li><strong>ROW EXCLUSIVE</strong> — <code>INSERT/UPDATE/DELETE</code> 获取，与 SHARE、SHARE ROW EXCLUSIVE、EXCLUSIVE、ACCESS EXCLUSIVE 冲突</li><li><strong>SHARE UPDATE EXCLUSIVE</strong> — <code>VACUUM/ANALYZE/CREATE INDEX CONCURRENTLY</code> 获取</li><li><strong>SHARE</strong> — <code>CREATE INDEX</code>（非并发）获取</li><li><strong>SHARE ROW EXCLUSIVE</strong> — <code>CREATE TRIGGER/ALTER TABLE ADD FOREIGN KEY</code> 获取</li><li><strong>EXCLUSIVE</strong> — 很少显式使用</li><li><strong>ACCESS EXCLUSIVE</strong> — <code>ALTER TABLE/DROP TABLE/TRUNCATE/REINDEX/CLUSTER/VACUUM FULL/LOCK TABLE</code> 获取，与<strong>所有其他锁</strong>冲突</li></ol><p><strong>会阻塞 SELECT 的锁：</strong> 只有 <strong>ACCESS EXCLUSIVE</strong> 锁会阻塞 <code>SELECT</code>（因为 SELECT 获取 ACCESS SHARE，与 ACCESS EXCLUSIVE 冲突）。</p><p><strong>实践意义：</strong></p><ul><li><code>ALTER TABLE</code> 需要 ACCESS EXCLUSIVE，会阻塞所有查询</li><li>长时间运行的事务 + <code>ALTER TABLE</code> = 严重锁等待</li><li>解决方案：<code>lock_timeout = &#39;3s&#39;</code>（超时则放弃 DDL）/ <code>CREATE INDEX CONCURRENTLY</code> 替代 <code>CREATE INDEX</code></li></ul><hr><p><strong>Q18. 如何诊断和解决 PostgreSQL 中的查询性能问题？列出系统化的排查思路。</strong></p><p><strong>参考答案：</strong></p><p><strong>系统化排查步骤：</strong></p><p><strong>Step 1：发现问题查询</strong></p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 当前正在执行的长查询</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pid, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">now</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> query_start </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> duration, query, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">state</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_activity</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> state</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;active&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> AND</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> now</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> query_start </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> interval </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;5 seconds&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> duration </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 历史最耗时（pg_stat_statements）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> query, calls, total_exec_time</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">calls </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> avg_ms</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_statements </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> total_exec_time </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> LIMIT</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 20</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><strong>Step 2：分析执行计划</strong></p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">EXPLAIN (ANALYZE, BUFFERS, FORMAT </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">your query</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 关注：Seq Scan on large table / actual rows &gt;&gt; estimated rows / Buffers: read (磁盘IO)</span></span></code></pre></div><p><strong>Step 3：常见问题及修复</strong></p><table tabindex="0"><thead><tr><th>现象</th><th>原因</th><th>解决方案</th></tr></thead><tbody><tr><td>Seq Scan 大表</td><td>缺少索引或 <code>random_page_cost</code> 过高</td><td>添加索引 / 降低 <code>random_page_cost=1.1</code>（SSD）</td></tr><tr><td>rows 估算严重偏差</td><td>统计信息过时</td><td><code>ANALYZE table</code> / 提高 <code>default_statistics_target</code></td></tr><tr><td>Hash Join 内存不足</td><td>work_mem 太小</td><td>增大 <code>work_mem=64MB</code>（慎：每连接）</td></tr><tr><td>锁等待</td><td>长事务或 DDL</td><td><code>pg_blocking_pids()</code> 找到阻塞源，kill 或优化事务</td></tr><tr><td>大量 dead tuples</td><td>autovacuum 跟不上</td><td>手动 VACUUM / 调整 autovacuum 参数</td></tr></tbody></table><p><strong>Step 4：索引优化</strong></p><ul><li>添加复合索引时，等值条件列放前，范围/排序列放后</li><li>考虑部分索引（<code>WHERE is_active = true</code>）减少索引大小</li><li>使用 <code>pg_stat_user_indexes</code> 找出从未使用的索引（可删除）</li></ul><hr><p><strong>Q19. 解释 <code>EXPLAIN</code> 输出中 <code>cost</code> 的含义以及如何调整代价参数影响查询计划选择。</strong></p><p><strong>参考答案：</strong></p><p><strong><code>cost=start..total</code> 的含义：</strong></p><ul><li><code>start cost</code>：返回<strong>第一行</strong>的估算代价（对 LIMIT 查询有意义）</li><li><code>total cost</code>：返回<strong>所有行</strong>的估算代价（规划器默认优化目标）</li><li>代价单位：无量纲，以 <code>seq_page_cost=1.0</code> 为基准</li></ul><p><strong>代价参数及调整场景：</strong></p><table tabindex="0"><thead><tr><th>参数</th><th>默认值</th><th>说明</th><th>调整场景</th></tr></thead><tbody><tr><td><code>seq_page_cost</code></td><td>1.0</td><td>顺序读一页的代价（基准）</td><td>-</td></tr><tr><td><code>random_page_cost</code></td><td>4.0</td><td>随机读一页的代价</td><td>SSD 环境改为 1.1-2.0</td></tr><tr><td><code>cpu_tuple_cost</code></td><td>0.01</td><td>处理一行的 CPU 代价</td><td>-</td></tr><tr><td><code>cpu_index_tuple_cost</code></td><td>0.005</td><td>处理一条索引记录的代价</td><td>-</td></tr><tr><td><code>effective_cache_size</code></td><td>4GB</td><td>OS 文件缓存估算（影响 Index Scan 代价）</td><td>设为实际内存 * 0.75</td></tr><tr><td><code>parallel_tuple_cost</code></td><td>0.1</td><td>并行 worker 传输一行的代价</td><td>-</td></tr></tbody></table><p><strong>典型场景：</strong> 数据库在 SSD 上，但优化器仍然选择 Seq Scan：</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 全局或会话级别降低 random_page_cost</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> random_page_cost </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 然后重新执行 EXPLAIN，通常会切换为 Index Scan</span></span></code></pre></div><hr><p><strong>Q20. 什么情况下需要手动执行 <code>VACUUM FULL</code>？它与普通 <code>VACUUM</code> 有什么本质区别？</strong></p><p><strong>参考答案：</strong></p><p><strong>本质区别：</strong></p><table tabindex="0"><thead><tr><th></th><th>VACUUM（普通）</th><th>VACUUM FULL</th></tr></thead><tbody><tr><td>锁级别</td><td>ShareUpdateExclusiveLock（不阻塞读写）</td><td>AccessExclusiveLock（阻塞所有）</td></tr><tr><td>空间处理</td><td>原地标记死元组空间为可重用，<strong>不缩小文件</strong></td><td>完整重建表文件，<strong>将空间归还 OS</strong></td></tr><tr><td>执行时间</td><td>快（只扫描包含死元组的页）</td><td>慢（需要重建整张表 + 所有索引）</td></tr><tr><td>停机影响</td><td>在线执行，业务无感知</td><td>需要维护窗口（表完全不可访问）</td></tr></tbody></table><p><strong>何时需要 VACUUM FULL：</strong></p><ol><li><strong>表严重膨胀</strong>：删除了大量数据（如清理历史数据）后，表文件很大但实际数据很少，普通 VACUUM 无法缩小文件</li><li><strong><code>pg_total_relation_size</code> &gt;&gt; 实际数据量</strong>：说明有大量虚假空间</li><li><strong>查询性能因 bloat 严重下降</strong>（需要扫描大量空页）</li></ol><p><strong>替代方案（减少停机时间）：</strong></p><ul><li><code>pg_repack</code> 扩展：在线重建表，只需短暂的最终切换锁</li><li>分区表：定期 DROP 旧分区（立即归还空间，无需 VACUUM FULL）</li></ul><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 检查表膨胀程度</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> relname,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       pg_size_pretty(pg_total_relation_size(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">oid</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)) total,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       pg_size_pretty(pg_relation_size(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">oid</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)) table_only,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       n_dead_tup, n_live_tup,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">       round</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">100</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> *</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> n_dead_tup </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> nullif</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(n_live_tup </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> n_dead_tup, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">), </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) dead_pct</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_user_tables</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> n_dead_tup </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 10000</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> n_dead_tup </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><hr><p><strong>Q21. PostgreSQL 的查询执行器采用什么模型？与向量化执行引擎相比有何差异？</strong></p><p><strong>参考答案：</strong></p><p><strong>火山模型（Volcano/Iterator Model）：</strong></p><p>PostgreSQL 采用经典的**逐行拉取（Pull-based Row-at-a-time）**执行模型：</p><ul><li>每个算子节点实现 <code>Init()</code> / <code>GetNext()</code> / <code>End()</code> 三个接口</li><li>顶层节点调用 <code>GetNext()</code>，递归向下驱动，每次返回一行</li><li>链式调用构成执行树，数据从叶子节点&quot;流&quot;向根节点</li></ul><p><strong>优点：</strong> 实现简单，内存占用少（每次只处理一行），适合高并发 OLTP</p><p><strong>缺点：</strong></p><ul><li>函数调用开销大（每行每节点一次虚函数调用）</li><li>对 CPU 流水线不友好（分支多）</li><li>CPU 缓存利用率低（每行数据很小）</li></ul><p><strong>向量化执行（Vectorized Execution，如 DuckDB/ClickHouse）：</strong></p><ul><li>每次 <code>GetNext()</code> 返回一个<strong>向量</strong>（批量行，如 1024 行）</li><li>算子对整个向量进行 SIMD 批量处理</li><li>减少虚函数调用次数，提高 CPU 利用率，适合 OLAP 大批量扫描</li></ul><p><strong>PostgreSQL 的方向：</strong> JIT（Just-In-Time）编译，使用 LLVM 将关键循环（表达式求值、比较操作）编译为机器码，减少解释器开销（<code>jit = on</code>，默认在 JIT 收益大时自动开启）。</p><hr><p><strong>Q22. 描述 PostgreSQL autovacuum 在生产环境中常见的调优场景。</strong></p><p><strong>参考答案：</strong></p><p><strong>场景一：高频写入表（autovacuum 跟不上）</strong></p><p>现象：<code>pg_stat_user_tables.n_dead_tup</code> 持续增大，查询变慢。</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 针对高频写入表降低触发阈值，增加资源</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ALTER</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> TABLE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> hot_table </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    autovacuum_vacuum_scale_factor </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">01</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 1% 而非默认 20%</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    autovacuum_vacuum_threshold </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 100</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    autovacuum_vacuum_cost_delay </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 不限速（后台负载允许时）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    autovacuum_vacuum_cost_limit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 800</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       -- 提高 cost limit（默认 200）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span></code></pre></div><p><strong>场景二：全局 autovacuum worker 数量不足</strong></p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># postgresql.conf</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">autovacuum_max_workers</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> = 6  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 默认 3，大库可增加</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">autovacuum_naptime</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> = 30s    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 默认 60s，提高检查频率</span></span></code></pre></div><p><strong>场景三：大表 VACUUM 被长事务阻塞</strong></p><p>现象：vacuum 启动但无法清理，因为有事务持有旧快照（旧 <code>xmin</code>）。</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 找出阻塞 vacuum 的长事务</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pid, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">now</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> xact_start </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> duration, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">state</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, query</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_stat_activity</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> xact_start </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">IS NOT NULL</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> xact_start;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- vacuum 无法清理比 xmin_horizon 更新的 dead tuples</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> datname, datfrozenxid, age(datfrozenxid)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_database;</span></span></code></pre></div><p><strong>场景四：Visibility Map 不更新导致 IndexOnlyScan 频繁 fallback</strong></p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 检查各表的 vm 覆盖情况</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SELECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> relname,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       heap_blks_hit, heap_blks_read,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">       idx_blks_hit, idx_blks_read</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pg_statio_user_tables</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> heap_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1000</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ORDER BY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> heap_blks_read </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DESC</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 大量 heap_blks_read 说明 vm 未设置，index only scan 仍要读堆</span></span></code></pre></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("02-面试指南/03-数据库面试/03-PostgreSQL面试题.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03PostgreSQL___ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03PostgreSQL___ as default
};
