import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"网络协议面试题","description":"","frontmatter":{},"headers":[],"relativePath":"02-面试指南/02-后端面试/03-网络协议面试题.md","filePath":"02-面试指南/02-后端面试/03-网络协议面试题.md","lastUpdated":1774066204000}');
const _sfc_main = { name: "02-面试指南/02-后端面试/03-网络协议面试题.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="网络协议面试题" tabindex="-1">网络协议面试题 <a class="header-anchor" href="#网络协议面试题" aria-label="Permalink to &quot;网络协议面试题&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>涵盖 TCP/IP 核心机制、HTTP 协议演进（HTTP/1.1 → HTTP/2 → HTTP/3/QUIC）、HTTPS/TLS 安全握手与 WebSocket 协议的分层面试题。</p></blockquote><h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to &quot;相关链接&quot;">&amp;ZeroWidthSpace;</a></h2><ul><li>对应技术资料：<a href="./../../01-技术资料/02-后端/03-网络协议与HTTP.html">网络协议与 HTTP</a></li></ul><h2 id="🔥-高频考点速记" tabindex="-1">🔥 高频考点速记 <a class="header-anchor" href="#🔥-高频考点速记" aria-label="Permalink to &quot;🔥 高频考点速记&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>面试中最常被问到的核心知识点，按出现频率排序。建议优先掌握前 5 项。</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>考点</th><th>核心要点（一句话）</th><th>出题概率</th></tr></thead><tbody><tr><td>1</td><td>TCP 三次握手</td><td>SYN→SYN+ACK→ACK，防止历史连接+确认双方收发能力</td><td>★★★★★</td></tr><tr><td>2</td><td>TCP 四次挥手</td><td>FIN→ACK→FIN→ACK，TIME_WAIT=2MSL 等报文消失</td><td>★★★★★</td></tr><tr><td>3</td><td>HTTP/2 多路复用</td><td>一个 TCP 连接上多 stream 并行，解决 HTTP/1.1 队头阻塞</td><td>★★★★★</td></tr><tr><td>4</td><td>HTTPS/TLS 握手</td><td>TLS 1.2(2RTT)/1.3(1RTT)，非对称密钥协商+对称加密传输</td><td>★★★★★</td></tr><tr><td>5</td><td>TCP 拥塞控制</td><td>慢启动→拥塞避免→快重传→快恢复，BBR 基于带宽时延模型</td><td>★★★★☆</td></tr><tr><td>6</td><td>HTTP/3 与 QUIC</td><td>UDP+TLS1.3 融合，0-RTT 恢复，连接迁移，流级别无队头阻塞</td><td>★★★★☆</td></tr><tr><td>7</td><td>TCP vs UDP</td><td>TCP 可靠有序有连接、UDP 无连接轻量快速、QUIC 兼顾两者</td><td>★★★★☆</td></tr><tr><td>8</td><td>TIME_WAIT 问题</td><td>端口耗尽/连接复用，<code>tcp_tw_reuse</code>+<code>SO_REUSEADDR</code></td><td>★★★☆☆</td></tr><tr><td>9</td><td>WebSocket 协议</td><td>HTTP Upgrade 握手→全双工帧通信，适合实时双向场景</td><td>★★★☆☆</td></tr><tr><td>10</td><td>DNS 解析流程</td><td>浏览器缓存→OS缓存→递归DNS→根→TLD→权威，A/AAAA/CNAME</td><td>★★★☆☆</td></tr></tbody></table><hr><h2 id="题目列表" tabindex="-1">题目列表 <a class="header-anchor" href="#题目列表" aria-label="Permalink to &quot;题目列表&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="⭐-基础题" tabindex="-1">⭐ 基础题 <a class="header-anchor" href="#⭐-基础题" aria-label="Permalink to &quot;⭐ 基础题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q1. TCP 三次握手的流程是什么？为什么是三次而不是两次？</strong></p><p><strong>参考答案：</strong></p><p><strong>三次握手流程：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>客户端 ──[SYN, seq=x]──────────────────→ 服务器</span></span>
<span class="line"><span>客户端 ←──[SYN+ACK, seq=y, ack=x+1]─── 服务器</span></span>
<span class="line"><span>客户端 ──[ACK, ack=y+1]──────────────→  服务器</span></span>
<span class="line"><span>                连接建立，可以传输数据</span></span></code></pre></div><p><strong>为什么需要三次（不能两次）：</strong></p><ol><li><p><strong>两次握手无法确认客户端的接收能力：</strong> 服务器发出 SYN+ACK 后，不知道客户端是否收到（网络可能丢包）。三次握手中，客户端的 ACK 确认了它收到了服务器的 SYN+ACK。</p></li><li><p><strong>防止历史连接（旧 SYN 包）被接受：</strong> 若之前有一个因网络延迟而滞留的 SYN 包到达服务器，服务器发出 SYN+ACK。在两次握手情况下，服务器会认为连接已建立（浪费资源）。三次握手中，客户端会发送 RST（因为它根本没发这个 SYN），服务器中止这个无效连接。</p></li></ol><p><strong>三次握手确认了：</strong></p><ul><li>客户端的<strong>发送</strong>和服务器的<strong>接收</strong>能力 ✓（第1次）</li><li>服务器的<strong>发送</strong>和客户端的<strong>接收</strong>能力 ✓（第2次）</li><li>客户端<strong>已知道</strong>连接已建立 ✓（第3次，服务器通过这次 ACK 确认）</li></ul><hr><p><strong>Q2. TCP 四次挥手的流程是什么？为什么需要 TIME_WAIT 状态？</strong></p><p><strong>参考答案：</strong></p><p><strong>四次挥手流程：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>主动关闭方 ──[FIN]──→  被动关闭方（进入 CLOSE_WAIT）</span></span>
<span class="line"><span>主动关闭方 ←──[ACK]── 被动关闭方</span></span>
<span class="line"><span>（被动方继续发送剩余数据）</span></span>
<span class="line"><span>主动关闭方 ←──[FIN]── 被动关闭方（进入 LAST_ACK）</span></span>
<span class="line"><span>主动关闭方 ──[ACK]──→  被动关闭方（进入 CLOSED）</span></span>
<span class="line"><span>主动关闭方进入 TIME_WAIT（2MSL 后 CLOSED）</span></span></code></pre></div><p><strong>为什么是四次而非三次：</strong> 收到 FIN 后，被动关闭方可能还有数据要发送，所以 ACK 和 FIN 必须分开发送（两次）。</p><p><strong>TIME_WAIT 的两个目的（2MSL = 2 × Maximum Segment Lifetime）：</strong></p><ol><li><p><strong>确保最后的 ACK 能到达对方：</strong> 若最后的 ACK 丢失，被动关闭方会重发 FIN，TIME_WAIT 状态的主动关闭方能正确响应（再次发送 ACK），而不是发送 RST。</p></li><li><p><strong>让本次连接的所有报文在网络中消失：</strong> 确保网络中不存在属于此连接的延迟到达报文，防止被下一个使用相同四元组（src IP, src port, dst IP, dst port）的新连接误收。</p></li></ol><p><strong>TIME_WAIT 过多的危害：</strong> 客户端端口资源耗尽（Linux 默认可用端口 1024-65535）。优化：<code>net.ipv4.tcp_tw_reuse=1</code>（在有时间戳支持时复用 TIME_WAIT 连接）。</p><hr><p><strong>Q3. HTTP/1.1 相比 HTTP/1.0 有哪些重要改进？</strong></p><p><strong>参考答案：</strong></p><p><strong>1. 持久连接（Persistent Connection）：</strong></p><ul><li>HTTP/1.0：每个请求-响应后关闭 TCP 连接（需要 <code>Connection: keep-alive</code> 头显式开启）</li><li>HTTP/1.1：默认持久连接（<code>Connection: keep-alive</code>），同一连接可发送多个请求，减少三次握手次数</li></ul><p><strong>2. 管道化（Pipelining）：</strong></p><ul><li>客户端可以不等第一个请求响应，就发送第二个请求</li><li>但服务器响应必须按序，存在**队头阻塞（Head-of-Line Blocking）**问题</li><li>实际上大多数浏览器默认禁用管道化（实现复杂且效果差）</li></ul><p><strong>3. 分块传输编码（Chunked Transfer Encoding）：</strong></p><ul><li>不需要预先知道响应体大小，可以流式发送（<code>Transfer-Encoding: chunked</code>）</li><li>用于动态生成的内容</li></ul><p><strong>4. 虚拟主机支持（Host 头字段必须：）：</strong></p><ul><li>一个 IP 可以托管多个域名（<code>Host: example.com</code>），是现代 Web 共享主机的基础</li></ul><p><strong>5. 缓存控制增强：</strong></p><ul><li>新增 <code>Cache-Control</code> 头，提供比 HTTP/1.0 的 <code>Expires</code> 更精细的缓存策略</li><li>新增条件请求：<code>If-None-Match</code>/<code>ETag</code>，<code>If-Modified-Since</code>/<code>Last-Modified</code></li></ul><hr><p><strong>Q4. HTTPS 是什么？它在 HTTP 的基础上增加了什么？</strong></p><p><strong>参考答案：</strong></p><p>HTTPS = HTTP + TLS（Transport Layer Security）</p><p><strong>TLS 在 HTTP 基础上增加了三项保证：</strong></p><ol><li><p><strong>加密（Confidentiality）：</strong> 数据在传输过程中被对称加密（如 AES-256-GCM），即使被截获也无法读取内容</p></li><li><p><strong>完整性（Integrity）：</strong> 使用 AEAD（Authenticated Encryption with Associated Data）算法（如 GCM 模式），任何篡改都能被检测</p></li><li><p><strong>身份认证（Authentication）：</strong> 服务器提供数字证书，客户端验证证书是否由可信 CA 签发、是否与域名匹配，防止中间人攻击（MITM）</p></li></ol><p><strong>工作层次：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>应用层:  HTTP（明文）</span></span>
<span class="line"><span>            ↕</span></span>
<span class="line"><span>传输层:  TLS（加密/完整性/认证）</span></span>
<span class="line"><span>            ↕</span></span>
<span class="line"><span>传输层:  TCP（可靠传输）</span></span>
<span class="line"><span>            ↕</span></span>
<span class="line"><span>网络层:  IP</span></span></code></pre></div><p><strong>性能影响：</strong> TLS 握手增加 1-2 个 RTT（TLS 1.2 需要 2 RTT，TLS 1.3 只需 1 RTT，再次连接可 0 RTT），以及加密/解密的 CPU 开销（现代 CPU 硬件 AES 加速后影响很小，通常 &lt; 5%）。</p><hr><p><strong>Q5. 什么是 TCP 的粘包问题？如何解决？</strong></p><p><strong>参考答案：</strong></p><p><strong>粘包根本原因：</strong> TCP 是<strong>字节流</strong>协议，没有消息边界。发送方发送的多个&quot;消息&quot;可能被合并为一个 TCP 包（Nagle 算法合并小包），或一个大消息被拆分为多个 TCP 包。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>发送方依次发送：[Message1: 10B] [Message2: 20B]</span></span>
<span class="line"><span>接收方可能接收到：[10B + 20B = 30B 的完整流]</span></span>
<span class="line"><span>或接收到：[15B][15B] (拆包)</span></span></code></pre></div><p><strong>解决方案：</strong></p><ol><li><p><strong>固定长度消息：</strong> 每条消息固定 N 字节（简单但浪费，不灵活）</p></li><li><p><strong>分隔符：</strong> 消息间用特殊字节序列分隔</p><ul><li>HTTP 头部：<code>\\r\\n\\r\\n</code> 作为头部结束符</li><li>Redis RESP 协议：<code>\\r\\n</code> 分隔</li><li>缺点：消息内容中不能含有分隔符（或需要转义）</li></ul></li><li><p><strong>长度前缀（Length-Value，最常用）：</strong></p><ul><li>每条消息前置 N 字节的长度字段</li><li>接收方先读 N 字节得到长度 L，再读 L 字节得到消息体</li><li>大多数自定义二进制协议（如 Dubbo/Kafka）采用此方案</li></ul></li><li><p><strong>TLV（Type-Length-Value）：</strong> 包含类型、长度、值三字段，支持可扩展的消息格式（如 Protocol Buffers）</p></li></ol><hr><p><strong>Q6. 什么是 DNS 解析过程？从浏览器输入 URL 到获得 IP 地址经历了哪些步骤？</strong></p><p><strong>参考答案：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>浏览器输入 https://www.example.com</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 1: 本地缓存检查</span></span>
<span class="line"><span>  → 浏览器 DNS 缓存（几秒到几分钟）</span></span>
<span class="line"><span>  → OS DNS 缓存（/etc/hosts 文件 + 系统解析器缓存）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 2: 本地 DNS 解析器（Local Resolver）</span></span>
<span class="line"><span>  → 查询配置的 DNS 服务器（通常是 ISP 的 DNS 或 8.8.8.8）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 3: 递归解析（若本地 DNS 无缓存）</span></span>
<span class="line"><span>  → DNS Resolver → Root Name Server（.）→ 返回 .com 的 NS 地址</span></span>
<span class="line"><span>  → DNS Resolver → TLD Name Server（.com）→ 返回 example.com 的 NS 地址</span></span>
<span class="line"><span>  → DNS Resolver → Authoritative Name Server（example.com）→ 返回 www.example.com 的 A/AAAA 记录</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 4: 返回 IP 地址（带 TTL）</span></span>
<span class="line"><span>  → 本地 DNS 解析器缓存结果（TTL 时间内）</span></span>
<span class="line"><span>  → 返回给浏览器</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 5: 浏览器使用 IP 发起 TCP + TLS 连接</span></span></code></pre></div><p><strong>DNS 记录类型：</strong></p><ul><li><strong>A</strong>：域名 → IPv4 地址</li><li><strong>AAAA</strong>：域名 → IPv6 地址</li><li><strong>CNAME</strong>：域名 → 另一个域名（别名）</li><li><strong>MX</strong>：邮件服务器</li><li><strong>TXT</strong>：任意文本（SPF/DKIM 邮件验证）</li><li><strong>NS</strong>：权威名称服务器</li></ul><hr><h3 id="⭐⭐-进阶题" tabindex="-1">⭐⭐ 进阶题 <a class="header-anchor" href="#⭐⭐-进阶题" aria-label="Permalink to &quot;⭐⭐ 进阶题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q7. HTTP/2 的多路复用（Multiplexing）是如何工作的？它解决了什么问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>HTTP/1.1 的问题（队头阻塞）：</strong></p><ul><li>即使开启管道化（Pipelining），服务器也必须<strong>按序</strong>响应</li><li>第一个响应慢（大文件/复杂计算）→ 阻塞后续所有响应</li><li>浏览器绕过方案：每个域名开 6-8 个并行连接，浪费资源</li></ul><p><strong>HTTP/2 多路复用原理：</strong></p><p>HTTP/2 将所有数据封装为<strong>帧（Frame）</strong>，并引入 <strong>Stream</strong> 概念：</p><ul><li>一个 TCP 连接上可以并发多个独立的 Stream（每个请求/响应是一个 Stream）</li><li>每个 Stream 有唯一 ID（客户端发起的为奇数：1, 3, 5, ...）</li><li>不同 Stream 的帧可以<strong>交错发送</strong>（Interleaving）</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TCP 连接中的帧传输：</span></span>
<span class="line"><span>Stream 1 帧 → Stream 3 帧 → Stream 1 帧 → Stream 5 帧 → Stream 3 帧 → ...</span></span>
<span class="line"><span>（多个流的帧交错传输，接收方根据 Stream ID 重组）</span></span></code></pre></div><p><strong>Stream 的帧传输不需要等待其他 Stream 完成</strong> → 解决了 HTTP 层的队头阻塞。</p><p><strong>残留问题（TCP 层队头阻塞）：</strong> TCP 的丢包重传会阻塞<strong>所有</strong> Stream，直到丢失的包被重传并按序交付。这是 HTTP/3（QUIC）要解决的问题。</p><hr><p><strong>Q8. TLS 1.3 相比 TLS 1.2 做了哪些重要改进？</strong></p><p><strong>参考答案：</strong></p><p><strong>1. 握手 RTT 减少：1-RTT 而非 2-RTT</strong></p><p>TLS 1.2 需要 2 个 RTT（ClientHello → ServerHello + Cert + Done → ClientKey + Finished → ServerFinished）。</p><p>TLS 1.3 客户端在 <code>ClientHello</code> 中直接包含 ECDHE 公钥（<code>key_share</code>），服务器第一次响应就可以计算出共享密钥，握手仅需 1 RTT。</p><p><strong>2. 0-RTT 会话恢复（Session Resumption）</strong></p><p>基于 PSK（Pre-Shared Key / Session Ticket），再次连接时客户端在第一个消息中直接携带应用数据（0-RTT）。注意：0-RTT 数据不具备前向保密性，且有重放攻击风险（应只用于幂等操作）。</p><p><strong>3. 强制前向保密（Forward Secrecy）</strong></p><p>TLS 1.2 支持静态 RSA 密钥交换（用服务器私钥直接加密 Pre-Master Secret），如果服务器私钥泄露，历史流量可被解密。TLS 1.3 移除了静态 RSA，<strong>只允许 ECDHE 临时密钥交换</strong>，每次会话的密钥独立，历史记录无法被解密。</p><p><strong>4. 精简密码套件（5 种，全部 AEAD）</strong></p><p>移除了 RC4、3DES、CBC 模式、SHA-1/MD5 等弱算法，只保留 AES-GCM、AES-CCM、ChaCha20-Poly1305 等安全算法。</p><p><strong>5. 加密 ServerHello 之后的握手消息（Certificate、CertificateVerify）</strong></p><p>TLS 1.2 的证书是明文传输的，攻击者可以知道你访问了哪个网站的证书。TLS 1.3 将 <code>Certificate</code> 和 <code>CertificateVerify</code> 加密传输（使用握手密钥），提升了隐私保护。</p><hr><p><strong>Q9. 解释 WebSocket 的握手过程和帧格式，以及它与 HTTP 长轮询的区别。</strong></p><p><strong>参考答案：</strong></p><p><strong>WebSocket 升级握手：</strong></p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 客户端请求：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">GET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /ws </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Upgrade</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> websocket</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Connection</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Upgrade</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Sec-WebSocket-Key</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dGhlIHNhbXBsZSBub25jZQ==  ← 随机 16 字节 Base64</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Sec-WebSocket-Version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 13</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 服务器响应：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 101</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Switching Protocols</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Upgrade</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> websocket</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Connection</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Upgrade</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Sec-WebSocket-Accept</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Accept = Base64(SHA1(Key + &quot;258EAFA5-E914-47DA-95CA-C5AB0DC85B11&quot;))</span></span></code></pre></div><p><strong>握手后：</strong> TCP 连接转为 WebSocket 协议，双方可以互发帧（全双工）。</p><p><strong>WebSocket 帧格式（关键字段）：</strong></p><ul><li>FIN (1bit)：是否最后一个分片</li><li>Opcode (4bit)：0x1=文本, 0x2=二进制, 0x8=关闭, 0x9=Ping, 0xA=Pong</li><li>MASK (1bit)：客户端→服务器必须掩码（4字节随机掩码 XOR 数据）</li><li>Payload Length (7/16/64 bit)：可变长度字段</li></ul><p><strong>vs HTTP 长轮询：</strong></p><table tabindex="0"><thead><tr><th></th><th>长轮询（Long Polling）</th><th>WebSocket</th></tr></thead><tbody><tr><td>方向</td><td>单向（服务器→客户端响应后关闭）</td><td>全双工（同时双向）</td></tr><tr><td>延迟</td><td>有（每次响应后需重建连接）</td><td>极低（持久连接）</td></tr><tr><td>HTTP 兼容</td><td>完全兼容</td><td>需要 Upgrade 支持</td></tr><tr><td>服务器推送</td><td>间接（等待客户端发起）</td><td>直接</td></tr><tr><td>适用场景</td><td>低频率通知（新邮件）</td><td>高频率实时（聊天/游戏）</td></tr></tbody></table><hr><p><strong>Q10. TCP 的拥塞控制有哪几个算法？慢启动阶段为什么叫&quot;慢&quot;启动？</strong></p><p><strong>参考答案：</strong></p><p><strong>TCP 拥塞控制四个算法（RFC 5681 Reno 为基础）：</strong></p><ol><li><p><strong>慢启动（Slow Start）：</strong></p><ul><li>初始 <code>cwnd = 1 MSS</code>（或 10 MSS，RFC 6928）</li><li>每收到一个 ACK，<code>cwnd += 1 MSS</code>（指数增长，每 RTT 翻倍）</li><li>增长到 <code>ssthresh</code> 时进入拥塞避免</li></ul></li><li><p><strong>拥塞避免（Congestion Avoidance）：</strong></p><ul><li><code>cwnd &gt;= ssthresh</code> 后，每 RTT 只 <code>cwnd += 1 MSS</code>（线性增长）</li><li>更保守地探测带宽上限</li></ul></li><li><p><strong>快速重传（Fast Retransmit）：</strong></p><ul><li>收到 3 个重复 ACK 时，立即重传丢失包（不等超时）</li><li>进入快速恢复：<code>ssthresh = cwnd/2</code>, <code>cwnd = ssthresh + 3</code></li></ul></li><li><p><strong>快速恢复（Fast Recovery）：</strong></p><ul><li>在快速重传后，进入快速恢复状态</li><li>每收到重复 ACK，<code>cwnd++</code>（维持数据发送）</li><li>收到新 ACK（确认重传成功），<code>cwnd = ssthresh</code>，退出快速恢复</li></ul></li></ol><p><strong>&quot;慢&quot;启动的含义：</strong></p><p>慢启动的&quot;慢&quot;不是指增长速度慢（实际上是<strong>指数增长</strong>，非常快），而是相对于 TCP 之前的&quot;快速启动&quot;策略（直接以完整窗口发送）——慢启动从 <strong>1 个包开始</strong>，逐渐增加，因此叫&quot;慢&quot;启动。</p><p><strong>历史背景：</strong> 1988 年 Van Jacobson 引入慢启动之前，新连接直接发送满窗口数据，导致大量丢包，造成了著名的&quot;拥塞崩溃（Congestion Collapse）&quot;。</p><hr><p><strong>Q11. 解释 HTTP/3 和 QUIC 协议，它如何彻底解决队头阻塞问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>HTTP/2 残留的 TCP 队头阻塞：</strong></p><p>HTTP/2 虽然在 HTTP 层实现了多路复用，但底层使用 TCP。TCP 丢包时，其可靠性保证要求丢失的包被重传且按序交付，在此期间所有 Stream 的数据交付都被阻塞，即使其他 Stream 的数据已经到达。</p><p><strong>QUIC 的解决方案：</strong></p><p>QUIC 在 UDP 之上实现了<strong>Stream 级别的独立可靠传输</strong>：</p><ul><li>每个 Stream 有独立的有序交付队列</li><li>某个 Stream 的包丢失 → 只影响该 Stream，其他 Stream 继续正常交付</li><li>从根本上消除了 TCP 层的队头阻塞</li></ul><p><strong>QUIC 其他关键特性：</strong></p><table tabindex="0"><thead><tr><th>特性</th><th>说明</th></tr></thead><tbody><tr><td>集成 TLS 1.3</td><td>握手+加密合一，新连接 1-RTT，会话恢复 0-RTT</td></tr><tr><td>Connection Migration</td><td>用 Connection ID 标识连接（非 IP:Port），网络切换不断连</td></tr><tr><td>用户态实现</td><td>运行在 UDP 之上，无需修改内核，更新灵活</td></tr><tr><td>前向纠错（FEC）</td><td>可选，减少丢包重传的影响</td></tr></tbody></table><p><strong>HTTP/3 = HTTP/2 语义 + QUIC 传输</strong>，保留了 HPACK 头部压缩（升级为 QPACK 以适应 QUIC 的乱序特性）。</p><hr><p><strong>Q12. 什么是 DNS over HTTPS（DoH）？它解决了什么安全问题？</strong></p><p><strong>参考答案：</strong></p><p><strong>传统 DNS 的安全问题：</strong></p><ol><li><strong>明文传输（UDP 53 端口）</strong>：DNS 查询不加密，ISP/任何中间人可以监听你的所有 DNS 查询</li><li><strong>DNS 劫持</strong>：中间人可以伪造 DNS 响应，将域名解析到恶意 IP</li><li><strong>DNS 污染</strong>：在某些网络环境下，DNS 响应被篡改</li></ol><p><strong>DoH（DNS over HTTPS，RFC 8484）：</strong></p><ul><li>将 DNS 查询封装在 HTTPS 请求中（<code>Content-Type: application/dns-message</code>）</li><li>使用 HTTPS 的 TLS 加密，隐藏查询内容，防止窃听和篡改</li><li>请求路径：<code>https://dns.google/dns-query?dns=...</code>（或 Cloudflare <code>1.1.1.1</code>）</li></ul><p><strong>vs DoT（DNS over TLS，RFC 7858）：</strong></p><ul><li>DoT 使用独立端口 853，网络管理员可以通过封锁 853 端口阻止</li><li>DoH 使用 443 端口（与正常 HTTPS 混合），难以被屏蔽</li><li>DoT 延迟略低（无 HTTP 层开销），但 DoH 更难被过滤</li></ul><hr><h3 id="⭐⭐⭐-高级题" tabindex="-1">⭐⭐⭐ 高级题 <a class="header-anchor" href="#⭐⭐⭐-高级题" aria-label="Permalink to &quot;⭐⭐⭐ 高级题&quot;">&amp;ZeroWidthSpace;</a></h3><hr><p><strong>Q13. 详细描述 TLS 1.2 完整握手过程中的密钥派生，以及前向保密是如何实现的。</strong></p><p><strong>参考答案：</strong></p><p><strong>TLS 1.2 + ECDHE 完整握手密钥派生：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Step 1: 客户端发送 ClientHello</span></span>
<span class="line"><span>  - 随机数 client_random（32 bytes）</span></span>
<span class="line"><span>  - 支持的加密套件列表</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 2: 服务器发送 ServerHello + Certificate + ServerKeyExchange</span></span>
<span class="line"><span>  - 随机数 server_random（32 bytes）</span></span>
<span class="line"><span>  - 选择的加密套件</span></span>
<span class="line"><span>  - Certificate：服务器证书（含 RSA/ECDSA 公钥）</span></span>
<span class="line"><span>  - ServerKeyExchange：</span></span>
<span class="line"><span>      临时 ECDHE 公钥（server_ecdhe_pub）</span></span>
<span class="line"><span>      + 用服务器私钥对 (client_random + server_random + server_ecdhe_pub) 的签名</span></span>
<span class="line"><span>      （签名证明这个临时 ECDHE 公钥确实是该服务器生成的，防止 MITM）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 3: 客户端发送 ClientKeyExchange</span></span>
<span class="line"><span>  - 客户端临时 ECDHE 公钥（client_ecdhe_pub）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 4: 双方独立计算 Pre-Master Secret</span></span>
<span class="line"><span>  - pre_master_secret = ECDH(server_ecdhe_pub, client_ecdhe_priv)</span></span>
<span class="line"><span>                      = ECDH(client_ecdhe_pub, server_ecdhe_priv)</span></span>
<span class="line"><span>  （ECDH 的特性：双方使用对方公钥和自己私钥计算出相同的值）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 5: 派生 Master Secret（PRF = HMAC-SHA256/SHA384）</span></span>
<span class="line"><span>  master_secret = PRF(pre_master_secret, &quot;master secret&quot;,</span></span>
<span class="line"><span>                      client_random + server_random, 48 bytes)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Step 6: 派生会话密钥（密钥块 Key Block）</span></span>
<span class="line"><span>  key_block = PRF(master_secret, &quot;key expansion&quot;,</span></span>
<span class="line"><span>                  server_random + client_random, key_block_len)</span></span>
<span class="line"><span>  从中提取：</span></span>
<span class="line"><span>    client_write_key  → 客户端→服务器的数据加密密钥</span></span>
<span class="line"><span>    server_write_key  → 服务器→客户端的数据加密密钥</span></span>
<span class="line"><span>    client_write_IV   → 客户端加密的初始向量</span></span>
<span class="line"><span>    server_write_IV   → 服务器加密的初始向量</span></span></code></pre></div><p><strong>前向保密（Forward Secrecy）的实现：</strong></p><p>使用 <strong>ECDHE（临时 ECDH）</strong>：服务器每次握手都生成新的临时 ECDH 密钥对（<code>server_ecdhe_priv/pub</code>），使用后丢弃。</p><p>即使将来服务器的长期 RSA/ECDSA 私钥泄露：</p><ul><li>攻击者可以验证之前的证书签名</li><li>但<strong>无法获得临时 ECDHE 私钥</strong>（已丢弃）</li><li>因此<strong>无法计算 Pre-Master Secret → 无法解密历史会话流量</strong></li></ul><p>对比：若使用静态 RSA 密钥交换（TLS 1.3 已移除），<code>pre_master_secret</code> 用服务器 RSA 公钥加密，私钥泄露后历史流量全部可解密。</p><hr><p><strong>Q14. 在高并发场景下，如何解决 TIME_WAIT 过多的问题？每种方案的原理是什么？</strong></p><p><strong>参考答案：</strong></p><p><strong>TIME_WAIT 过多的原因：</strong> 大量短连接（如 HTTP/1.0 或没有连接复用的 HTTP/1.1），主动关闭方留下大量 TIME_WAIT 状态连接，占用端口资源（端口耗尽后新连接失败）。</p><p><strong>解决方案：</strong></p><p><strong>方案 1：<code>net.ipv4.tcp_tw_reuse = 1</code>（推荐）</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sysctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -w</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> net.ipv4.tcp_tw_reuse=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span></span></code></pre></div><p>允许新连接复用处于 TIME_WAIT 状态的连接（要求新连接的时间戳大于 TIME_WAIT 连接的时间戳，依赖 TCP 时间戳选项 <code>tcp_timestamps=1</code>）。安全，不影响数据完整性。</p><p><strong>方案 2：<code>net.ipv4.tcp_fin_timeout</code> 缩短</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sysctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -w</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> net.ipv4.tcp_fin_timeout=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">15</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # 默认 60 秒</span></span></code></pre></div><p>缩短 FIN_WAIT_2 和 TIME_WAIT 的超时时间（TIME_WAIT = 2*MSL，Linux 实际 MSL=30s，可调整）。</p><p><strong>方案 3：扩大端口范围</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sysctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -w</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> net.ipv4.ip_local_port_range=&quot;1024 65535&quot;</span></span></code></pre></div><p><strong>方案 4：HTTP 连接复用（根本解决方案）</strong></p><ul><li>开启 HTTP Keep-Alive（HTTP/1.1 默认）</li><li>使用连接池复用 TCP 连接</li><li>升级到 HTTP/2（单连接多路复用）</li></ul><p><strong>方案 5：<code>net.ipv4.tcp_tw_recycle = 1</code>（不推荐，已废弃）</strong></p><ul><li>快速回收 TIME_WAIT 连接，不等 2MSL</li><li>在 NAT 环境下可能导致连接失败（同一 NAT 后的不同客户端时间戳可能乱序）</li><li>Linux 4.12 内核已移除此选项</li></ul><hr><p><strong>Q15. 解释 TCP 滑动窗口和拥塞控制窗口的关系，以及流量控制与拥塞控制的区别。</strong></p><p><strong>参考答案：</strong></p><p><strong>两种窗口：</strong></p><ul><li><p><strong>接收窗口（rwnd，Receiver Window）：</strong> 接收方缓冲区的剩余空间，通过 TCP 头的 Window 字段通告给发送方。<strong>流量控制</strong>用——防止发送方压垮接收方。</p></li><li><p><strong>拥塞窗口（cwnd，Congestion Window）：</strong> 发送方根据拥塞控制算法（慢启动/拥塞避免）维护的内部变量。<strong>拥塞控制</strong>用——防止发送方压垮网络。</p></li></ul><p><strong>实际发送量 = min(rwnd, cwnd)</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>例：</span></span>
<span class="line"><span>  rwnd = 64KB（接收方告知，接收缓冲区还有 64KB）</span></span>
<span class="line"><span>  cwnd = 32KB（拥塞控制认为网络容量约 32KB）</span></span>
<span class="line"><span>  → 实际可发送 32KB</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  rwnd = 16KB（接收方缓冲区快满了）</span></span>
<span class="line"><span>  cwnd = 32KB</span></span>
<span class="line"><span>  → 实际可发送 16KB（受流量控制限制）</span></span></code></pre></div><p><strong>流量控制 vs 拥塞控制：</strong></p><table tabindex="0"><thead><tr><th></th><th>流量控制（Flow Control）</th><th>拥塞控制（Congestion Control）</th></tr></thead><tbody><tr><td>目的</td><td>防止接收方缓冲区溢出</td><td>防止网络链路拥塞</td></tr><tr><td>依据</td><td>接收方通告的 rwnd</td><td>丢包/RTT 变化（网络反馈）</td></tr><tr><td>机制</td><td>滑动窗口</td><td>慢启动/拥塞避免/快速恢复</td></tr><tr><td>控制对象</td><td>端到端（发送方-接收方）</td><td>网络整体</td></tr><tr><td>RFC</td><td>RFC 793</td><td>RFC 5681</td></tr></tbody></table><hr><p><strong>Q16. HTTP 缓存机制是如何工作的？强缓存和协商缓存的区别是什么？</strong></p><p><strong>参考答案：</strong></p><p><strong>强缓存（不向服务器发请求）：</strong></p><p>浏览器检查本地缓存是否过期：</p><ul><li><code>Cache-Control: max-age=3600</code>（相对时间，推荐，优先级更高）</li><li><code>Expires: Wed, 21 Oct 2025 07:28:00 GMT</code>（绝对时间，HTTP/1.0，受客户端时钟影响）</li></ul><p>缓存未过期 → 直接使用本地缓存，返回 <code>200 OK (from cache)</code>，<strong>不发送网络请求</strong>。</p><p><strong>协商缓存（向服务器验证是否有更新）：</strong></p><p>强缓存过期后，浏览器发请求到服务器验证：</p><table tabindex="0"><thead><tr><th>客户端发送</th><th>服务器响应（未修改）</th><th>服务器响应（已修改）</th></tr></thead><tbody><tr><td><code>If-None-Match: &quot;etag_value&quot;</code></td><td><code>304 Not Modified</code></td><td><code>200 OK</code> + 新 <code>ETag</code></td></tr><tr><td><code>If-Modified-Since: date</code></td><td><code>304 Not Modified</code></td><td><code>200 OK</code> + 新 <code>Last-Modified</code></td></tr></tbody></table><p><code>304 Not Modified</code> 只返回响应头，不返回 body，节省带宽。</p><p><strong>ETag vs Last-Modified 的优先级：</strong> ETag 优先级更高（<code>ETag</code> 是内容哈希，精确；<code>Last-Modified</code> 只有秒级精度，且文件修改时间改变不代表内容改变）。</p><p><strong>Cache-Control 指令：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>public: 任何缓存可存储（CDN、代理等）</span></span>
<span class="line"><span>private: 只有浏览器可存储（用户私有内容）</span></span>
<span class="line"><span>no-cache: 必须向服务器验证（走协商缓存，不走强缓存）</span></span>
<span class="line"><span>no-store: 不缓存（敏感数据）</span></span>
<span class="line"><span>must-revalidate: 缓存过期后必须验证，不允许使用过期缓存</span></span>
<span class="line"><span>s-maxage: CDN 缓存时间（覆盖 max-age）</span></span>
<span class="line"><span>stale-while-revalidate=N: 返回旧缓存同时后台更新（SWR 模式）</span></span></code></pre></div><hr><p><strong>Q17. 什么是跨域资源共享（CORS）？浏览器的同源策略是什么？</strong></p><p><strong>参考答案：</strong></p><p><strong>同源策略（Same-Origin Policy）：</strong> 浏览器安全基石。&quot;同源&quot;要求<strong>协议 + 域名 + 端口</strong>完全相同。不同源的 JavaScript 不能读取对方的响应、Cookie、DOM。</p><p><strong>CORS（Cross-Origin Resource Sharing）：</strong> HTTP 头部机制，允许服务器声明哪些跨域请求是允许的。</p><p><strong>简单请求（直接发送，附带 CORS 头）：</strong></p><ul><li>方法：GET/POST/HEAD</li><li>头部：只包含简单字段（Content-Type 限于 form 格式等）</li></ul><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 请求</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">GET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /data </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://frontend.example.com</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 响应</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Allow-Origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://frontend.example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Allow-Credentials</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> true  # 允许携带 Cookie</span></span></code></pre></div><p><strong>预检请求（Preflight，OPTIONS 方法）：</strong></p><ul><li>非简单请求（如 <code>Content-Type: application/json</code> 或自定义头）先发 OPTIONS 请求</li><li>服务器返回允许的方法和头部，浏览器再发实际请求</li></ul><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 预检请求</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">OPTIONS</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /data </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://frontend.example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Request-Method</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> POST</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Request-Headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Content-Type, Authorization</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 预检响应</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 204</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> No Content</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Allow-Origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://frontend.example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Allow-Methods</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> GET, POST, PUT</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Allow-Headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Content-Type, Authorization</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Access-Control-Max-Age</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 86400  # 预检结果缓存时间（秒）</span></span></code></pre></div><hr><p><strong>Q18. HTTPS 证书链验证的详细过程是什么？什么是证书透明度（Certificate Transparency）？</strong></p><p><strong>参考答案：</strong></p><p><strong>证书链验证步骤：</strong></p><ol><li><p><strong>链的构建：</strong> 从服务器证书开始，查找颁发者（Issuer），找到中间 CA 证书，直到找到系统信任的 Root CA。</p></li><li><p><strong>签名验证（每个证书）：</strong> 用上级证书的公钥验证当前证书的签名。服务器证书由中间 CA 签名，中间 CA 由 Root CA 签名，Root CA 自签名（系统预置）。</p></li><li><p><strong>有效期验证：</strong> 检查每个证书的 NotBefore 和 NotAfter 字段是否包含当前时间。</p></li><li><p><strong>域名验证：</strong> 检查证书的 CN（Common Name）或 SAN（Subject Alternative Name）是否与请求的域名匹配（支持通配符 <code>*.example.com</code>）。</p></li><li><p><strong>撤销检查（OCSP/CRL）：</strong></p><ul><li>OCSP（Online Certificate Status Protocol）：实时查询 CA 的 OCSP 服务器</li><li>CRL（Certificate Revocation List）：下载证书吊销列表（较慢）</li><li>OCSP Stapling（推荐）：服务器预先查询 OCSP 并将响应缓存在 TLS 握手中</li></ul></li></ol><p><strong>证书透明度（Certificate Transparency, CT）：</strong></p><p>为防止 CA 私下颁发伪造证书（如伪造 google.com 的证书）：</p><ul><li>所有受信任 CA 颁发的证书必须记录到公开的 <strong>CT Log</strong>（基于 Merkle 树，不可篡改）</li><li>每个证书颁发时获得一个 <strong>SCT（Signed Certificate Timestamp）</strong>（CT Log 的签名凭证）</li><li>浏览器在 TLS 握手中检查 SCT，证书未在 CT Log 中则拒绝</li></ul><p>这样，任何 CA 颁发的伪造证书都会被公开，域名所有者可以监控（如 Google 的 crt.sh 网站）。</p><hr><p><strong>Q19. 介绍 HTTP 请求走私（Request Smuggling）攻击原理。</strong></p><p><strong>参考答案：</strong></p><p><strong>背景：</strong> 现代 Web 架构通常有前端代理（CDN/负载均衡）+ 后端服务器，两者都需要解析 HTTP 请求边界。</p><p><strong>攻击原理：</strong> HTTP 规定两种指定请求体大小的方式：</p><ul><li><code>Content-Length</code>：明确的字节长度</li><li><code>Transfer-Encoding: chunked</code>：分块传输，用 <code>0\\r\\n\\r\\n</code> 标记结束</li></ul><p>当前端代理和后端服务器对请求边界的解析不一致时，攻击者可以&quot;走私&quot;一个额外的请求到后端：</p><p><strong>CL.TE 攻击（前端用 Content-Length，后端用 Transfer-Encoding）：</strong></p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">POST</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> / </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Content-Length</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 49</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Transfer-Encoding</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> chunked</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e\\r\\n</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">SMUGGLED DATA\\r\\n</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">0\\r\\n</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">\\r\\n</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">GET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /secret </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">HTTP</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> evil.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Foo</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> x</span></span></code></pre></div><p>前端看到 <code>Content-Length: 49</code>，转发 49 字节到后端。后端使用 chunked 解析，处理完 <code>e\\r\\n...0\\r\\n\\r\\n</code> 后，<code>GET /secret HTTP/1.1...</code> 被当作<strong>下一个请求的开头</strong>，由下一个用户的请求完成——可以读取其他用户的请求、绕过 ACL、缓存投毒等。</p><p><strong>防御：</strong></p><ul><li>前后端统一解析方式（都用 CL 或都用 TE）</li><li>后端拒绝模糊请求（同时含 CL 和 TE 的请求）</li><li>使用 HTTP/2（帧格式定义了精确的消息边界，无此问题）</li></ul><hr><p><strong>Q20. 解释 TCP 的 Nagle 算法，以及什么场景下应该禁用它。</strong></p><p><strong>参考答案：</strong></p><p><strong>Nagle 算法的目的：</strong> 减少网络中小包（Tiny Packet）数量，提高网络利用率。</p><p><strong>核心逻辑：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if (有数据要发送):</span></span>
<span class="line"><span>    if (未确认的数据量 == 0 或 待发送数据 &gt;= MSS):</span></span>
<span class="line"><span>        立即发送</span></span>
<span class="line"><span>    else:</span></span>
<span class="line"><span>        等待，将小数据缓存，直到收到 ACK 或缓存满 MSS 再发送</span></span></code></pre></div><p><strong>Nagle 算法与延迟 ACK（Delayed ACK）的交互问题：</strong></p><p>TCP 接收方默认有延迟 ACK（等待 40-200ms 再 ACK，期望合并 ACK 与响应数据）。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>客户端发 16B 小包 → 服务器</span></span>
<span class="line"><span>服务器：延迟 ACK 等 40ms</span></span>
<span class="line"><span>客户端：Nagle 等 ACK 再发下一个包</span></span>
<span class="line"><span>结果：双方都在等对方，延迟 40ms + Nagle 缓存时间！</span></span></code></pre></div><p>这是著名的 <strong>Nagle + Delayed ACK 性能问题</strong>，在 SSH/MySQL 客户端等交互式应用中表现明显。</p><p><strong>禁用 Nagle 的场景（<code>TCP_NODELAY</code> 选项）：</strong></p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> flag </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">setsockopt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(fd, IPPROTO_TCP, TCP_NODELAY, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">flag</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">sizeof</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(flag));</span></span></code></pre></div><ul><li><strong>SSH、Telnet 等交互式应用</strong>：用户每次击键都需要立即发送，Nagle 引入额外延迟</li><li><strong>在线游戏客户端</strong>：实时性要求高，每帧状态更新不能等待缓存</li><li><strong>MySQL/PostgreSQL 客户端库</strong>：默认开启 TCP_NODELAY（或 MySQL 的 <code>CLIENT_NO_DELAY</code>）</li><li><strong>延迟敏感的金融交易系统</strong>：微秒级延迟要求</li></ul><p><strong>不应禁用：</strong> 批量文件传输、大数据量上传——Nagle 减少了大量小包，提高了吞吐量。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("02-面试指南/02-后端面试/03-网络协议面试题.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03________ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03________ as default
};
