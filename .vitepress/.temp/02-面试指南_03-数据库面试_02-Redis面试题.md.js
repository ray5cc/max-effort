import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Redis 面试题","description":"","frontmatter":{},"headers":[],"relativePath":"02-面试指南/03-数据库面试/02-Redis面试题.md","filePath":"02-面试指南/03-数据库面试/02-Redis面试题.md","lastUpdated":1774066204000}');
const _sfc_main = { name: "02-面试指南/03-数据库面试/02-Redis面试题.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="redis-面试题" tabindex="-1">Redis 面试题 <a class="header-anchor" href="#redis-面试题" aria-label="Permalink to &quot;Redis 面试题&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>涵盖 Redis 高频面试考点：数据结构底层实现（SDS/listpack/skiplist/quicklist）、持久化机制（RDB COW/AOF 重写/混合持久化）、内存管理、网络模型、集群与高可用（16384 slots/Gossip/脑裂）、事务与 Lua 脚本、Redlock 争议、布隆过滤器、分布式限流与延迟队列。共 33 道分层题目，适合从初级到高级工程师备战。</p></blockquote><h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to &quot;相关链接&quot;">&amp;ZeroWidthSpace;</a></h2><ul><li>对应技术资料：<a href="./../../01-技术资料/03-数据库/02-Redis核心技术.html">Redis 核心技术深度解析</a></li></ul><h2 id="目录" tabindex="-1">目录 <a class="header-anchor" href="#目录" aria-label="Permalink to &quot;目录&quot;">&amp;ZeroWidthSpace;</a></h2><ol><li><a href="#1-基础题">⭐ 基础题（熟悉概念）</a></li><li><a href="#2-进阶题">⭐⭐ 进阶题（理解原理）</a></li><li><a href="#3-高级题">⭐⭐⭐ 高级题（深度原理 / 源码级）</a></li><li><a href="#4-场景题">场景题（系统设计）</a></li><li><a href="#5-深度补充题q23-q32">深度补充题（Q23-Q33）</a>—— 数据结构底层 / 持久化 COW / 集群 Gossip / Redlock 争议 / 限流器 / 延迟队列</li></ol><hr><h2 id="🔥-高频考点速记" tabindex="-1">🔥 高频考点速记 <a class="header-anchor" href="#🔥-高频考点速记" aria-label="Permalink to &quot;🔥 高频考点速记&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>面试中最常被问到的核心知识点，按出现频率排序。建议优先掌握前 5 项。</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>考点</th><th>核心要点（一句话）</th><th>出题概率</th></tr></thead><tbody><tr><td>1</td><td>Redis 数据结构与编码</td><td>5种类型底层7种编码，SDS/ziplist/skiplist</td><td>★★★★★</td></tr><tr><td>2</td><td>Redis 为什么这么快</td><td>纯内存+单线程避免锁+IO多路复用+高效数据结构</td><td>★★★★★</td></tr><tr><td>3</td><td>缓存穿透/击穿/雪崩</td><td>穿透(不存在key)用布隆过滤器，击穿(热key过期)用互斥锁，雪崩(大量key同时过期)设随机过期</td><td>★★★★★</td></tr><tr><td>4</td><td>持久化 RDB vs AOF</td><td>RDB快照(fork子进程全量)，AOF日志(append追写)，混合模式</td><td>★★★★☆</td></tr><tr><td>5</td><td>分布式锁</td><td>SET NX EX + Lua原子释放 + Redlock争议</td><td>★★★★★</td></tr><tr><td>6</td><td>过期删除与淘汰策略</td><td>惰性删除+定期采样，8种淘汰(LRU/LFU/TTL/随机)</td><td>★★★★☆</td></tr><tr><td>7</td><td>主从复制</td><td>PSYNC增量同步(repl_backlog)，全量(RDB传输)</td><td>★★★☆☆</td></tr><tr><td>8</td><td>Cluster 集群</td><td>16384 hash slot + gossip + MOVED/ASK 重定向</td><td>★★★★☆</td></tr><tr><td>9</td><td>Redis 6.0 多线程 IO</td><td>主线程+IO线程分工，命令执行仍单线程</td><td>★★★☆☆</td></tr><tr><td>10</td><td>大Key/热Key 问题</td><td>SCAN扫描+拆分+本地缓存</td><td>★★★★☆</td></tr></tbody></table><hr><h2 id="_1-基础题" tabindex="-1">1. 基础题 <a class="header-anchor" href="#_1-基础题" aria-label="Permalink to &quot;1. 基础题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q1-redis-有哪些基本数据类型-各自底层编码是什么" tabindex="-1">Q1：Redis 有哪些基本数据类型？各自底层编码是什么？ <a class="header-anchor" href="#q1-redis-有哪些基本数据类型-各自底层编码是什么" aria-label="Permalink to &quot;Q1：Redis 有哪些基本数据类型？各自底层编码是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>数据类型</th><th>小数据编码</th><th>大数据编码</th></tr></thead><tbody><tr><td>String</td><td>int / embstr (≤44B)</td><td>raw (SDS)</td></tr><tr><td>List</td><td>listpack</td><td>quicklist</td></tr><tr><td>Hash</td><td>listpack</td><td>hashtable (dict)</td></tr><tr><td>Set</td><td>intset（纯整数）</td><td>hashtable (dict)</td></tr><tr><td>ZSet (Sorted Set)</td><td>listpack</td><td>skiplist + hashtable</td></tr></tbody></table><p>关键点：Redis 会根据元素数量和大小动态切换编码。例如 Hash 在字段数 ≤ 128 且值长度 ≤ 64B 时使用 listpack，超过任一阈值自动升级为 hashtable。</p><hr><h3 id="q2-redis-为什么快" tabindex="-1">Q2：Redis 为什么快？ <a class="header-anchor" href="#q2-redis-为什么快" aria-label="Permalink to &quot;Q2：Redis 为什么快？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li><strong>纯内存操作</strong>：所有数据存在 RAM，读写无磁盘 IO 瓶颈</li><li><strong>单线程命令执行</strong>：无锁竞争、无上下文切换开销（命令执行阶段）</li><li><strong>IO 多路复用</strong>：单线程用 epoll 管理成千上万并发连接</li><li><strong>高效数据结构</strong>：针对不同场景使用最优底层编码（ziplist/listpack/skiplist 等）</li><li><strong>非阻塞 IO + 事件驱动</strong>：ae 事件库将网络 IO 和定时任务统一调度</li></ol><hr><h3 id="q3-rdb-和-aof-的区别是什么-各自适用场景" tabindex="-1">Q3：RDB 和 AOF 的区别是什么？各自适用场景？ <a class="header-anchor" href="#q3-rdb-和-aof-的区别是什么-各自适用场景" aria-label="Permalink to &quot;Q3：RDB 和 AOF 的区别是什么？各自适用场景？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>对比项</th><th>RDB</th><th>AOF</th></tr></thead><tbody><tr><td>数据格式</td><td>二进制快照</td><td>写命令文本（RESP 格式）</td></tr><tr><td>数据安全</td><td>可能丢失最后一次快照后的数据</td><td><code>everysec</code> 最多丢 1 秒</td></tr><tr><td>文件大小</td><td>小（压缩二进制）</td><td>大（尤其写命令频繁时）</td></tr><tr><td>恢复速度</td><td>快（直接加载二进制）</td><td>慢（需逐条回放命令）</td></tr><tr><td>适用场景</td><td>定期备份、允许丢分钟级数据</td><td>对数据安全要求高</td></tr></tbody></table><p><strong>推荐</strong>：生产环境开启混合持久化（<code>aof-use-rdb-preamble yes</code>），兼顾安全性和恢复速度。</p><hr><h3 id="q4-redis-支持哪些过期键删除策略" tabindex="-1">Q4：Redis 支持哪些过期键删除策略？ <a class="header-anchor" href="#q4-redis-支持哪些过期键删除策略" aria-label="Permalink to &quot;Q4：Redis 支持哪些过期键删除策略？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li><strong>惰性删除（Lazy Expiry）</strong>：访问 key 时调用 <code>expireIfNeeded()</code> 检查是否过期，已过期则删除。节省 CPU，但可能造成内存泄漏（从不访问的 key 不会被删除）。</li><li><strong>定期主动删除（Active Expiry）</strong>：<code>serverCron</code> 调用 <code>activeExpireCycle()</code>，定期随机采样带有 expire 的 key，批量删除已过期的 key，防止内存无限增长。</li></ol><hr><h3 id="q5-redis-有哪些内存淘汰策略" tabindex="-1">Q5：Redis 有哪些内存淘汰策略？ <a class="header-anchor" href="#q5-redis-有哪些内存淘汰策略" aria-label="Permalink to &quot;Q5：Redis 有哪些内存淘汰策略？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong> 共 8 种，由 <code>maxmemory-policy</code> 配置：</p><ul><li><code>noeviction</code>：不淘汰，写操作返回 OOM 错误</li><li><code>allkeys-lru</code>：对所有 key 用近似 LRU 淘汰</li><li><code>volatile-lru</code>：只对设置了 expire 的 key 用近似 LRU 淘汰</li><li><code>allkeys-lfu</code>：对所有 key 用近似 LFU 淘汰</li><li><code>volatile-lfu</code>：只对设置了 expire 的 key 用近似 LFU 淘汰</li><li><code>allkeys-random</code>：随机淘汰任意 key</li><li><code>volatile-random</code>：随机淘汰有 expire 的 key</li><li><code>volatile-ttl</code>：淘汰 TTL 最短的 key</li></ul><hr><h3 id="q6-redis-的主从复制是如何工作的" tabindex="-1">Q6：Redis 的主从复制是如何工作的？ <a class="header-anchor" href="#q6-redis-的主从复制是如何工作的" aria-label="Permalink to &quot;Q6：Redis 的主从复制是如何工作的？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li>Slave 连接 Master，发送 <code>PSYNC &lt;replid&gt; &lt;offset&gt;</code></li><li>若 offset 在 Master 的复制积压缓冲区（repl_backlog）范围内 → <strong>部分重同步（Partial Resync）</strong>，发送增量数据</li><li>否则 → <strong>全量重同步（Full Resync）</strong>：Master fork 子进程生成 RDB，发给 Slave；期间新命令缓存在 <code>repl_backlog</code> 中，RDB 传输完后补发</li><li>之后 Master 的每条写命令都会实时传播给所有 Slave（命令传播阶段）</li></ol><hr><h2 id="_2-进阶题" tabindex="-1">2. 进阶题 <a class="header-anchor" href="#_2-进阶题" aria-label="Permalink to &quot;2. 进阶题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q7-解释-redis-dict-的渐进式-rehash-原理" tabindex="-1">Q7：解释 Redis dict 的渐进式 Rehash 原理 <a class="header-anchor" href="#q7-解释-redis-dict-的渐进式-rehash-原理" aria-label="Permalink to &quot;Q7：解释 Redis dict 的渐进式 Rehash 原理&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>Redis 的 dict 包含两个 hashtable（<code>ht[0]</code> 和 <code>ht[1]</code>）。Rehash 触发条件：</p><ul><li>扩容：<code>load factor &gt; 1</code>（BGSAVE/BGREWRITEAOF 时放宽到 <code>&gt; 5</code>）</li><li>缩容：<code>load factor &lt; 0.1</code></li></ul><p>渐进式流程：</p><ol><li>分配新的 <code>ht[1]</code>（容量为 2 的幂，≥ <code>ht[0].used * 2</code>）</li><li>设置 <code>rehashidx = 0</code>，标志 rehash 开始</li><li><strong>每次 CRUD 操作</strong>时，迁移 <code>ht[0].table[rehashidx]</code> 整个链表到 <code>ht[1]</code>，<code>rehashidx++</code></li><li><code>serverCron</code> 在空闲时也会批量迁移（最多 1ms 内迁移 100 步）</li><li><code>ht[0].used == 0</code> 时，交换 <code>ht[0] = ht[1]</code>，清空 <code>ht[1]</code></li></ol><p><strong>双表并存期间</strong>：读操作先查 <code>ht[0]</code> 再查 <code>ht[1]</code>；写操作（新增）只写 <code>ht[1]</code>。</p><hr><h3 id="q8-sds-相比-c-字符串有哪些优势-attribute-packed-的作用是什么" tabindex="-1">Q8：SDS 相比 C 字符串有哪些优势？<code>__attribute__((__packed__))</code> 的作用是什么？ <a class="header-anchor" href="#q8-sds-相比-c-字符串有哪些优势-attribute-packed-的作用是什么" aria-label="Permalink to &quot;Q8：SDS 相比 C 字符串有哪些优势？\`__attribute__((__packed__))\` 的作用是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>SDS 优势：</p><ul><li>O(1) 获取长度（<code>len</code> 字段，无需 <code>strlen</code>）</li><li>自动扩容 + 空间预分配（<code>alloc</code> 字段），避免频繁 realloc</li><li>二进制安全（以 <code>len</code> 计长度，不被 <code>\\0</code> 截断）</li><li>5 种头部类型（<code>sdshdr5/8/16/32/64</code>），按字符串长度选择最小头部</li></ul><p><code>__attribute__((__packed__))</code> 禁止编译器为结构体字段做内存对齐填充，保证 <code>sdshdr8</code> 的 <code>buf[-1]</code> 精确指向 <code>flags</code> 字段，SDS API 通过 <code>buf[-1]</code> 做 O(1) 类型判断，无需传递头指针。</p><hr><h3 id="q9-ziplist-的连锁更新-cascade-update-是什么-listpack-如何解决这个问题" tabindex="-1">Q9：ziplist 的连锁更新（cascade update）是什么？listpack 如何解决这个问题？ <a class="header-anchor" href="#q9-ziplist-的连锁更新-cascade-update-是什么-listpack-如何解决这个问题" aria-label="Permalink to &quot;Q9：ziplist 的连锁更新（cascade update）是什么？listpack 如何解决这个问题？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>ziplist 连锁更新</strong>：每个 entry 用 <code>prevrawlen</code> 字段存储前一个 entry 的长度。若前一个 entry 长度从 &lt; 254 增长到 ≥ 254，当前 entry 的 <code>prevrawlen</code> 需从 1 字节扩展为 5 字节（+4B）。这可能导致当前 entry 的下一个 entry 也需要扩展，最坏情况 N 个 entry 全部触发 realloc，时间复杂度 O(N²)。</p><p><strong>listpack 解决方案</strong>：每个 lp-entry 尾部存储<strong>自身的总长度</strong>（<code>backlen</code>），不再存储前一个 entry 的长度。从尾部向前遍历时，通过 <code>backlen</code> 字段定位前一个 entry 的起始位置。任何 entry 的增删改<strong>不会影响其他 entry</strong>，彻底消除连锁更新风险。</p><hr><h3 id="q10-redis-的近似-lru-和精确-lru-有什么区别-redis-为什么不用精确-lru" tabindex="-1">Q10：Redis 的近似 LRU 和精确 LRU 有什么区别？Redis 为什么不用精确 LRU？ <a class="header-anchor" href="#q10-redis-的近似-lru-和精确-lru-有什么区别-redis-为什么不用精确-lru" aria-label="Permalink to &quot;Q10：Redis 的近似 LRU 和精确 LRU 有什么区别？Redis 为什么不用精确 LRU？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>精确 LRU</strong> 需要维护一个全局 LRU 链表，每次访问需要将 key 移到链表头部，每次淘汰从链表尾部取出。对于 Redis 这种海量 key 场景，维护双向链表的内存开销巨大（每个 key 需要额外 16B 指针），且每次操作需要修改指针（写密集型，CPU cache 不友好）。</p><p><strong>Redis 近似 LRU</strong>（<code>eviction.c: evictionPoolPopulate</code>）：</p><ul><li>每个 robj 有 24 位 <code>lru</code> 字段存储最后访问的秒级时间戳</li><li>淘汰时随机采样 <code>maxmemory-samples</code>（默认 5）个 key，插入 16 个元素的候选池</li><li>从候选池中选 idle time 最长的淘汰</li></ul><p>误差可控（samples=10 时近似精度接近精确 LRU），内存零额外开销，性能极高。</p><hr><h3 id="q11-aof-重写的原理是什么-重写期间主进程如何保证数据不丢" tabindex="-1">Q11：AOF 重写的原理是什么？重写期间主进程如何保证数据不丢？ <a class="header-anchor" href="#q11-aof-重写的原理是什么-重写期间主进程如何保证数据不丢" aria-label="Permalink to &quot;Q11：AOF 重写的原理是什么？重写期间主进程如何保证数据不丢？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>AOF 重写（<code>BGREWRITEAOF</code>）目的：压缩 AOF 文件，去除冗余命令（如多次 SET 只保留最后一条）。</p><p>流程：</p><ol><li>主进程 <code>fork()</code> 子进程</li><li>子进程遍历内存中所有 db 的 key-value，为每个 key 生成最简命令写入新 AOF 文件</li><li><strong>重写期间</strong>，主进程新的写命令同时追加到两个缓冲区： <ul><li><code>server.aof_buf</code>：继续写入旧 AOF 文件（保证旧文件可用）</li><li><code>server.aof_rewrite_buf</code>：积累增量</li></ul></li><li>子进程完成后，主进程将 <code>aof_rewrite_buf</code> 追加到新 AOF 文件</li><li><code>rename()</code> 原子替换旧文件</li></ol><p>这样即使子进程生成快照期间主进程崩溃，旧 AOF 文件仍然完整，保证数据安全。</p><hr><h3 id="q12-redis-cluster-的-hash-slot-是如何计算的-什么是-hash-tag" tabindex="-1">Q12：Redis Cluster 的 hash slot 是如何计算的？什么是 hash tag？ <a class="header-anchor" href="#q12-redis-cluster-的-hash-slot-是如何计算的-什么是-hash-tag" aria-label="Permalink to &quot;Q12：Redis Cluster 的 hash slot 是如何计算的？什么是 hash tag？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>Redis Cluster 将键空间划分为 16384 个槽（<code>CLUSTER_SLOTS = 16384</code>）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>slot = CRC16(key) &amp; 0x3FFF</span></span></code></pre></div><p><strong>Hash tag</strong>：若 key 中包含 <code>{...}</code>，只对 <code>{}</code> 内的字符串计算 CRC16：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SET {user}:1001 &quot;Alice&quot;  → CRC16(&quot;user&quot;) &amp; 0x3FFF</span></span>
<span class="line"><span>SET {user}:1002 &quot;Bob&quot;    → CRC16(&quot;user&quot;) &amp; 0x3FFF  ← 同一个槽！</span></span></code></pre></div><p>Hash tag 保证相关 key 落在同一节点，使得跨 key 的 MGET/MSET/事务等操作成为可能（同一槽 = 同一节点，无需跨节点协调）。</p><hr><h2 id="_3-高级题" tabindex="-1">3. 高级题 <a class="header-anchor" href="#_3-高级题" aria-label="Permalink to &quot;3. 高级题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q13-redis-的跳表-skiplist-为什么选-p-0-25-而不是-p-0-5-span-字段有什么用" tabindex="-1">Q13：Redis 的跳表（skiplist）为什么选 p=0.25 而不是 p=0.5？<code>span</code> 字段有什么用？ <a class="header-anchor" href="#q13-redis-的跳表-skiplist-为什么选-p-0-25-而不是-p-0-5-span-字段有什么用" aria-label="Permalink to &quot;Q13：Redis 的跳表（skiplist）为什么选 p=0.25 而不是 p=0.5？\`span\` 字段有什么用？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>跳表节点层数由 <code>zslRandomLevel()</code> 以概率 <code>p</code> 决定每层是否晋升：</p><ul><li><code>p=0.5</code>：平均每个节点 2 层，每层平均节点数为 N/2^L，理论 O(log N) 查找</li><li><code>p=0.25</code>：平均每个节点 1.33 层，内存占用减少约 33%，查找仍是 O(log N)，但常数略大</li></ul><p>Redis 选 <code>p=0.25</code> 是内存效率优先的权衡（Redis 文档注释：<code>ZSKIPLIST_P = 0.25</code>，对内存更友好）。</p><p><strong><code>span</code> 字段</strong>：每个 level 的 <code>forward</code> 指针附带 <code>span</code>，记录从当前节点到 <code>forward</code> 节点之间跳过了多少个节点（第 0 层相邻节点 span=1）。<code>ZRANK</code> 命令通过累加 span 计算节点排名，时间复杂度 O(log N)；<code>ZRANGE ... BYSCORE</code> 也依赖 span 快速定位范围边界。</p><hr><h3 id="q14-redis-6-0-多线程-io-的架构是怎样的-为什么命令执行还是单线程" tabindex="-1">Q14：Redis 6.0 多线程 IO 的架构是怎样的？为什么命令执行还是单线程？ <a class="header-anchor" href="#q14-redis-6-0-多线程-io-的架构是怎样的-为什么命令执行还是单线程" aria-label="Permalink to &quot;Q14：Redis 6.0 多线程 IO 的架构是怎样的？为什么命令执行还是单线程？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>Redis 6.0 的多线程 IO 架构：</p><ul><li><strong>主线程</strong>：<code>epoll_wait</code> 检测可读 fd → 将 client 分配给 IO 线程 → 等待 IO 线程完成 → <strong>单线程执行所有命令</strong> → 将需回复的 client 分配给 IO 线程写回</li><li><strong>IO 线程</strong>：仅负责 <code>read()/parse()</code>（读取并解析请求）和 <code>write()</code>（写回响应），不执行任何命令</li></ul><p>命令执行保持单线程的原因：</p><ol><li>Redis 的高性能主要瓶颈在网络 IO，不在命令执行（绝大多数命令 O(1) 或 O(log N)）</li><li>单线程执行无需任何数据结构加锁，代码复杂度极低</li><li>避免并发命令带来的原子性问题（MULTI/EXEC、Lua 脚本的隔离性依赖单线程）</li></ol><p>实测：IO 多线程对读多写少场景吞吐量提升 ~60%-100%（取决于 pipeline 大小和连接数）。</p><hr><h3 id="q15-sentinel-的-sdown-odown-机制-以及-leader-选举是如何工作的" tabindex="-1">Q15：Sentinel 的 SDOWN/ODOWN 机制，以及 Leader 选举是如何工作的？ <a class="header-anchor" href="#q15-sentinel-的-sdown-odown-机制-以及-leader-选举是如何工作的" aria-label="Permalink to &quot;Q15：Sentinel 的 SDOWN/ODOWN 机制，以及 Leader 选举是如何工作的？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>SDOWN（主观下线）</strong>：单个 Sentinel 在 <code>down-after-milliseconds</code> 内持续收不到 Master 的有效回复（PING 无 PONG、错误响应），将该 Master 标记为 SDOWN。</p><p><strong>ODOWN（客观下线）</strong>：发现 SDOWN 的 Sentinel 向其他 Sentinel 发送 <code>SENTINEL is-master-down-by-addr</code> 请求，若收到 <code>≥ quorum</code> 个 Sentinel 认同（回复 is_down=1），升级为 ODOWN，触发故障转移流程。</p><p><strong>Leader 选举（类 Raft）</strong>：</p><ol><li>检测到 ODOWN 的 Sentinel 在新 <code>epoch</code> 中请求其他 Sentinel 投票给自己（发送含自身 <code>runid</code> 的 <code>is-master-down-by-addr</code>）</li><li>每个 Sentinel 在每个 epoch 只投一票（先到先得）</li><li>获得 <code>&gt; N/2 + 1</code> 票的 Sentinel 成为 Leader，执行 failover：选最优 Slave → <code>REPLICAOF NO ONE</code> → 广播新主节点给其他 Slave</li></ol><hr><h3 id="q16-解释-lfu-计数器的对数自增和衰减逻辑-为什么用对数概率而不是线性自增" tabindex="-1">Q16：解释 LFU 计数器的对数自增和衰减逻辑，为什么用对数概率而不是线性自增？ <a class="header-anchor" href="#q16-解释-lfu-计数器的对数自增和衰减逻辑-为什么用对数概率而不是线性自增" aria-label="Permalink to &quot;Q16：解释 LFU 计数器的对数自增和衰减逻辑，为什么用对数概率而不是线性自增？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>对数自增（<code>LFULogIncr</code>）</strong>：</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">double</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> p </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1.0</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> /</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (baseval </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> lfu_log_factor </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">rand</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> p) counter</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">++</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><code>baseval = counter - 5</code>（初始值 5），<code>lfu_log_factor</code> 默认 10。计数器越高，自增概率越小（对数增长）。</p><p><strong>原因</strong>：若线性自增，访问 1 万次的 key 和访问 100 次的 key，计数器差距为 9900，但 8 位计数器只能表示 0~255，很快溢出。对数方案让计数器在 255 范围内区分 <code>10^(255/10)</code> ≈ 10^25 量级的访问频率差异，实用范围远超线性方案。</p><p><strong>衰减（<code>LFUDecrAndReturn</code>）</strong>：计算从上次访问到现在经过了多少个 <code>lfu-decay-time</code> 分钟，每经过一个周期计数器减 1。确保历史热点 key 不永久占据高位，新兴热点能够被淘汰策略识别。</p><hr><h3 id="q17-watch-multi-exec-实现乐观锁的原理-和数据库的乐观锁有何异同" tabindex="-1">Q17：WATCH + MULTI/EXEC 实现乐观锁的原理，和数据库的乐观锁有何异同？ <a class="header-anchor" href="#q17-watch-multi-exec-实现乐观锁的原理-和数据库的乐观锁有何异同" aria-label="Permalink to &quot;Q17：WATCH + MULTI/EXEC 实现乐观锁的原理，和数据库的乐观锁有何异同？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>Redis 实现原理</strong>：</p><ul><li><code>WATCH key</code> → 在 <code>db-&gt;watched_keys[key]</code> 中记录当前 client</li><li>任何修改 key 的操作（<code>dbAdd/dbOverwrite/dbDelete</code>）→ 调用 <code>touchWatchedKey()</code> → 为所有监听该 key 的 client 设置 <code>CLIENT_DIRTY_CAS</code> flag</li><li><code>EXEC</code> 执行时检查 <code>CLIENT_DIRTY_CAS</code> → 若已设置则返回 nil，事务取消</li></ul><p><strong>与数据库乐观锁对比</strong>：</p><table tabindex="0"><thead><tr><th>维度</th><th>Redis WATCH</th><th>数据库乐观锁（CAS/version）</th></tr></thead><tbody><tr><td>冲突检测粒度</td><td>key 级别</td><td>行/字段级别</td></tr><tr><td>版本号</td><td>隐式（脏标记）</td><td>显式 version 字段</td></tr><tr><td>冲突后</td><td>直接取消事务，客户端重试</td><td>更新失败，客户端重试</td></tr><tr><td>适用场景</td><td>低并发冲突场景</td><td>低并发冲突场景</td></tr><tr><td>ABA 问题</td><td>存在（key 改回原值仍触发冲突）</td><td>用版本号可解决</td></tr></tbody></table><hr><h3 id="q18-redis-的-cluster-模式下-moved-和-ask-重定向有什么区别" tabindex="-1">Q18：Redis 的 CLUSTER 模式下，MOVED 和 ASK 重定向有什么区别？ <a class="header-anchor" href="#q18-redis-的-cluster-模式下-moved-和-ask-重定向有什么区别" aria-label="Permalink to &quot;Q18：Redis 的 CLUSTER 模式下，MOVED 和 ASK 重定向有什么区别？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>重定向</th><th>触发场景</th><th>客户端处理</th><th>是否更新路由表</th></tr></thead><tbody><tr><td><code>MOVED slot ip:port</code></td><td>slot 永久在目标节点</td><td>直接向目标发请求</td><td><strong>是</strong>，更新本地 slot 路由</td></tr><tr><td><code>ASK slot ip:port</code></td><td>slot 正在迁移中（源节点的这个 key 已迁走）</td><td>先发 ASKING，再发请求</td><td><strong>否</strong>，临时一次性重定向</td></tr></tbody></table><p><strong>ASK 场景细节</strong>：</p><ul><li>迁移中，源节点标记为 <code>MIGRATING</code>，目标节点标记为 <code>IMPORTING</code></li><li>对已迁移的 key，源节点返回 <code>-ASK</code></li><li>目标节点的 IMPORTING 状态会拦截正常请求（返回 MOVED 回源节点），但 <code>ASKING</code> 命令可解除拦截</li><li>迁移完成后，slot 归属更新，所有节点恢复正常 MOVED 行为</li></ul><hr><h2 id="_4-场景题" tabindex="-1">4. 场景题 <a class="header-anchor" href="#_4-场景题" aria-label="Permalink to &quot;4. 场景题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q19-如何用-redis-实现分布式锁-有哪些坑" tabindex="-1">Q19：如何用 Redis 实现分布式锁？有哪些坑？ <a class="header-anchor" href="#q19-如何用-redis-实现分布式锁-有哪些坑" aria-label="Permalink to &quot;Q19：如何用 Redis 实现分布式锁？有哪些坑？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>基础实现（SET NX + PX）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SET lock_key unique_value NX PX 30000</span></span></code></pre></div><ul><li><code>NX</code>：key 不存在时才设置（获取锁）</li><li><code>PX 30000</code>：30 秒自动过期（防止客户端崩溃后锁不释放）</li><li><code>unique_value</code>：客户端唯一标识，确保只有加锁者能解锁</li></ul><p><strong>解锁（Lua 脚本保证原子性）</strong>：</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;GET&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;DEL&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">else</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span></code></pre></div><p><strong>主要坑</strong>：</p><ol><li><strong>过期时间设置</strong>：业务执行时间 &gt; 锁过期时间 → 锁提前释放 → 并发问题。需加看门狗（watchdog）续期</li><li><strong>主从切换</strong>：Master 设置锁后崩溃，Slave 升级为 Master 前锁未同步 → 另一客户端可以加锁（重复加锁）。RedLock 算法（向 N 个独立 Redis 节点加锁，多数成功才算获得锁）可缓解，但有争议</li><li><strong>网络分区</strong>：Cluster 模式下需注意 MOVED 重定向对锁可见性的影响</li></ol><hr><h3 id="q20-redis-实现消息队列有哪些方案-各自优缺点" tabindex="-1">Q20：Redis 实现消息队列有哪些方案？各自优缺点？ <a class="header-anchor" href="#q20-redis-实现消息队列有哪些方案-各自优缺点" aria-label="Permalink to &quot;Q20：Redis 实现消息队列有哪些方案？各自优缺点？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>方案</th><th>优点</th><th>缺点</th><th>适用场景</th></tr></thead><tbody><tr><td>List（LPUSH/BRPOP）</td><td>简单，BRPOP 阻塞等待</td><td>消费后消息丢失，不支持多消费者组</td><td>简单任务队列</td></tr><tr><td>Pub/Sub</td><td>支持广播，多订阅者</td><td>无持久化，消费者离线丢消息</td><td>实时通知</td></tr><tr><td>Sorted Set（延迟队列）</td><td>支持定时任务，按 score(时间戳)排序</td><td>需轮询，不支持 blocking</td><td>延迟任务</td></tr><tr><td>Stream（Redis 5.0+）</td><td>持久化、消费组、ACK 确认、历史回溯</td><td>相对复杂</td><td>生产级消息队列</td></tr></tbody></table><p><strong>Redis Stream 关键特性</strong>：</p><ul><li><code>XADD</code> 写入消息（自动生成 <code>timestamp-seq</code> 格式 ID）</li><li><code>XGROUP CREATE</code> 创建消费组</li><li><code>XREADGROUP</code> 读取消息，消费后需 <code>XACK</code> 确认</li><li><code>XPENDING</code> 查看未确认消息，支持 Dead Letter 处理</li></ul><hr><h3 id="q21-你们生产环境-redis-出现过内存爆满的情况-如何排查和处理" tabindex="-1">Q21：你们生产环境 Redis 出现过内存爆满的情况，如何排查和处理？ <a class="header-anchor" href="#q21-你们生产环境-redis-出现过内存爆满的情况-如何排查和处理" aria-label="Permalink to &quot;Q21：你们生产环境 Redis 出现过内存爆满的情况，如何排查和处理？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答（排查思路）：</strong></p><ol><li><strong><code>INFO memory</code></strong>：查看 <code>used_memory_human</code>（实际使用）、<code>mem_fragmentation_ratio</code>（碎片率，&gt; 1.5 说明碎片严重）</li><li><strong><code>INFO keyspace</code></strong>：查看各 db 的 key 数量和带 expire 的 key 数</li><li><strong><code>redis-cli --bigkeys</code></strong>：扫描大 key（可能是没有 expire 的大 list/hash/set）</li><li><strong><code>OBJECT ENCODING key</code></strong>：确认热点 key 的编码（listpack 升级为 hashtable 会大幅增加内存）</li><li><strong><code>DEBUG SLEEP</code></strong> + <code>MEMORY DOCTOR</code>：Redis 4.0+ 内置内存分析</li></ol><p><strong>处理方案</strong>：</p><ul><li><strong>短期</strong>：<code>maxmemory</code> + 合适的淘汰策略（<code>allkeys-lru</code> 或 <code>volatile-lru</code>）</li><li><strong>中期</strong>：清理大 key（SCAN + DEL，避免 KEYS * 阻塞），为无 expire 的 key 设置 TTL</li><li><strong>长期</strong>：数据分片（Cluster）、冷热数据分层（Redis + 持久化存储），合理使用 Hash/Set 代替大量小 key</li></ul><hr><h3 id="q22-redis-单线程执行模型下-有哪些操作会导致线上阻塞-如何避免" tabindex="-1">Q22：Redis 单线程执行模型下，有哪些操作会导致线上阻塞？如何避免？ <a class="header-anchor" href="#q22-redis-单线程执行模型下-有哪些操作会导致线上阻塞-如何避免" aria-label="Permalink to &quot;Q22：Redis 单线程执行模型下，有哪些操作会导致线上阻塞？如何避免？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答（高危命令及替代方案）：</strong></p><table tabindex="0"><thead><tr><th>危险命令</th><th>时间复杂度</th><th>替代方案</th></tr></thead><tbody><tr><td><code>KEYS pattern</code></td><td>O(N) 全量扫描</td><td><code>SCAN cursor MATCH pattern COUNT 100</code>（渐进式）</td></tr><tr><td><code>FLUSHDB / FLUSHALL</code></td><td>O(N)</td><td><code>FLUSHDB ASYNC</code>（后台线程异步删除，Redis 4.0+）</td></tr><tr><td><code>DEL</code> 大 key</td><td>O(N)（集合类型）</td><td><code>UNLINK</code>（异步删除，非阻塞，Redis 4.0+）</td></tr><tr><td><code>LRANGE 0 -1</code> 超大 list</td><td>O(N)</td><td>分页读取，或使用 Stream</td></tr><tr><td><code>SORT</code> 大集合</td><td>O(N log N)</td><td>提前在应用层排序</td></tr><tr><td><code>SMEMBERS</code> 大 set</td><td>O(N)</td><td><code>SSCAN</code> 渐进式遍历</td></tr><tr><td>AOF <code>fsync always</code></td><td>每写一次 fsync</td><td>改为 <code>everysec</code>（最多丢 1 秒）</td></tr><tr><td>fork（BGSAVE/BGREWRITEAOF）</td><td>内存越大，fork COW 开销越大</td><td>控制单实例内存 ≤ 10GB，避免在写高峰触发</td></tr></tbody></table><hr><h2 id="_5-深度补充题-q23-q32" tabindex="-1">5. 深度补充题（Q23-Q32） <a class="header-anchor" href="#_5-深度补充题-q23-q32" aria-label="Permalink to &quot;5. 深度补充题（Q23-Q32）&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>数据结构底层实现、持久化深度机制、集群架构细节、高并发实战与综合场景设计。</p></blockquote><h3 id="q23-sds-simple-dynamic-string-为什么设计了-sdshdr5-8-16-32-64-五种头部-这和-attribute-packed-有什么关系-⭐⭐⭐" tabindex="-1">Q23：SDS（Simple Dynamic String）为什么设计了 sdshdr5/8/16/32/64 五种头部？这和 <code>__attribute__((__packed__))</code> 有什么关系？⭐⭐⭐ <a class="header-anchor" href="#q23-sds-simple-dynamic-string-为什么设计了-sdshdr5-8-16-32-64-五种头部-这和-attribute-packed-有什么关系-⭐⭐⭐" aria-label="Permalink to &quot;Q23：SDS（Simple Dynamic String）为什么设计了 sdshdr5/8/16/32/64 五种头部？这和 \`__attribute__((__packed__))\` 有什么关系？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>核心动机</strong>：Redis 存储大量短字符串（如 key 名），SDS 头部大小直接影响内存占用。五种头部根据字符串长度选择最紧凑的表示：</p><table tabindex="0"><thead><tr><th>类型</th><th>len/alloc 字段大小</th><th>最大长度</th><th>头部开销</th></tr></thead><tbody><tr><td>sdshdr5</td><td>不存储（flags 高5位）</td><td>31 字节</td><td>1 字节</td></tr><tr><td>sdshdr8</td><td>uint8_t</td><td>255 字节</td><td>3 字节</td></tr><tr><td>sdshdr16</td><td>uint16_t</td><td>64 KB</td><td>5 字节</td></tr><tr><td>sdshdr32</td><td>uint32_t</td><td>4 GB</td><td>9 字节</td></tr><tr><td>sdshdr64</td><td>uint64_t</td><td>2^64</td><td>17 字节</td></tr></tbody></table><p><strong><code>__attribute__((__packed__))</code> 的作用</strong>：</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">struct</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> __attribute__</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((__packed__)) sdshdr8 {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    uint8_t</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> len;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // 已使用长度</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    uint8_t</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> alloc;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // 分配的总长度（不含头部和 \\0）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    unsigned</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> char</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> flags;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> // 低 3 位标识类型</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    char</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> buf</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">[]</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        // 柔性数组，实际数据</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre></div><ul><li>取消编译器的结构体对齐填充（padding），确保字段之间没有空隙</li><li>这样可以通过 <code>buf[-1]</code> 直接访问 <code>flags</code> 字段，快速判断 SDS 类型</li><li>如果没有 <code>packed</code>，编译器可能在 <code>len</code> 和 <code>alloc</code> 之间插入填充字节，导致偏移量不可预测</li></ul><p><strong>类比</strong>：像信封分大小号——寄一张名片用小信封（sdshdr5），寄一本书用大信封（sdshdr32）。如果所有信都用最大信封，邮费（内存）会很浪费。</p><p><strong>追问</strong>：sdshdr5 实际在 Redis 中几乎不使用（创建时会升级为 sdshdr8），为什么保留？ → 历史原因 + 极端内存优化场景下可手动启用；<code>flags</code> 字段的低 3 位可以标识 0-4 五种类型，sdshdr5 不存储 len/alloc，而是将长度编码在 flags 的高 5 位中。</p></details><hr><h3 id="q24-listpack-的编码格式和-ziplist-有什么本质区别-quicklist-是如何结合两者优势的-⭐⭐⭐" tabindex="-1">Q24：listpack 的编码格式和 ziplist 有什么本质区别？quicklist 是如何结合两者优势的？⭐⭐⭐ <a class="header-anchor" href="#q24-listpack-的编码格式和-ziplist-有什么本质区别-quicklist-是如何结合两者优势的-⭐⭐⭐" aria-label="Permalink to &quot;Q24：listpack 的编码格式和 ziplist 有什么本质区别？quicklist 是如何结合两者优势的？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>ziplist 的致命缺陷——连锁更新</strong>：</p><p>ziplist 每个 entry 的 <code>prevlen</code> 字段记录前一个 entry 的长度：</p><ul><li>前一项 &lt; 254 字节 → <code>prevlen</code> 用 1 字节</li><li>前一项 ≥ 254 字节 → <code>prevlen</code> 用 5 字节</li></ul><p>当中间插入一个大 entry，后续所有 entry 的 <code>prevlen</code> 可能从 1 字节扩展为 5 字节，引发<strong>连锁更新</strong>，最坏 O(N²)。</p><p><strong>listpack 的解决方案</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[total-bytes] [num-elements] [entry1] [entry2] ... [end-byte]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>每个 entry:</span></span>
<span class="line"><span>[encoding-type] [data] [backlen]</span></span>
<span class="line"><span>                        ↑ 记录自己的长度，而非前一项</span></span></code></pre></div><ul><li><code>backlen</code>：记录<strong>当前 entry 自身的总长度</strong>（而非前一个 entry 的长度）</li><li>反向遍历时：用 <code>backlen</code> 回退到自己的起始位置，再减一个 <code>backlen</code> 的编码长度到前一个 entry 的末尾</li><li>关键改进：修改任意 entry 不会影响相邻 entry 的编码，<strong>彻底消除连锁更新</strong></li></ul><p><strong>quicklist = 双向链表 + listpack（Redis 7.0 前为 ziplist）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>quicklist:</span></span>
<span class="line"><span>  head → [listpack1] ↔ [listpack2] ↔ [listpack3] → tail</span></span>
<span class="line"><span></span></span>
<span class="line"><span>每个节点是一个 quicklistNode：</span></span>
<span class="line"><span>  - *listpack: 指向一个 listpack</span></span>
<span class="line"><span>  - count: 该 listpack 中的元素数</span></span>
<span class="line"><span>  - encoding: RAW 或 LZF 压缩</span></span></code></pre></div><p><strong>关键配置参数</strong>：</p><table tabindex="0"><thead><tr><th>参数</th><th>含义</th><th>推荐值</th></tr></thead><tbody><tr><td><code>list-max-listpack-size</code></td><td>正数=每个节点最大元素数；负数=-1(4KB)到-5(64KB)的大小限制</td><td>-2(8KB)</td></tr><tr><td><code>list-compress-depth</code></td><td>两端不压缩的节点数（0=不压缩，1=首尾不压缩其余LZF压缩）</td><td>1</td></tr></tbody></table><p><strong>设计思想</strong>：纯 listpack 在元素多时遍历慢（O(N)）；纯链表每个节点都有 prev/next 指针开销。quicklist 取折中——链表提供 O(1) 首尾操作，每个节点内用 listpack 保持内存紧凑。</p><p><strong>追问</strong>：ziplist → listpack 的迁移在 Redis 哪个版本完成？ → Redis 7.0。Hash/ZSet 的小数据编码从 ziplist 改为 listpack；List 类型底层早已使用 quicklist，7.0 后 quicklist 内部节点也从 ziplist 换为 listpack。</p></details><hr><h3 id="q25-跳表-skiplist-的概率分析——为什么-redis-选-p-0-25-期望层高和空间复杂度的推导过程是什么-⭐⭐⭐" tabindex="-1">Q25：跳表（skiplist）的概率分析——为什么 Redis 选 p=0.25？期望层高和空间复杂度的推导过程是什么？⭐⭐⭐ <a class="header-anchor" href="#q25-跳表-skiplist-的概率分析——为什么-redis-选-p-0-25-期望层高和空间复杂度的推导过程是什么-⭐⭐⭐" aria-label="Permalink to &quot;Q25：跳表（skiplist）的概率分析——为什么 Redis 选 p=0.25？期望层高和空间复杂度的推导过程是什么？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>跳表的层级构建</strong>：</p><p>每个新节点插入时，随机决定层高：以概率 p 升一层，以概率 (1-p) 停止。</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// Redis 源码 t_zset.c</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> zslRandomLevel</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> level </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    while</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ((</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">random</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> 0x</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">FFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (ZSKIPLIST_P </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> 0x</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">FFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">))</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        level </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (level </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ZSKIPLIST_MAXLEVEL) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> level </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ZSKIPLIST_MAXLEVEL;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ZSKIPLIST_P = 0.25, ZSKIPLIST_MAXLEVEL = 32</span></span></code></pre></div><p><strong>期望层高推导</strong>：</p><p>节点层高为 k 的概率：P(level = k) = p^(k-1) × (1-p)</p><p>期望层高 = Σ(k=1→∞) k × p^(k-1) × (1-p) = <strong>1 / (1-p)</strong></p><ul><li>p = 0.5 → 期望层高 = 2</li><li>p = 0.25 → 期望层高 = 1.33</li></ul><p><strong>空间开销分析</strong>：</p><p>每个节点平均指针数 = 1/(1-p)</p><ul><li>p = 0.5 → 平均 2 个指针/节点（额外空间 = 原始数据的 2 倍）</li><li>p = 0.25 → 平均 1.33 个指针/节点（额外空间 = 原始数据的 1.33 倍）</li></ul><p><strong>为什么选 p=0.25 而不是 p=0.5</strong>：</p><table tabindex="0"><thead><tr><th>指标</th><th>p=0.25</th><th>p=0.5</th></tr></thead><tbody><tr><td>期望层高</td><td>1.33</td><td>2.0</td></tr><tr><td>额外空间</td><td>33%</td><td>100%</td></tr><tr><td>查找比较次数</td><td>(log₄n)/2 + 1/(1-p) ≈ 略多</td><td>(log₂n)/2 + 1/(1-p)</td></tr><tr><td>实际性能</td><td>接近平衡树</td><td>略优但不明显</td></tr></tbody></table><p>p=0.25 用微小的查找性能代价，换来了<strong>约 50% 的内存节省</strong>。对于 Redis 这样的内存数据库，这是最优权衡。</p><p><strong>最大层高 32 的数学依据</strong>：</p><p>P(层高 &gt; 32) = 0.25^32 = 2^(-64) ≈ 5.4 × 10^(-20)</p><p>即使有 2^64 个元素（远超任何实际场景），期望中也只有约 1 个节点达到 32 层。所以 32 层的上限足够覆盖任意规模数据。</p><p><strong>追问</strong>：跳表的 span 字段有什么用？ → 每一层的前进指针都记录了 <code>span</code>（跨越的节点数），用于 O(log n) 计算元素的排名（rank）。<code>ZRANK</code> 命令就是沿着查找路径累加各层 span。</p></details><hr><h3 id="q26-aof-重写的-fork-cow-机制详解-重写期间的写命令如何保证不丢-rdb-的-fork-与-aof-重写的-fork-有何区别-混合持久化的文件格式是怎样的-⭐⭐⭐" tabindex="-1">Q26：AOF 重写的 fork + COW 机制详解：重写期间的写命令如何保证不丢？RDB 的 fork 与 AOF 重写的 fork 有何区别？混合持久化的文件格式是怎样的？⭐⭐⭐ <a class="header-anchor" href="#q26-aof-重写的-fork-cow-机制详解-重写期间的写命令如何保证不丢-rdb-的-fork-与-aof-重写的-fork-有何区别-混合持久化的文件格式是怎样的-⭐⭐⭐" aria-label="Permalink to &quot;Q26：AOF 重写的 fork + COW 机制详解：重写期间的写命令如何保证不丢？RDB 的 fork 与 AOF 重写的 fork 有何区别？混合持久化的文件格式是怎样的？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>AOF 重写的完整流程</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>主进程                              子进程</span></span>
<span class="line"><span>  │                                   │</span></span>
<span class="line"><span>  ├─ fork() ──────────────────────►  │ 子进程获得内存快照（COW 共享页）</span></span>
<span class="line"><span>  │                                   │ 遍历数据库，将当前数据写成新 AOF</span></span>
<span class="line"><span>  │                                   │</span></span>
<span class="line"><span>  ├─ 继续处理客户端命令                │</span></span>
<span class="line"><span>  │   写入：① 旧 AOF 文件             │</span></span>
<span class="line"><span>  │         ② AOF 重写缓冲区          │</span></span>
<span class="line"><span>  │                                   │</span></span>
<span class="line"><span>  │                                   ├─ 写完新 AOF 文件</span></span>
<span class="line"><span>  │ ◄─ 通知主进程 ─────────────────── │</span></span>
<span class="line"><span>  │                                   │</span></span>
<span class="line"><span>  ├─ 将 AOF 重写缓冲区追加到新 AOF     │</span></span>
<span class="line"><span>  ├─ 原子替换旧 AOF 文件               │</span></span>
<span class="line"><span>  └─ 完成                             └─ 子进程退出</span></span></code></pre></div><p><strong>COW（Copy-On-Write）机制</strong>：</p><ul><li>fork 时不复制物理内存，父子进程共享相同的内存页（只复制页表，非常快）</li><li>当主进程修改某个内存页时，操作系统才复制该页（写时复制）</li><li>因此 fork 的内存开销 ≈ 被修改的页数 × 4KB（页大小），而非全部数据</li></ul><p><strong>实际影响</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 关闭 Transparent Huge Pages（THP），避免 COW 粒度从 4KB 变为 2MB</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">echo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> never</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /sys/kernel/mm/transparent_hugepage/enabled</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 设置 vm.overcommit_memory = 1，允许 fork 即使内存不足</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sysctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> vm.overcommit_memory=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span></span></code></pre></div><p><strong>RDB fork vs AOF 重写 fork 的区别</strong>：</p><table tabindex="0"><thead><tr><th>对比项</th><th>RDB (BGSAVE)</th><th>AOF 重写 (BGREWRITEAOF)</th></tr></thead><tbody><tr><td>子进程做什么</td><td>将内存数据序列化为 RDB 二进制格式</td><td>将内存数据转写为 AOF 命令文本</td></tr><tr><td>写入内容</td><td>紧凑二进制</td><td>Redis 命令序列</td></tr><tr><td>重写缓冲区</td><td>不需要</td><td>需要（重写期间的增量命令）</td></tr><tr><td>文件体积</td><td>小</td><td>大（但比旧 AOF 小，去除了冗余命令）</td></tr><tr><td>恢复速度</td><td>快（直接加载二进制）</td><td>慢（逐条重放命令）</td></tr></tbody></table><p><strong>混合持久化（Redis 4.0+，<code>aof-use-rdb-preamble yes</code>）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>混合 AOF 文件格式：</span></span>
<span class="line"><span>┌─────────────────────┐</span></span>
<span class="line"><span>│   RDB 二进制数据     │  ← 子进程 fork 时的全量快照</span></span>
<span class="line"><span>├─────────────────────┤</span></span>
<span class="line"><span>│   AOF 增量命令       │  ← 重写期间主进程接收的新命令</span></span>
<span class="line"><span>└─────────────────────┘</span></span></code></pre></div><ul><li>恢复时：先加载 RDB 部分（快），再重放 AOF 增量部分（少量命令）</li><li>兼顾了 RDB 的恢复速度和 AOF 的数据安全性</li><li>文件头部有 <code>REDIS</code> 魔数标识 RDB 部分</li></ul><p><strong>追问</strong>：fork 期间如果写入量很大，COW 会导致内存翻倍吗？ → 理论上最坏情况是全部页被修改，内存翻倍。实际中，生产环境应控制单实例内存 ≤ 10GB，并避免在写高峰触发 BGSAVE/BGREWRITEAOF。可通过 <code>INFO persistence</code> 的 <code>latest_fork_usec</code> 监控 fork 耗时。</p></details><hr><h3 id="q27-redis-cluster-为什么是-16384-个-slot-而不是-65536-gossip-协议如何传播集群状态-脑裂场景下如何防止数据丢失-⭐⭐⭐" tabindex="-1">Q27：Redis Cluster 为什么是 16384 个 slot 而不是 65536？Gossip 协议如何传播集群状态？脑裂场景下如何防止数据丢失？⭐⭐⭐ <a class="header-anchor" href="#q27-redis-cluster-为什么是-16384-个-slot-而不是-65536-gossip-协议如何传播集群状态-脑裂场景下如何防止数据丢失-⭐⭐⭐" aria-label="Permalink to &quot;Q27：Redis Cluster 为什么是 16384 个 slot 而不是 65536？Gossip 协议如何传播集群状态？脑裂场景下如何防止数据丢失？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>为什么是 16384 个 slot</strong>：</p><p>Antirez（Redis 作者）在 GitHub issue 中给出了明确理由：</p><ol><li><p><strong>Gossip 消息大小</strong>：每个节点在心跳包中携带自己负责的 slot 位图</p><ul><li>16384 slots → 位图 = 16384/8 = <strong>2KB</strong></li><li>65536 slots → 位图 = 65536/8 = <strong>8KB</strong></li><li>集群节点间高频交换心跳（每秒多次），2KB 比 8KB 节省大量带宽</li></ul></li><li><p><strong>集群规模上限</strong>：Redis 官方建议集群不超过 1000 个节点</p><ul><li>16384 / 1000 ≈ 16 个 slot/节点，粒度足够</li><li>如果只有 3 个主节点，每个负责 ~5461 个 slot，完全够用</li></ul></li><li><p><strong>slot 迁移粒度</strong>：slot 数越多，单个 slot 包含的 key 越少，迁移更细粒度。但 16384 已经足够细。</p></li></ol><p><strong>Gossip 协议工作机制</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>节点 A 每秒随机选择若干节点发送 PING 消息：</span></span>
<span class="line"><span>┌──────────────────────────────────┐</span></span>
<span class="line"><span>│ PING 消息内容：                    │</span></span>
<span class="line"><span>│  - 发送者的 clusterState          │</span></span>
<span class="line"><span>│  - 发送者已知的部分节点信息        │</span></span>
<span class="line"><span>│    (随机选择一些节点的 IP/Port/    │</span></span>
<span class="line"><span>│     状态/负责的 slots)            │</span></span>
<span class="line"><span>│  - 发送者自己负责的 slots 位图     │</span></span>
<span class="line"><span>└──────────────────────────────────┘</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>节点 B 收到 PING 后回复 PONG（格式相同）</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>节点 B 根据收到的信息更新自己的 clusterState：</span></span>
<span class="line"><span>  - 发现新节点 → 记录</span></span>
<span class="line"><span>  - 发现 slot 归属变化 → 更新映射表</span></span>
<span class="line"><span>  - 发现节点失联 → 标记 PFAIL</span></span></code></pre></div><p><strong>状态传播的关键机制</strong>：</p><table tabindex="0"><thead><tr><th>机制</th><th>说明</th></tr></thead><tbody><tr><td>PFAIL</td><td>单个节点认为某节点下线（Possible Fail）</td></tr><tr><td>FAIL</td><td>超过半数主节点在 <code>cluster-node-timeout</code> 内报告 PFAIL → 标记为 FAIL</td></tr><tr><td>Epoch</td><td>每次配置变更（如 failover）递增 configEpoch，版本大的覆盖小的</td></tr></tbody></table><p><strong>脑裂（Split-Brain）防护</strong>：</p><p>脑裂场景：网络分区导致少数派主节点与多数派隔离，少数派继续接受写入 → 分区恢复后数据冲突。</p><p><strong>防护配置</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 当主节点发现自己连接的从节点数 &lt; min-replicas-to-write 时，拒绝写入</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min-replicas-to-write</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 从节点必须在 min-replicas-max-lag 秒内有过 ACK</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min-replicas-max-lag</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 10</span></span></code></pre></div><p><strong>原理</strong>：少数派分区中的主节点失去所有从节点连接，因不满足 <code>min-replicas-to-write</code> 条件而拒绝写入。这样即使脑裂，少数派不会产生脏数据。代价是在主从全部断开时写入不可用（选择 CP 而非 AP）。</p><p><strong>追问</strong>：Cluster 模式下如何实现跨 slot 的事务？ → 使用 Hash Tag：将相关 key 放在 <code>{tag}</code> 中，如 <code>user:{1001}:name</code> 和 <code>user:{1001}:age</code>，CRC16 只计算 <code>{1001}</code> 部分，保证落在同一个 slot，从而支持 MULTI/EXEC 事务和 Lua 脚本。</p></details><hr><h3 id="q28-pipeline、multi-exec-事务、lua-脚本三者在-原子性-上有什么区别-各自适用什么场景-⭐⭐⭐" tabindex="-1">Q28：Pipeline、MULTI/EXEC 事务、Lua 脚本三者在&quot;原子性&quot;上有什么区别？各自适用什么场景？⭐⭐⭐ <a class="header-anchor" href="#q28-pipeline、multi-exec-事务、lua-脚本三者在-原子性-上有什么区别-各自适用什么场景-⭐⭐⭐" aria-label="Permalink to &quot;Q28：Pipeline、MULTI/EXEC 事务、Lua 脚本三者在&quot;原子性&quot;上有什么区别？各自适用什么场景？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>三者对比</strong>：</p><table tabindex="0"><thead><tr><th>特性</th><th>Pipeline</th><th>MULTI/EXEC 事务</th><th>Lua 脚本</th></tr></thead><tbody><tr><td>原子性</td><td>❌ 无</td><td>⚠️ 弱（不支持回滚）</td><td>✅ 强（单线程串行执行）</td></tr><tr><td>隔离性</td><td>❌ 命令间可被插入</td><td>✅ EXEC 后命令连续执行</td><td>✅ 执行期间不会被打断</td></tr><tr><td>回滚</td><td>❌</td><td>❌ 部分失败不回滚</td><td>❌（但可以通过逻辑控制）</td></tr><tr><td>网络优化</td><td>✅ 批量发送减少 RTT</td><td>⚠️ 需逐条发送 QUEUED</td><td>✅ 一次发送脚本</td></tr><tr><td>条件逻辑</td><td>❌ 不能根据中间结果分支</td><td>❌ QUEUED 后不能修改</td><td>✅ 完整的 if/else/loop</td></tr><tr><td>Cluster 兼容</td><td>✅（同 slot）</td><td>✅（同 slot）</td><td>✅（同 slot + EVALSHA）</td></tr></tbody></table><p><strong>Pipeline 详解</strong>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Pipeline 只是客户端层面的批量发送，不保证原子性</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">pipe </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.pipeline(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">transaction</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">False</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">pipe.set(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;a&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">pipe.set(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;b&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">pipe.incr(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;a&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">results </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> pipe.execute()  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 一次性发送 3 条命令，减少 3 次 RTT 为 1 次</span></span></code></pre></div><ul><li>适用场景：批量读写无依赖关系的 key，追求吞吐量（如批量导入数据）</li><li>注意：Pipeline 中每条命令独立执行，中间可能被其他客户端的命令插入</li></ul><p><strong>MULTI/EXEC 事务</strong>：</p><div class="language-redis vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">redis</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WATCH balance           # 乐观锁：监视 key</span></span>
<span class="line"><span>MULTI                   # 开始事务，后续命令入队</span></span>
<span class="line"><span>DECRBY balance 100      # QUEUED（不立即执行）</span></span>
<span class="line"><span>INCRBY target 100       # QUEUED</span></span>
<span class="line"><span>EXEC                    # 原子执行所有 QUEUED 命令</span></span>
<span class="line"><span>                        # 如果 balance 在 WATCH 后被修改，EXEC 返回 nil（事务失败）</span></span></code></pre></div><ul><li>不支持回滚：如果 INCRBY 的 key 类型错误，DECRBY 已经执行的不会撤销</li><li>Redis 作者认为：事务中的错误是编程 bug，不应该在生产中依赖回滚</li></ul><p><strong>Lua 脚本</strong>：</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 原子性扣减库存：检查 + 扣减在一个脚本中</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stock </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;GET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]))</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stock </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">and</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stock </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;DECRBY&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 扣减成功</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">else</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 库存不足</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span></code></pre></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">EVALSHA</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">sha</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">1&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> product:stock:1001</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 5</span></span></code></pre></div><ul><li><strong>为什么是原子的</strong>：Redis 单线程执行模型，Lua 脚本执行期间不会处理其他客户端命令</li><li><strong>风险</strong>：长时间运行的 Lua 脚本会阻塞整个 Redis（默认 <code>lua-time-limit 5000</code>ms 后可被 <code>SCRIPT KILL</code> 中断，但如果脚本已执行写操作则只能 <code>SHUTDOWN NOSAVE</code>）</li><li>适用场景：需要&quot;读-判断-写&quot;原子操作的业务逻辑（如库存扣减、限流计数）</li></ul><p><strong>追问</strong>：Redis 7.0 的 Function 和 EVAL 有什么区别？ → Function 通过 <code>FUNCTION LOAD</code> 持久化注册到 Redis 服务器端，重启后仍存在，且支持 AOF/RDB 持久化。EVAL 每次都需要传脚本文本（EVALSHA 需要客户端预加载）。Function 是 Lua 脚本的生产级替代。</p></details><hr><h3 id="q29-redlock-算法的工作流程是什么-martin-kleppmann-的批评和-antirez-的回应分别是什么观点-生产中你会怎么选-⭐⭐⭐" tabindex="-1">Q29：Redlock 算法的工作流程是什么？Martin Kleppmann 的批评和 Antirez 的回应分别是什么观点？生产中你会怎么选？⭐⭐⭐ <a class="header-anchor" href="#q29-redlock-算法的工作流程是什么-martin-kleppmann-的批评和-antirez-的回应分别是什么观点-生产中你会怎么选-⭐⭐⭐" aria-label="Permalink to &quot;Q29：Redlock 算法的工作流程是什么？Martin Kleppmann 的批评和 Antirez 的回应分别是什么观点？生产中你会怎么选？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>Redlock 算法流程</strong>（需要 N 个独立 Redis 实例，通常 N=5）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>客户端获取锁：</span></span>
<span class="line"><span>1. 记录当前时间 T1</span></span>
<span class="line"><span>2. 依次向 N 个 Redis 实例发送 SET key value NX PX ttl</span></span>
<span class="line"><span>3. 记录当前时间 T2</span></span>
<span class="line"><span>4. 如果满足以下两个条件，则认为获取锁成功：</span></span>
<span class="line"><span>   a. 成功获取锁的实例数 ≥ N/2 + 1（多数派）</span></span>
<span class="line"><span>   b. 总耗时 (T2 - T1) &lt; 锁的 TTL</span></span>
<span class="line"><span>5. 实际锁的有效时间 = TTL - (T2 - T1)</span></span>
<span class="line"><span>6. 如果获取失败，向所有实例发送 DEL 释放锁</span></span></code></pre></div><p><strong>Martin Kleppmann 的批评（&quot;How to do distributed locking&quot;）</strong>：</p><ol><li><strong>GC 暂停问题</strong>：客户端获取锁后发生 Full GC（或进程暂停），锁在 GC 期间过期，另一个客户端获取了同一把锁 → 两个客户端同时持有锁</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client A: 获取锁 → [Full GC 30秒] → 以为还持有锁 → 写数据</span></span>
<span class="line"><span>                     ↑ 锁已过期</span></span>
<span class="line"><span>Client B:           获取锁 → 写数据 → 数据冲突！</span></span></code></pre></div><ol start="2"><li><p><strong>时钟跳变问题</strong>：Redlock 依赖各节点的时钟大致同步。如果某个节点时钟突然跳变（NTP 校准），锁可能提前过期。</p></li><li><p><strong>根本观点</strong>：分布式锁分两类用途——</p><ul><li><strong>效率锁</strong>（避免重复工作）：单节点 Redis 锁就够了，不需要 Redlock</li><li><strong>正确性锁</strong>（保证数据安全）：Redlock 不够安全，应该用 ZooKeeper/etcd + fencing token</li></ul></li></ol><p><strong>Antirez 的回应（&quot;Is Redlock safe?&quot;）</strong>：</p><ol><li><strong>GC 问题被夸大</strong>：合理设置 TTL + watchdog 续期可以缓解</li><li><strong>时钟跳变是可控的</strong>：禁用 NTP step mode，改用 slew mode（渐进式调整）</li><li><strong>Redlock 不需要精确时钟同步</strong>：只需要各节点的&quot;时钟漂移率&quot;在合理范围内</li></ol><p><strong>生产建议</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────┬──────────────────────────────────┐</span></span>
<span class="line"><span>│ 场景             │ 推荐方案                         │</span></span>
<span class="line"><span>├──────────────────┼──────────────────────────────────┤</span></span>
<span class="line"><span>│ 幂等性保护       │ 单节点 Redis + SET NX PX         │</span></span>
<span class="line"><span>│ （重复执行无害） │ + 看门狗续期（如 Redisson）       │</span></span>
<span class="line"><span>├──────────────────┼──────────────────────────────────┤</span></span>
<span class="line"><span>│ 金融级互斥       │ etcd/ZooKeeper + fencing token   │</span></span>
<span class="line"><span>│ （不容许重复执行）│ （共识协议保证，非时钟依赖）     │</span></span>
<span class="line"><span>├──────────────────┼──────────────────────────────────┤</span></span>
<span class="line"><span>│ 跨数据中心       │ etcd（Raft 协议原生支持）        │</span></span>
<span class="line"><span>│                  │ 不推荐 Redlock（网络延迟加剧问题）│</span></span>
<span class="line"><span>└──────────────────┴──────────────────────────────────┘</span></span></code></pre></div><p><strong>追问</strong>：什么是 fencing token？ → 锁服务每次授锁时返回一个单调递增的 token（如 ZooKeeper 的 zxid）。客户端访问共享资源时必须携带 token，资源端（如数据库）拒绝比已见过的更小的 token。即使旧客户端的锁过期后才来写入，资源端也会因为 token 过旧而拒绝。</p></details><hr><h3 id="q30-redis-的布隆过滤器-bloom-filter-原理是什么-误判率公式如何推导-热-key-问题和-singleflight-模式如何解决-⭐⭐⭐" tabindex="-1">Q30：Redis 的布隆过滤器（Bloom Filter）原理是什么？误判率公式如何推导？热 Key 问题和 singleflight 模式如何解决？⭐⭐⭐ <a class="header-anchor" href="#q30-redis-的布隆过滤器-bloom-filter-原理是什么-误判率公式如何推导-热-key-问题和-singleflight-模式如何解决-⭐⭐⭐" aria-label="Permalink to &quot;Q30：Redis 的布隆过滤器（Bloom Filter）原理是什么？误判率公式如何推导？热 Key 问题和 singleflight 模式如何解决？⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>布隆过滤器原理</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>添加元素 x：</span></span>
<span class="line"><span>  hash1(x) % m → 置位 bit[i1] = 1</span></span>
<span class="line"><span>  hash2(x) % m → 置位 bit[i2] = 1</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>  hashk(x) % m → 置位 bit[ik] = 1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>查询元素 y：</span></span>
<span class="line"><span>  检查 bit[hash1(y)%m], bit[hash2(y)%m], ..., bit[hashk(y)%m]</span></span>
<span class="line"><span>  全部为 1 → &quot;可能存在&quot;（可能误判）</span></span>
<span class="line"><span>  任一为 0 → &quot;一定不存在&quot;（绝无漏判）</span></span></code></pre></div><p><strong>误判率公式推导</strong>：</p><ul><li>m 位的位数组，k 个哈希函数，n 个已插入元素</li><li>某一位在一次哈希后仍为 0 的概率：(1 - 1/m)</li><li>插入 n 个元素后（共 kn 次哈希）仍为 0：(1 - 1/m)^(kn) ≈ e^(-kn/m)</li><li>误判率 = 所有 k 位都为 1的概率：<strong>p = (1 - e^(-kn/m))^k</strong></li><li>最优 k = (m/n) × ln2 ≈ 0.693 × (m/n)</li></ul><p><strong>实际工程中的使用（Redis Bloom Module）</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 创建：误判率 0.01，预期元素数 100 万</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">BF.RESERVE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user_filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0.01</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1000000</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 添加</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">BF.ADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user_filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:1001&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查询</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">BF.EXISTS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user_filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:9999&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # → 0（一定不存在）</span></span></code></pre></div><p>应用：缓存穿透防护——查询 Redis 前先过 Bloom Filter，不存在的 key 直接拦截。</p><p><strong>热 Key 问题</strong>：</p><p>大量请求集中访问少数几个 key（如秒杀商品），导致单个 Redis 节点压力过大。</p><p><strong>解决方案矩阵</strong>：</p><table tabindex="0"><thead><tr><th>方案</th><th>原理</th><th>适用场景</th></tr></thead><tbody><tr><td>本地缓存</td><td>应用进程内缓存热 key 值（如 Caffeine/Guava Cache）</td><td>读多写少</td></tr><tr><td>key 分散</td><td>将 <code>hot_key</code> 拆分为 <code>hot_key:1</code> ~ <code>hot_key:N</code>，随机读取</td><td>读多写少</td></tr><tr><td>singleflight</td><td>同一进程内多个 goroutine 请求同一 key 时，只放行一个</td><td>缓存击穿防护</td></tr><tr><td>读写分离</td><td>从节点承担读流量</td><td>读 QPS 极高</td></tr></tbody></table><p><strong>singleflight 模式详解</strong>：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">golang.org/x/sync/singleflight</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">var</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> g </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">singleflight</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Group</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">func</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> GetData</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">key</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{}, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">error</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // 相同 key 的并发请求只执行一次 fn，其余等待结果</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    v, err, shared </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> g.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Do</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">func</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{}, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">error</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        // 只有第一个请求会执行这里</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        val, err </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">        if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> err </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.Nil {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            val, err </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> db.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Query</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key, val, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">5</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">time.Minute)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">        return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> val, err</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    })</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // shared=true 表示结果来自另一个并发调用的共享</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> v, err</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>追问</strong>：布隆过滤器不支持删除怎么办？ → 使用 <strong>Cuckoo Filter</strong>（Redis 的 <code>CF.*</code> 命令），支持删除操作，且在元素数较少时空间效率更高。或用 <strong>Counting Bloom Filter</strong>（每个位用计数器替代，但空间开销 ×4）。</p></details><hr><h3 id="q31-【场景题】设计一个支持亿级用户的实时排行榜系统-要求-实时更新分数、查询-top-n、查询用户排名、查询附近排名用户。⭐⭐⭐" tabindex="-1">Q31：【场景题】设计一个支持亿级用户的实时排行榜系统，要求：实时更新分数、查询 Top-N、查询用户排名、查询附近排名用户。⭐⭐⭐ <a class="header-anchor" href="#q31-【场景题】设计一个支持亿级用户的实时排行榜系统-要求-实时更新分数、查询-top-n、查询用户排名、查询附近排名用户。⭐⭐⭐" aria-label="Permalink to &quot;Q31：【场景题】设计一个支持亿级用户的实时排行榜系统，要求：实时更新分数、查询 Top-N、查询用户排名、查询附近排名用户。⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>核心数据结构：Redis Sorted Set</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 更新分数（O(log n)）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 9500</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:1001&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查询 Top-10（O(log n + 10)）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZREVRANGE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 9</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> WITHSCORES</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查询用户排名（O(log n)，0-based）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZREVRANK</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:1001&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查询用户分数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZSCORE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:1001&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查询某用户附近的排名（前后各 5 名）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 先获取排名 rank，再 ZREVRANGE rank-5 rank+5</span></span></code></pre></div><p><strong>亿级用户的分片策略</strong>：</p><p>单个 Sorted Set 存 1 亿用户，内存约 1 亿 × (member + score) ≈ 数 GB，单实例可以承受但 QPS 有瓶颈。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>方案一：分段排行榜</span></span>
<span class="line"><span>  ┌──────────────────────────────────────┐</span></span>
<span class="line"><span>  │ 第一层：100 个分片（按 user_id hash） │</span></span>
<span class="line"><span>  │ 每个分片 100 万用户                   │</span></span>
<span class="line"><span>  │ 各分片独立 Sorted Set                 │</span></span>
<span class="line"><span>  └──────────────────────────────────────┘</span></span>
<span class="line"><span>  ┌──────────────────────────────────────┐</span></span>
<span class="line"><span>  │ 第二层：全局 Top-K 合并              │</span></span>
<span class="line"><span>  │ 每个分片定期推送 Top-1000 到汇总 Set  │</span></span>
<span class="line"><span>  │ 汇总 Set 维护全局 Top-N              │</span></span>
<span class="line"><span>  └──────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>方案二：按分数段分桶（适合查排名）</span></span>
<span class="line"><span>  桶1：分数 90000-100000 → sorted set</span></span>
<span class="line"><span>  桶2：分数 80000-90000  → sorted set</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>  用户排名 = 高分桶总人数 + 本桶内排名</span></span></code></pre></div><p><strong>并列排名处理</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 将 score 编码为：实际分数 × 10^10 + (MAX_TIMESTAMP - 达到该分数的时间戳)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 分数相同时，先达到的 timestamp 更早 → 编码后 score 更大 → 排名靠前</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 95000000001699000000</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user:1001&quot;</span></span></code></pre></div><p><strong>多维度排行榜</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 日/周/月排行榜</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard:daily:2024-01-15</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> score</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard:weekly:2024-W03</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> score</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZADD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard:monthly:2024-01</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> score</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> user</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 利用 ZUNIONSTORE 合并多日数据生成周排行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ZUNIONSTORE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> leaderboard:weekly:2024-W03</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 7</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  leaderboard:daily:2024-01-15</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  leaderboard:daily:2024-01-16</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  ...</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> AGGREGATE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> SUM</span></span></code></pre></div><p><strong>追问链</strong>：</p><ol><li><strong>如何处理分数频繁更新</strong>？→ 客户端本地缓存 + 批量合并（每 100ms 一次 ZADD），减少 Redis 写入次数</li><li><strong>排行榜需要持久化吗</strong>？→ 分数来源（如游戏服务器）是真实数据源，排行榜是衍生数据，可以从源数据重建。但为了快速恢复，建议 RDB 快照</li><li><strong>全球多区域怎么做</strong>？→ 每个区域独立排行榜 + 全球汇总服务定期合并 Top-K</li></ol></details><hr><h3 id="q32-【场景题】用-redis-实现三种分布式限流器-固定窗口、滑动窗口、令牌桶。分析各自的优缺点和-lua-脚本实现。⭐⭐⭐" tabindex="-1">Q32：【场景题】用 Redis 实现三种分布式限流器：固定窗口、滑动窗口、令牌桶。分析各自的优缺点和 Lua 脚本实现。⭐⭐⭐ <a class="header-anchor" href="#q32-【场景题】用-redis-实现三种分布式限流器-固定窗口、滑动窗口、令牌桶。分析各自的优缺点和-lua-脚本实现。⭐⭐⭐" aria-label="Permalink to &quot;Q32：【场景题】用 Redis 实现三种分布式限流器：固定窗口、滑动窗口、令牌桶。分析各自的优缺点和 Lua 脚本实现。⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>方案一：固定窗口计数器</strong></p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- KEYS[1] = 限流 key，ARGV[1] = 限额，ARGV[2] = 窗口大小（秒）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> key </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> limit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> window </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> current </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;GET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">or</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;0&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> current </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> limit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 限流</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> current </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;SET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;EX&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, window)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">else</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;INCR&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 放行</span></span></code></pre></div><ul><li>✅ 简单高效，O(1) 操作</li><li>❌ <strong>临界点突刺</strong>：窗口 [0:00-1:00] 结尾集中 100 请求 + 窗口 [1:00-2:00] 开头集中 100 请求 → 1 秒内实际承受 200 请求</li></ul><p><strong>方案二：滑动窗口（Sorted Set）</strong></p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- KEYS[1] = 限流 key</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- ARGV[1] = 限额, ARGV[2] = 窗口大小(毫秒), ARGV[3] = 当前时间戳(毫秒), ARGV[4] = 唯一请求ID</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> key </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> limit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> window </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> now </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">3</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> request_id </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">4</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 移除窗口外的旧请求</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ZREMRANGEBYSCORE&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, now </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> window)</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 统计当前窗口内的请求数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ZCARD&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key)</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> limit </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 限流</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 添加当前请求（score = 时间戳, member = 唯一ID）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ZADD&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, now, request_id)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;PEXPIRE&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, window)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 放行</span></span></code></pre></div><ul><li>✅ 无临界点突刺，精确限流</li><li>❌ 每个请求存一条记录，高 QPS 下内存开销大（百万 QPS → 窗口内百万条记录）</li></ul><p><strong>方案三：令牌桶（Token Bucket）</strong></p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- KEYS[1] = 桶 key</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- ARGV[1] = 桶容量, ARGV[2] = 每秒产生令牌数, ARGV[3] = 当前时间戳(秒), ARGV[4] = 请求令牌数</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> key </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> capacity </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> rate </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> now </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">3</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> requested </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">4</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 获取桶状态</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> bucket </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;HMGET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tokens&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;last_refill&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(bucket[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> last_refill </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> tonumber</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(bucket[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> nil</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    -- 初始化：满桶</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> capacity</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    last_refill </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> now</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 计算自上次填充以来产生的令牌</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> elapsed </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> math.max</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, now </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> last_refill)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> math.min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(capacity, tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> elapsed </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> rate)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">last_refill </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> now</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> requested </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tokens </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> requested</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;HMSET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tokens&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, tokens, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;last_refill&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, last_refill)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;EXPIRE&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, capacity </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> rate </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 过期时间 = 填满桶的两倍时间</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 放行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">else</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;HMSET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tokens&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, tokens, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;last_refill&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, last_refill)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;EXPIRE&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key, capacity </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> rate </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  -- 限流</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span></code></pre></div><ul><li>✅ 允许突发流量（桶内有积累的令牌），平滑限流</li><li>✅ 只存两个字段（tokens + last_refill），内存开销极小</li><li>❌ 实现相对复杂</li></ul><p><strong>三种方案对比</strong>：</p><table tabindex="0"><thead><tr><th>方案</th><th>精确度</th><th>内存</th><th>突发支持</th><th>实现复杂度</th><th>推荐场景</th></tr></thead><tbody><tr><td>固定窗口</td><td>低</td><td>O(1)</td><td>❌</td><td>简单</td><td>粗粒度限流（如日配额）</td></tr><tr><td>滑动窗口</td><td>高</td><td>O(N)</td><td>❌</td><td>中等</td><td>精确计数限流（如 API 限频）</td></tr><tr><td>令牌桶</td><td>中</td><td>O(1)</td><td>✅</td><td>较高</td><td>平滑限流 + 允许突发</td></tr></tbody></table><p><strong>追问链</strong>：</p><ol><li><strong>分布式多节点限流如何保证全局一致</strong>？→ 所有节点共用同一个 Redis key，Lua 脚本保证原子性</li><li><strong>Redis 不可用时如何降级</strong>？→ 本地进程内限流（如 Go 的 <code>golang.org/x/time/rate</code>）+ 允许少量超限（宁可放行也不完全拒绝）</li><li><strong>漏桶（Leaky Bucket）和令牌桶的区别</strong>？→ 漏桶以固定速率处理请求（不允许突发），令牌桶允许突发（桶内令牌可累积）。漏桶适合&quot;严格匀速&quot;场景（如音视频流控）。</li></ol></details><hr><h3 id="q33-【场景题】设计一个基于-redis-的延迟队列-支持-延迟投递、精确触发、失败重试、死信队列。⭐⭐⭐" tabindex="-1">Q33：【场景题】设计一个基于 Redis 的延迟队列，支持：延迟投递、精确触发、失败重试、死信队列。⭐⭐⭐ <a class="header-anchor" href="#q33-【场景题】设计一个基于-redis-的延迟队列-支持-延迟投递、精确触发、失败重试、死信队列。⭐⭐⭐" aria-label="Permalink to &quot;Q33：【场景题】设计一个基于 Redis 的延迟队列，支持：延迟投递、精确触发、失败重试、死信队列。⭐⭐⭐&quot;">&amp;ZeroWidthSpace;</a></h3><details><summary>参考答案</summary><p><strong>核心思路：Sorted Set + Stream</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Sorted Set（delay_queue）：score = 触发时间戳，member = 消息ID</span></span>
<span class="line"><span>Hash（msg:{id}）：存储消息体和元数据</span></span>
<span class="line"><span>Stream（processing）：正在处理的消息（可选，用于 ACK 机制）</span></span></code></pre></div><p><strong>整体架构</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>生产者                     Redis                        消费者</span></span>
<span class="line"><span>  │                          │                            │</span></span>
<span class="line"><span>  ├─ ZADD delay_queue        │                            │</span></span>
<span class="line"><span>  │  score=now+delay         │                            │</span></span>
<span class="line"><span>  │  member=msg_id           │                            │</span></span>
<span class="line"><span>  ├─ HSET msg:{id}           │                            │</span></span>
<span class="line"><span>  │  body/retry/created      │                            │</span></span>
<span class="line"><span>  │                          │                            │</span></span>
<span class="line"><span>  │                     [调度器轮询]                       │</span></span>
<span class="line"><span>  │                          │                            │</span></span>
<span class="line"><span>  │                          ├─ ZRANGEBYSCORE              │</span></span>
<span class="line"><span>  │                          │  delay_queue 0 now          │</span></span>
<span class="line"><span>  │                          │  LIMIT 0 100                │</span></span>
<span class="line"><span>  │                          │                            │</span></span>
<span class="line"><span>  │                          ├─ 到期消息 ──────────────► │ 处理</span></span>
<span class="line"><span>  │                          │  XADD processing           │</span></span>
<span class="line"><span>  │                          │  ZREM delay_queue           │</span></span>
<span class="line"><span>  │                          │                            │</span></span>
<span class="line"><span>  │                          │                ◄──────────── │ XACK</span></span>
<span class="line"><span>  │                          │                            │</span></span></code></pre></div><p><strong>调度器核心逻辑（Lua 脚本保证原子性）</strong>：</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 获取并转移到期消息</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">local</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> msgs </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ZRANGEBYSCORE&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;0&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, ARGV[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;LIMIT&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">100</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> #</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msgs </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> then</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">for</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> i, msg_id </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">in</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ipairs</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(msgs) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">do</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    -- 转移到处理队列</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;XADD&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;*&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;msg_id&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, msg_id)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    redis.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">call</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ZREM&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, KEYS[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], msg_id)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">end</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> #</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msgs</span></span></code></pre></div><p><strong>失败重试机制</strong>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">MAX_RETRY</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 3</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">def</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> process_message</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(msg_id):</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    msg </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redis.hgetall(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;msg:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msg_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    retry_count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(msg.get(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;retry&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">))</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    try</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        do_business_logic(msg[</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;body&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">])</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        redis.delete(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;msg:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msg_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 成功，清理</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    except</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> Exception</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> as</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> e:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">        if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> retry_count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> MAX_RETRY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # 指数退避重试：1s, 4s, 9s</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            delay </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (retry_count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">**</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 2</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            redis.hset(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;msg:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msg_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;retry&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, retry_count </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            redis.zadd(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;delay_queue&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, {msg_id: time.time() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">+</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> delay})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">        else</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # 超过重试次数 → 进入死信队列</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            redis.xadd(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dead_letter_queue&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, {</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;msg_id&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: msg_id, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;error&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(e)})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">            redis.expire(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;msg:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">msg_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">7</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> *</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 86400</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 保留 7 天供排查</span></span></code></pre></div><p><strong>关键设计细节</strong>：</p><table tabindex="0"><thead><tr><th>问题</th><th>解决方案</th></tr></thead><tbody><tr><td>消息重复消费</td><td>消费者处理前用 <code>SET msg:{id}:lock NX EX 60</code> 加锁</td></tr><tr><td>调度器单点故障</td><td>多实例调度器 + 分布式锁竞争 + 只有获取锁的实例执行 ZRANGEBYSCORE</td></tr><tr><td>轮询空转浪费 CPU</td><td>空轮询时 <code>BLPOP</code> 或 <code>sleep(100ms)</code> + 指数退避</td></tr><tr><td>消息堆积</td><td>监控 ZCARD(delay_queue)，超过阈值告警；消费者水平扩展</td></tr><tr><td>时间精度</td><td>调度器轮询间隔决定精度（100ms 轮询 ≈ 100ms 误差），可接受大部分场景</td></tr></tbody></table><p><strong>与专业消息队列对比</strong>：</p><table tabindex="0"><thead><tr><th>特性</th><th>Redis 延迟队列</th><th>RabbitMQ (delayed-exchange)</th><th>Kafka (时间轮)</th></tr></thead><tbody><tr><td>延迟精度</td><td>~100ms</td><td>~1s</td><td>~1s</td></tr><tr><td>消息持久化</td><td>依赖 AOF/RDB</td><td>原生支持</td><td>原生支持</td></tr><tr><td>吞吐量</td><td>万级/s</td><td>万级/s</td><td>十万级/s</td></tr><tr><td>运维复杂度</td><td>低（复用已有 Redis）</td><td>中</td><td>高</td></tr><tr><td>推荐规模</td><td>&lt; 100 万消息/天</td><td>&lt; 1000 万消息/天</td><td>海量</td></tr></tbody></table><p><strong>追问链</strong>：</p><ol><li><strong>为什么不直接用 Redis 的 key 过期通知（keyspace notification）做延迟队列？</strong> → <code>__keyevent@0__:expired</code> 通知不可靠：Redis 的过期是惰性+定期采样，不保证精确时间触发；且通知是 fire-and-forget，消费者离线时消息丢失</li><li><strong>为什么不用 Redis Stream 的 XAUTOCLAIM 替代手动重试？</strong> → 可以。<code>XAUTOCLAIM</code> 在 Redis 6.2+ 支持自动认领超时未 ACK 的消息，简化了重试逻辑。但延迟投递（定时触发）仍需 Sorted Set</li></ol></details></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("02-面试指南/03-数据库面试/02-Redis面试题.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _02Redis___ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _02Redis___ as default
};
