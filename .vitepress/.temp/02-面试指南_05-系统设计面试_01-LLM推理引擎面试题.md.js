import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"vLLM 常见面试题库","description":"","frontmatter":{},"headers":[],"relativePath":"02-面试指南/05-系统设计面试/01-LLM推理引擎面试题.md","filePath":"02-面试指南/05-系统设计面试/01-LLM推理引擎面试题.md","lastUpdated":1774066204000}');
const _sfc_main = { name: "02-面试指南/05-系统设计面试/01-LLM推理引擎面试题.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="vllm-常见面试题库" tabindex="-1">vLLM 常见面试题库 <a class="header-anchor" href="#vllm-常见面试题库" aria-label="Permalink to &quot;vLLM 常见面试题库&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>涵盖 vLLM 核心原理、系统设计和工程实践的 30 道面试题，从基础概念到深度原理，适合中高级工程师备考。</p></blockquote><h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to &quot;相关链接&quot;">&amp;ZeroWidthSpace;</a></h2><ul><li>对应技术资料：<a href="./../../01-技术资料/05-系统设计/01-LLM推理引擎优化.html">01-LLM推理引擎优化</a></li></ul><hr><h2 id="🔥-高频考点速记" tabindex="-1">🔥 高频考点速记 <a class="header-anchor" href="#🔥-高频考点速记" aria-label="Permalink to &quot;🔥 高频考点速记&quot;">&amp;ZeroWidthSpace;</a></h2><blockquote><p>面试中最常被问到的核心知识点，按出现频率排序。建议优先掌握前 5 项。</p></blockquote><table tabindex="0"><thead><tr><th>#</th><th>考点</th><th>核心要点（一句话）</th><th>出题概率</th></tr></thead><tbody><tr><td>1</td><td>KV Cache</td><td>缓存历史K/V避免重复计算，生成阶段内存主要消耗</td><td>★★★★★</td></tr><tr><td>2</td><td>PagedAttention</td><td>操作系统分页思想管理KV Cache，解决内存碎片</td><td>★★★★★</td></tr><tr><td>3</td><td>Continuous Batching</td><td>动态插入/移除请求，GPU利用率提升2-3x</td><td>★★★★☆</td></tr><tr><td>4</td><td>量化 (INT8/INT4)</td><td>精度换速度+内存，GPTQ(权重)/AWQ(激活感知)</td><td>★★★★★</td></tr><tr><td>5</td><td>Prefill vs Decode</td><td>Prefill=计算密集(并行)，Decode=内存密集(逐token)</td><td>★★★★☆</td></tr><tr><td>6</td><td>vLLM 架构</td><td>PagedAttention+Continuous Batching+Tensor Parallel</td><td>★★★★☆</td></tr><tr><td>7</td><td>推测解码 (Speculative)</td><td>小模型草稿+大模型验证，保证分布一致</td><td>★★★☆☆</td></tr><tr><td>8</td><td>FlashAttention</td><td>IO感知算法，分块计算减少HBM访问，O(N)→O(N/M)</td><td>★★★★☆</td></tr><tr><td>9</td><td>TensorRT-LLM</td><td>NVIDIA优化: kernel fusion+FP8+inflight batching</td><td>★★★☆☆</td></tr><tr><td>10</td><td>llama.cpp 量化</td><td>GGUF格式，Q4_K_M性价比最佳，CPU推理方案</td><td>★★★★☆</td></tr></tbody></table><hr><h2 id="基础概念-q1-q8" tabindex="-1">基础概念（Q1-Q8） <a class="header-anchor" href="#基础概念-q1-q8" aria-label="Permalink to &quot;基础概念（Q1-Q8）&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q1-什么是-vllm-它解决了什么核心问题" tabindex="-1">Q1：什么是 vLLM？它解决了什么核心问题？ <a class="header-anchor" href="#q1-什么是-vllm-它解决了什么核心问题" aria-label="Permalink to &quot;Q1：什么是 vLLM？它解决了什么核心问题？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>vLLM（由 UC Berkeley 开发，SOSP 2023）是一个高吞吐量、低延迟的 LLM 推理引擎。</p><p><strong>核心问题</strong>：传统 LLM 推理框架在 KV Cache 管理上存在严重的内存浪费——预分配最大序列长度导致利用率仅 20-40%，同时内存碎片使得实际可用并发数极低。</p><p><strong>解决方案</strong>：</p><ol><li><strong>PagedAttention</strong>：借鉴 OS 虚拟内存分页思想，将 KV Cache 按固定大小（16 tokens/块）动态分配，消除碎片，利用率提升至 &gt;90%</li><li><strong>Continuous Batching</strong>：动态调整批次，任何时刻完成的请求立即被新请求替换，GPU 始终满负荷</li></ol><p><strong>效果</strong>：相比 HuggingFace Transformers，吞吐量提升约 24x；相比 TGI 提升约 6x。</p><hr><h3 id="q2-pagedattention-的核心原理是什么" tabindex="-1">Q2：PagedAttention 的核心原理是什么？ <a class="header-anchor" href="#q2-pagedattention-的核心原理是什么" aria-label="Permalink to &quot;Q2：PagedAttention 的核心原理是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>PagedAttention 将 KV Cache 组织成固定大小的<strong>物理块（Physical Block）</strong>，每块存储固定数量（默认 16 个）token 的 Key 和 Value 向量。</p><p><strong>关键概念</strong>：</p><ul><li><strong>逻辑块（Logical Block）</strong>：从请求视角看到的连续 KV Cache</li><li><strong>物理块（Physical Block）</strong>：实际在 GPU 内存中分配的块，可以不连续</li><li><strong>块表（Block Table）</strong>：维护每个序列的逻辑块到物理块的映射</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统方式：预分配 2048 tokens 的连续内存</span></span>
<span class="line"><span>[████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]</span></span>
<span class="line"><span> 已用10%              浪费90%</span></span>
<span class="line"><span></span></span>
<span class="line"><span>PagedAttention：按需分配物理块</span></span>
<span class="line"><span>Block 0 → Physical 3</span></span>
<span class="line"><span>Block 1 → Physical 7   （不连续，但通过块表索引）</span></span>
<span class="line"><span>Block 2 → Physical 1</span></span></code></pre></div><p><strong>关键改进</strong>：</p><ul><li>无外部碎片（块大小固定）</li><li>内部碎片仅在最后一个块（&lt; block_size tokens）</li><li>支持跨请求共享前缀（引用计数机制）</li></ul><hr><h3 id="q3-pagedattention-如何处理-beam-search-的内存共享" tabindex="-1">Q3：PagedAttention 如何处理 Beam Search 的内存共享？ <a class="header-anchor" href="#q3-pagedattention-如何处理-beam-search-的内存共享" aria-label="Permalink to &quot;Q3：PagedAttention 如何处理 Beam Search 的内存共享？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>Beam Search 需要维护多个候选序列（beam），它们共享相同的前缀。PagedAttention 使用 <strong>Copy-on-Write（CoW）</strong> 机制：</p><p><strong>初始状态</strong>：所有 beam 共享相同物理块，引用计数增加</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Prompt: &quot;The weather is&quot; (共享块 P0, P1)</span></span>
<span class="line"><span>Beam 1: [P0, P1, P_A(独占)]  ref(P0)=3, ref(P1)=3</span></span>
<span class="line"><span>Beam 2: [P0, P1, P_B(独占)]</span></span>
<span class="line"><span>Beam 3: [P0, P1, P_C(独占)]</span></span></code></pre></div><p><strong>写入时 CoW</strong>：若某个 beam 需要修改共享块：</p><ol><li>分配新物理块</li><li>复制原块内容到新块</li><li>原块引用计数减 1</li><li>该 beam 的块表指向新块</li></ol><p><strong>内存节省</strong>：对于 beam_width=4，共享前缀占 90% 时，节省约 67% 内存（相比每个 beam 独立分配）。</p><hr><h3 id="q4-continuous-batching-与-static-batching-有什么区别" tabindex="-1">Q4：Continuous Batching 与 Static Batching 有什么区别？ <a class="header-anchor" href="#q4-continuous-batching-与-static-batching-有什么区别" aria-label="Permalink to &quot;Q4：Continuous Batching 与 Static Batching 有什么区别？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>Static Batching</th><th>Continuous Batching</th></tr></thead><tbody><tr><td>调度时机</td><td>请求到达时组批</td><td>每个 iteration 动态调整</td></tr><tr><td>批次变化</td><td>固定，直到最长序列完成</td><td>每步可加入/退出</td></tr><tr><td>GPU 利用率</td><td>随时间递减（短序列完成后空闲）</td><td>始终高利用率</td></tr><tr><td>延迟</td><td>长请求拖累整批</td><td>短请求及时完成</td></tr></tbody></table><p><strong>Continuous Batching 核心</strong>：</p><p>每个 iteration（前向传播步骤）之后：</p><ul><li>已完成序列（EOS 或 max_tokens）从批次中移除</li><li>等待队列中的新序列立即补充进来</li><li>从不存在&quot;GPU 空等&quot;的情况</li></ul><p><strong>典型场景收益</strong>：ShareGPT 数据集中输出长度差异极大（几十到几千 token），静态批处理吞吐量比连续批处理低 3-6x。</p><hr><h3 id="q5-vllm-的内存效率为什么比传统方法高" tabindex="-1">Q5：vLLM 的内存效率为什么比传统方法高？ <a class="header-anchor" href="#q5-vllm-的内存效率为什么比传统方法高" aria-label="Permalink to &quot;Q5：vLLM 的内存效率为什么比传统方法高？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>传统方法的三种内存浪费</strong>：</p><ol><li><strong>过度预留</strong>：在请求开始时为整个 max_seq_len 分配内存，实际使用率 10-40%</li><li><strong>外部碎片</strong>：不同长度请求产生的内存空洞无法被利用</li><li><strong>内部碎片</strong>：为对齐预留的空间</li></ol><p><strong>vLLM 的解决</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统 LLM 服务：</span></span>
<span class="line"><span>GPU 内存 = 模型权重(固定) + KV Cache(过度预留)</span></span>
<span class="line"><span>实际 KV Cache 利用率 ≈ 20-40%</span></span>
<span class="line"><span></span></span>
<span class="line"><span>vLLM：</span></span>
<span class="line"><span>GPU 内存 = 模型权重(固定) + 动态块分配的 KV Cache</span></span>
<span class="line"><span>实际 KV Cache 利用率 ≈ 90-96%</span></span>
<span class="line"><span>（仅最后一个块有少量内部碎片）</span></span></code></pre></div><p><strong>量化对比</strong>（LLaMA-13B, A100-40GB）：</p><ul><li>传统方式：最大并发 ~10 请求</li><li>vLLM：最大并发 ~40-60 请求（4-6x 提升）</li></ul><hr><h3 id="q6-如何在-vllm-中配置-kv-cache-大小" tabindex="-1">Q6：如何在 vLLM 中配置 KV Cache 大小？ <a class="header-anchor" href="#q6-如何在-vllm-中配置-kv-cache-大小" aria-label="Permalink to &quot;Q6：如何在 vLLM 中配置 KV Cache 大小？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>vLLM 不直接设置 KV Cache 大小，而是通过以下参数间接控制：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式1：控制 GPU 显存占用比例</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--gpu-memory-utilization</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0.90</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># KV Cache = GPU总显存 × 0.90 - 模型权重大小</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式2：限制最大序列长度（减少每个 KV 块的内存需求）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--max-model-len</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 4096</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式3：控制 block_size（更多小块 vs 更少大块）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--block-size</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 16</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # 默认，适合大多数场景</span></span></code></pre></div><p><strong>计算公式</strong>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 可用 KV Cache 块数（近似）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">available_kv_memory </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> total_gpu_memory × gpu_memory_utilization </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> model_weights</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">bytes_per_block </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> block_size × num_layers × </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> × num_heads × head_dim × dtype_size</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">num_blocks </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> available_kv_memory </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> bytes_per_block</span></span></code></pre></div><hr><h3 id="q7-vllm-如何处理请求超过-gpu-内存的情况" tabindex="-1">Q7：vLLM 如何处理请求超过 GPU 内存的情况？ <a class="header-anchor" href="#q7-vllm-如何处理请求超过-gpu-内存的情况" aria-label="Permalink to &quot;Q7：vLLM 如何处理请求超过 GPU 内存的情况？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>vLLM 有两种 KV Cache <strong>抢占（Preemption）策略</strong>：</p><p><strong>1. Swapping（换出）</strong>：</p><ul><li>将最老的运行中请求的 KV Cache 从 GPU 复制到 CPU</li><li>释放 GPU 块供新请求使用</li><li>当内存充裕时换回 GPU</li><li>开销：PCIe 传输带宽（约 16-32 GB/s）</li></ul><p><strong>2. Recomputation（重计算）</strong>：</p><ul><li>丢弃 KV Cache，将请求移回等待队列</li><li>等内存充裕时重新进行 prefill</li><li>无 CPU 内存开销，但需要重新计算</li></ul><p><strong>选择策略</strong>：</p><ul><li>短序列 → Recomputation（重算代价小）</li><li>长序列 → Swapping（重算代价太高）</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 配置换出空间</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--swap-space</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 8</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # 8GB CPU 内存用于 KV Cache 换出</span></span></code></pre></div><hr><h3 id="q8-什么是-chunked-prefill-它解决什么问题" tabindex="-1">Q8：什么是 Chunked Prefill？它解决什么问题？ <a class="header-anchor" href="#q8-什么是-chunked-prefill-它解决什么问题" aria-label="Permalink to &quot;Q8：什么是 Chunked Prefill？它解决什么问题？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>问题</strong>：LLM 推理分为两个阶段：</p><ul><li><strong>Prefill</strong>：处理输入 prompt，计算所有 token 的 KV Cache（耗时随 prompt 长度增长）</li><li><strong>Decode</strong>：逐个生成输出 token</li></ul><p>长 prompt 的 prefill 会阻塞其他请求的 decode，导致 TPOT（每 token 延迟）急剧上升。</p><p><strong>Chunked Prefill 解决方案</strong>： 将长 prompt 的 prefill 分成多个 chunk，与 decode 步骤交织执行：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统方式：</span></span>
<span class="line"><span>Step 1: [Prefill: 2048 tokens for Req_A]    ← 阻塞其他请求</span></span>
<span class="line"><span>Step 2: [Decode: Req_B, C, D, E]</span></span>
<span class="line"><span>Step 3: [Decode: Req_B, C, D, E]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Chunked Prefill：</span></span>
<span class="line"><span>Step 1: [Prefill: 512 tokens for Req_A] + [Decode: Req_B, C, D]</span></span>
<span class="line"><span>Step 2: [Prefill: 512 tokens for Req_A] + [Decode: Req_B, C, D]</span></span>
<span class="line"><span>Step 3: [Prefill: 512 tokens for Req_A] + [Decode: Req_B, C, D]</span></span>
<span class="line"><span>Step 4: [Prefill: 512 tokens for Req_A] + [Decode: Req_B, C, D]</span></span></code></pre></div><p><strong>配置</strong>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">LLM(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">enable_chunked_prefill</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">True</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">max_num_batched_tokens</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">512</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre></div><hr><h2 id="深度原理-q9-q16" tabindex="-1">深度原理（Q9-Q16） <a class="header-anchor" href="#深度原理-q9-q16" aria-label="Permalink to &quot;深度原理（Q9-Q16）&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q9-pagedattention-在-cuda-层面是如何实现的" tabindex="-1">Q9：PagedAttention 在 CUDA 层面是如何实现的？ <a class="header-anchor" href="#q9-pagedattention-在-cuda-层面是如何实现的" aria-label="Permalink to &quot;Q9：PagedAttention 在 CUDA 层面是如何实现的？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>传统 Attention 需要 KV 矩阵在内存中连续存储，PagedAttention 通过自定义 CUDA kernel 实现非连续内存的 Attention 计算：</p><p><strong>核心实现</strong>（类似 FlashAttention，但支持分页）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统 Attention:</span></span>
<span class="line"><span>K: [k0, k1, k2, ..., kN]  ← 连续内存</span></span>
<span class="line"><span></span></span>
<span class="line"><span>PagedAttention:</span></span>
<span class="line"><span>K: Block0→[k0..k15], Block7→[k16..k31], Block3→[k32..k47]</span></span>
<span class="line"><span>   ↑ 不连续！</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CUDA Kernel 中：</span></span>
<span class="line"><span>1. 从 block_table 获取 block 编号</span></span>
<span class="line"><span>2. 计算 block 在 GPU 内存中的实际地址</span></span>
<span class="line"><span>3. 逐块加载 K、V 并计算 attention score</span></span>
<span class="line"><span>4. 使用在线 softmax 累积结果（避免存储完整注意力矩阵）</span></span></code></pre></div><p><strong>关键优化</strong>：</p><ul><li>使用共享内存（Shared Memory）缓存当前处理的块</li><li>向量化内存访问（128-bit 对齐）</li><li>Warp 级别并行处理多个注意力头</li></ul><hr><h3 id="q10-张量并行和流水线并行的原理和适用场景" tabindex="-1">Q10：张量并行和流水线并行的原理和适用场景？ <a class="header-anchor" href="#q10-张量并行和流水线并行的原理和适用场景" aria-label="Permalink to &quot;Q10：张量并行和流水线并行的原理和适用场景？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>张量并行（Tensor Parallelism）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>原理：将单个层（Attention、FFN）的权重矩阵按列/行分割</span></span>
<span class="line"><span>      每个 GPU 计算部分结果，通过 AllReduce 合并</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Attention（QKV 投影按列分割）：</span></span>
<span class="line"><span>  GPU 0: Q[0:H/2], K[0:H/2], V[0:H/2]  → Attention Head 0-15</span></span>
<span class="line"><span>  GPU 1: Q[H/2:H], K[H/2:H], V[H/2:H]  → Attention Head 16-31</span></span>
<span class="line"><span>  同步: AllReduce(output)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>FFN（两层矩阵，列-行分割）：</span></span>
<span class="line"><span>  GPU 0: W1[:,0:d/2], W2[0:d/2,:]</span></span>
<span class="line"><span>  GPU 1: W1[:,d/2:d], W2[d/2:d,:]</span></span>
<span class="line"><span>  同步: AllReduce(output)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>适合：内存不足以放下整个模型（常用）</span></span></code></pre></div><p><strong>流水线并行（Pipeline Parallelism）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>原理：按层分割，每个 GPU 处理部分层</span></span>
<span class="line"><span>  GPU 0: Embedding + Layer 0-9</span></span>
<span class="line"><span>  GPU 1: Layer 10-19</span></span>
<span class="line"><span>  GPU 2: Layer 20-31 + LM Head</span></span>
<span class="line"><span></span></span>
<span class="line"><span>数据流: Micro-batch 流过各 GPU</span></span>
<span class="line"><span>适合：超大模型（&gt;100B参数），但有流水线气泡（bubble）开销</span></span></code></pre></div><p><strong>组合使用</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>4 GPU, TP=2, PP=2:</span></span>
<span class="line"><span>  GPU 0+1: Layer 0-15（张量并行）</span></span>
<span class="line"><span>  GPU 2+3: Layer 16-31（张量并行）</span></span></code></pre></div><hr><h3 id="q11-vllm-的-scheduler-如何做抢占决策" tabindex="-1">Q11：vLLM 的 Scheduler 如何做抢占决策？ <a class="header-anchor" href="#q11-vllm-的-scheduler-如何做抢占决策" aria-label="Permalink to &quot;Q11：vLLM 的 Scheduler 如何做抢占决策？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>vLLM 调度器按照 FCFS（先来先服务）调度，但内存不足时触发抢占：</p><p><strong>抢占触发条件</strong>： 当处理等待队列中的新请求（prefill）时，如果 KV Cache 块不足</p><p><strong>抢占策略选择</strong>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># vLLM 源码逻辑（简化）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">def</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> _preempt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(self, seq_group, blocks_to_free):</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    if</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> self</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.preemption_mode </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">==</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> PreemptionMode.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">RECOMPUTE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # 方式1：重计算</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">        self</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">._preempt_by_recompute(seq_group)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # 丢弃 KV Cache，移回 waiting 队列</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    else</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # 方式2：换出到 CPU</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">        self</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">._preempt_by_swap(seq_group)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # 将 KV Cache 块换出到 CPU，移入 swapped 队列</span></span></code></pre></div><p><strong>抢占顺序</strong>：</p><ul><li>优先抢占<strong>最晚到达</strong>的请求（LCFS 抢占）</li><li>确保最早的请求优先完成（防止饥饿）</li></ul><hr><h3 id="q12-speculative-decoding-在-vllm-中的实现细节" tabindex="-1">Q12：Speculative Decoding 在 vLLM 中的实现细节？ <a class="header-anchor" href="#q12-speculative-decoding-在-vllm-中的实现细节" aria-label="Permalink to &quot;Q12：Speculative Decoding 在 vLLM 中的实现细节？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>核心思想</strong>：小模型（draft）快速生成多个候选，大模型（target）并行验证，接受/拒绝候选。</p><p><strong>vLLM 中的实现</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Draft 阶段（小模型生成 γ=5 个候选 token）：</span></span>
<span class="line"><span>   输入: x_1...x_n</span></span>
<span class="line"><span>   输出: d_1, d_2, d_3, d_4, d_5</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2. Verification 阶段（大模型并行处理 n+5 个位置）：</span></span>
<span class="line"><span>   一次 forward pass 同时得到所有位置的 logits</span></span>
<span class="line"><span></span></span>
<span class="line"><span>3. Token 接受/拒绝：</span></span>
<span class="line"><span>   for i in range(γ):</span></span>
<span class="line"><span>     p = target_prob[d_i]</span></span>
<span class="line"><span>     q = draft_prob[d_i]</span></span>
<span class="line"><span>     if random() &lt; min(1, p/q):  # 接受</span></span>
<span class="line"><span>         accept(d_i)</span></span>
<span class="line"><span>     else:  # 拒绝，从修正分布中采样</span></span>
<span class="line"><span>         sample from max(0, p-q) / Z</span></span>
<span class="line"><span>         break</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4. 平均接受长度 &gt; 1，实现速度提升</span></span></code></pre></div><p><strong>vLLM 支持的 Draft 策略</strong>：</p><ul><li>小模型 Draft（需要加载额外小模型）</li><li>[ngram] Draft（无需额外模型，从 prompt 中查找 n-gram 匹配）</li><li>Medusa（多头预测）</li></ul><hr><h3 id="q13-vllm-的内存管理与操作系统虚拟内存有哪些相似和不同" tabindex="-1">Q13：vLLM 的内存管理与操作系统虚拟内存有哪些相似和不同？ <a class="header-anchor" href="#q13-vllm-的内存管理与操作系统虚拟内存有哪些相似和不同" aria-label="Permalink to &quot;Q13：vLLM 的内存管理与操作系统虚拟内存有哪些相似和不同？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>相似之处</strong>：</p><table tabindex="0"><thead><tr><th>OS 虚拟内存</th><th>vLLM PagedAttention</th></tr></thead><tbody><tr><td>虚拟页（Virtual Page）</td><td>逻辑 KV 块</td></tr><tr><td>物理页框（Frame）</td><td>物理 KV 块</td></tr><tr><td>页表（Page Table）</td><td>块表（Block Table）</td></tr><tr><td>页面置换（Swapping）</td><td>KV Cache 换出到 CPU</td></tr><tr><td>写时复制（CoW）</td><td>Beam Search 共享块 CoW</td></tr><tr><td>共享内存</td><td>前缀共享（相同 System Prompt）</td></tr></tbody></table><p><strong>不同之处</strong>：</p><ol><li><strong>访问模式已知</strong>：KV Cache 的访问模式是顺序追加的，不需要 LRU 等复杂置换策略</li><li><strong>块大小固定且较大</strong>：OS 页通常 4KB，vLLM 块约 8MB（16 tokens × LLaMA-7B 参数）</li><li><strong>无随机访问</strong>：一旦 token 被处理，其 KV Cache 只读，不会随机修改</li><li><strong>显式管理</strong>：OS 页面管理对程序透明，vLLM 需要显式通知 CUDA kernel 块映射</li></ol><hr><h3 id="q14-如何评估-vllm-的服务性能-关键指标有哪些" tabindex="-1">Q14：如何评估 vLLM 的服务性能？关键指标有哪些？ <a class="header-anchor" href="#q14-如何评估-vllm-的服务性能-关键指标有哪些" aria-label="Permalink to &quot;Q14：如何评估 vLLM 的服务性能？关键指标有哪些？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>关键指标</strong>：</p><table tabindex="0"><thead><tr><th>指标</th><th>定义</th><th>典型值</th><th>适用场景</th></tr></thead><tbody><tr><td>TTFT (Time to First Token)</td><td>从请求到第一个 token 的时间</td><td>100-500ms</td><td>交互式应用</td></tr><tr><td>TPOT (Time per Output Token)</td><td>每个生成 token 的平均时间</td><td>10-50ms</td><td>流式输出流畅度</td></tr><tr><td>Throughput</td><td>每秒总生成 token 数</td><td>5K-50K tok/s</td><td>批量处理</td></tr><tr><td>P99 Latency</td><td>99% 请求的端到端延迟</td><td>&lt; 5s</td><td>SLA 保障</td></tr><tr><td>GPU KV Cache Utilization</td><td>KV Cache 使用率</td><td>&gt; 80%</td><td>内存效率</td></tr></tbody></table><p><strong>权衡关系</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>吞吐量 ↑ ↔ 延迟 ↑（批越大，单个请求等待越长）</span></span>
<span class="line"><span>max_num_seqs ↑ → 吞吐量 ↑, TTFT ↑</span></span>
<span class="line"><span>max_num_seqs ↓ → 延迟 ↓, 吞吐量 ↓</span></span></code></pre></div><p><strong>优化建议</strong>：</p><ul><li>在线服务（聊天应用）：优先优化 TTFT 和 TPOT，限制 max_num_seqs</li><li>批量处理（数据生成）：最大化吞吐量，接受更高延迟</li></ul><hr><h3 id="q15-vllm-如何支持前缀共享-prefix-caching" tabindex="-1">Q15：vLLM 如何支持前缀共享（Prefix Caching）？ <a class="header-anchor" href="#q15-vllm-如何支持前缀共享-prefix-caching" aria-label="Permalink to &quot;Q15：vLLM 如何支持前缀共享（Prefix Caching）？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>场景</strong>：多个请求共享相同的 System Prompt（如 RAG 场景中的上下文）</p><p><strong>实现原理</strong>（Automatic Prefix Caching, APC）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>请求1: [System Prompt(256 tokens)] + [User Query A]</span></span>
<span class="line"><span>请求2: [System Prompt(256 tokens)] + [User Query B]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>没有 APC：</span></span>
<span class="line"><span>  请求1 Prefill: 计算 256+len(A) 个 token</span></span>
<span class="line"><span>  请求2 Prefill: 计算 256+len(B) 个 token（重复计算！）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>有 APC（vLLM v0.3+）：</span></span>
<span class="line"><span>  请求1 Prefill: 计算所有 token，System Prompt 的 KV 块保留</span></span>
<span class="line"><span>  请求2 Prefill: 直接复用 System Prompt 的物理块！</span></span>
<span class="line"><span>               只需计算 len(B) 个 token</span></span></code></pre></div><p><strong>实现细节</strong>：</p><ul><li>以 block_size 为单位，对每个块的 token ID 序列计算哈希</li><li>相同哈希的块视为可复用</li><li>块被所有共享者引用计数，最后使用者释放时才回收</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 启用 APC</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">python</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -m</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> vllm.entrypoints.openai.api_server</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    --model</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> llama-2-7b</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    --enable-prefix-caching</span></span></code></pre></div><hr><h3 id="q16-vllm-中-flashattention-是如何集成的" tabindex="-1">Q16：vLLM 中 FlashAttention 是如何集成的？ <a class="header-anchor" href="#q16-vllm-中-flashattention-是如何集成的" aria-label="Permalink to &quot;Q16：vLLM 中 FlashAttention 是如何集成的？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>vLLM 将 FlashAttention 与 PagedAttention 结合，形成 <strong>PagedFlashAttention</strong>：</p><p><strong>FlashAttention 贡献</strong>：</p><ul><li>IO-aware：减少 HBM 读写次数（分块计算，不存中间注意力矩阵）</li><li>不需要 O(N²) 的注意力矩阵，只需 O(N) 的额外空间</li></ul><p><strong>PagedAttention 贡献</strong>：</p><ul><li>非连续 KV Cache 的高效访问（块表索引）</li><li>支持动态长度序列</li></ul><p><strong>结合后的 CUDA Kernel</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>对于每个 Query token q：</span></span>
<span class="line"><span>  初始化 running_sum = 0, running_max = -inf</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  for block_idx in block_table[seq_id]:</span></span>
<span class="line"><span>    # 加载当前 KV 块（16 个 token）</span></span>
<span class="line"><span>    K_block = load_kv_block(block_idx)</span></span>
<span class="line"><span>    V_block = load_kv_block(block_idx)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # 计算该块的注意力分数</span></span>
<span class="line"><span>    scores = q @ K_block.T / sqrt(head_dim)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # 在线 softmax 更新</span></span>
<span class="line"><span>    running_max, running_sum = update_online_softmax(</span></span>
<span class="line"><span>        scores, running_max, running_sum)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # 累积 Value</span></span>
<span class="line"><span>    output += softmax(scores) @ V_block</span></span>
<span class="line"><span></span></span>
<span class="line"><span>return output / running_sum</span></span></code></pre></div><hr><h2 id="系统设计-q17-q22" tabindex="-1">系统设计（Q17-Q22） <a class="header-anchor" href="#系统设计-q17-q22" aria-label="Permalink to &quot;系统设计（Q17-Q22）&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q17-设计一个高吞吐量-llm-服务系统" tabindex="-1">Q17：设计一个高吞吐量 LLM 服务系统 <a class="header-anchor" href="#q17-设计一个高吞吐量-llm-服务系统" aria-label="Permalink to &quot;Q17：设计一个高吞吐量 LLM 服务系统&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>系统架构：</span></span>
<span class="line"><span></span></span>
<span class="line"><span>用户层</span></span>
<span class="line"><span>  │ HTTPS 请求</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>负载均衡（Nginx / AWS ALB）</span></span>
<span class="line"><span>  │ 路由策略：最少连接数</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>API Gateway（FastAPI / Kong）</span></span>
<span class="line"><span>  ├── 认证授权（JWT）</span></span>
<span class="line"><span>  ├── 请求限流（Token Bucket）</span></span>
<span class="line"><span>  ├── 请求验证（max_tokens, prompt_length）</span></span>
<span class="line"><span>  └── 请求日志（Async Logger）</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>请求队列（Kafka / Redis Stream）</span></span>
<span class="line"><span>  │ 优先级队列（VIP 用户高优先级）</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>vLLM 服务集群</span></span>
<span class="line"><span>  ├── 实例1: LLaMA-7B (GPU 0-1, TP=2) → 通用请求</span></span>
<span class="line"><span>  ├── 实例2: LLaMA-7B (GPU 2-3, TP=2) → 通用请求</span></span>
<span class="line"><span>  └── 实例3: LLaMA-70B (GPU 4-7, TP=4) → 高质量请求</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>结果返回（SSE 流式 / WebSocket）</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>监控层（Prometheus + Grafana + AlertManager）</span></span>
<span class="line"><span>  ├── GPU 利用率</span></span>
<span class="line"><span>  ├── 请求队列深度</span></span>
<span class="line"><span>  └── P99 延迟</span></span></code></pre></div><p><strong>关键设计决策</strong>：</p><ol><li><strong>模型路由</strong>：小模型处理简单请求，大模型处理复杂请求（成本优化）</li><li><strong>水平扩展</strong>：按 GPU 节点扩展，不跨节点进行 TP（网络延迟大）</li><li><strong>故障隔离</strong>：单个 vLLM 实例崩溃不影响其他实例</li><li><strong>背压机制</strong>：队列满时返回 503 而不是无限积压</li></ol><hr><h3 id="q18-vllm-与-tgi-text-generation-inference-的对比" tabindex="-1">Q18：vLLM 与 TGI（Text Generation Inference）的对比？ <a class="header-anchor" href="#q18-vllm-与-tgi-text-generation-inference-的对比" aria-label="Permalink to &quot;Q18：vLLM 与 TGI（Text Generation Inference）的对比？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>vLLM</th><th>TGI (HuggingFace)</th></tr></thead><tbody><tr><td>内存管理</td><td>PagedAttention（几乎无碎片）</td><td>连续预分配（碎片较多）</td></tr><tr><td>批处理</td><td>Continuous Batching</td><td>Continuous Batching（v2+）</td></tr><tr><td>吞吐量</td><td>更高（~6x vs TGI）</td><td>中等</td></tr><tr><td>量化支持</td><td>AWQ/GPTQ/INT8/FP8</td><td>GPTQ/BitsAndBytes</td></tr><tr><td>模型支持</td><td>广泛</td><td>广泛（与 HF 生态深度集成）</td></tr><tr><td>部署难度</td><td>较简单</td><td>中等</td></tr><tr><td>生产成熟度</td><td>高</td><td>高</td></tr><tr><td>特殊功能</td><td>Speculative Decoding, APC</td><td>LoRA 支持更完善</td></tr></tbody></table><p><strong>选择建议</strong>：</p><ul><li>追求最高吞吐量 → vLLM</li><li>需要 LoRA 动态加载 → TGI 或 vLLM LoRA（v0.4+）</li><li>HuggingFace 生态深度集成 → TGI</li></ul><hr><h3 id="q19-如何为-vllm-服务设置-sla-保障" tabindex="-1">Q19：如何为 vLLM 服务设置 SLA 保障？ <a class="header-anchor" href="#q19-如何为-vllm-服务设置-sla-保障" aria-label="Permalink to &quot;Q19：如何为 vLLM 服务设置 SLA 保障？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p><strong>SLA 定义示例</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>P50 TTFT &lt; 200ms</span></span>
<span class="line"><span>P99 TTFT &lt; 1s</span></span>
<span class="line"><span>P99 TPOT &lt; 50ms</span></span>
<span class="line"><span>可用性 &gt; 99.9%</span></span></code></pre></div><p><strong>实现手段</strong>：</p><ol><li><strong>请求限流</strong>：超过 QPS 限制时返回 429，保护服务不被打垮</li><li><strong>超时控制</strong>：设置请求最大等待时间，超时返回 504</li><li><strong>优先级队列</strong>：VIP 用户请求优先处理</li><li><strong>弹性扩缩容</strong>：基于队列深度和延迟指标自动扩展副本</li><li><strong>熔断机制</strong>：单实例错误率超阈值时，流量不再路由到该实例</li></ol><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 客户端超时配置</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">client </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> openai.OpenAI(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    timeout</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">httpx.Timeout(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">        connect</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">5.0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,     </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 连接超时</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">        read</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">60.0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 读取超时</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">        write</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">10.0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    )</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre></div><hr><h3 id="q20-如何优化-vllm-在长文档摘要场景下的性能" tabindex="-1">Q20：如何优化 vLLM 在长文档摘要场景下的性能？ <a class="header-anchor" href="#q20-如何优化-vllm-在长文档摘要场景下的性能" aria-label="Permalink to &quot;Q20：如何优化 vLLM 在长文档摘要场景下的性能？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><p>长文档摘要特点：超长 prompt（8K-128K tokens），短输出（500-2000 tokens）</p><p><strong>优化策略</strong>：</p><ol><li><strong>Chunked Prefill</strong>：避免长 prefill 阻塞其他请求</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--enable-chunked-prefill</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --max-num-batched-tokens</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 2048</span></span></code></pre></div><ol start="2"><li><strong>前缀缓存</strong>：同一文档的多次处理复用 KV Cache</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--enable-prefix-caching</span></span></code></pre></div><ol start="3"><li><strong>增大 max_model_len</strong>：</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--max-model-len</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 32768</span></span></code></pre></div><ol start="4"><li><strong>选择支持长上下文的模型</strong>：</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>LLaMA 3.1（128K）, Qwen2（32K+）, Mistral Long（32K）</span></span></code></pre></div><ol start="5"><li><strong>减少并发</strong>：长 prompt 单请求内存占用大，降低 max_num_seqs</li></ol><hr><h2 id="对比与选型-q21-q26" tabindex="-1">对比与选型（Q21-Q26） <a class="header-anchor" href="#对比与选型-q21-q26" aria-label="Permalink to &quot;对比与选型（Q21-Q26）&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q21-vllm-与-deepspeed-mii-的对比" tabindex="-1">Q21：vLLM 与 DeepSpeed-MII 的对比？ <a class="header-anchor" href="#q21-vllm-与-deepspeed-mii-的对比" aria-label="Permalink to &quot;Q21：vLLM 与 DeepSpeed-MII 的对比？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>vLLM</th><th>DeepSpeed-MII</th></tr></thead><tbody><tr><td>内存管理</td><td>PagedAttention</td><td>传统连续分配</td></tr><tr><td>吞吐量</td><td>领先</td><td>较低</td></tr><tr><td>ZeRO 优化</td><td>不支持</td><td>支持（内存极小时有优势）</td></tr><tr><td>量化</td><td>AWQ/GPTQ/INT8/FP8</td><td>ZeroQuant, GPTQ</td></tr><tr><td>生态</td><td>独立项目</td><td>与 DeepSpeed 深度集成</td></tr><tr><td>适用场景</td><td>通用 LLM 推理服务</td><td>超大模型（trillion 参数级）</td></tr></tbody></table><hr><h3 id="q22-什么场景下-vllm-不是最优选择" tabindex="-1">Q22：什么场景下 vLLM 不是最优选择？ <a class="header-anchor" href="#q22-什么场景下-vllm-不是最优选择" aria-label="Permalink to &quot;Q22：什么场景下 vLLM 不是最优选择？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li><strong>CPU 推理</strong>：vLLM 强依赖 NVIDIA GPU，CPU 推理使用 llama.cpp 更合适</li><li><strong>移动端/边缘设备</strong>：使用 GGUF + llama.cpp 或 MLC-LLM</li><li><strong>需要精细 LoRA 控制</strong>：TGI 的 LoRA 支持更完善（多 LoRA 动态切换）</li><li><strong>单一短请求低延迟</strong>：vLLM 的 continuous batching 有少量调度开销，对极低延迟（&lt;20ms）场景用 TensorRT-LLM 更优</li><li><strong>训练时推理</strong>：使用 HuggingFace Transformers 或 DeepSpeed</li></ol><hr><h3 id="q23-q26-快问快答" tabindex="-1">Q23-Q26：快问快答 <a class="header-anchor" href="#q23-q26-快问快答" aria-label="Permalink to &quot;Q23-Q26：快问快答&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>Q23：vLLM 中的 <code>gpu_memory_utilization=0.9</code> 是什么意思？</strong></p><p>保留 10% GPU 显存作为安全缓冲（防止 OOM），90% 分配给模型权重和 KV Cache。KV Cache 块数 = (总显存 × 0.9 - 模型权重) / 块大小。</p><p><strong>Q24：为什么 PagedAttention 的块大小选 16？</strong></p><p>权衡碎片率和开销：块越小，内部碎片越少，但块表更大、元数据开销更多；块越大，碎片率上升。16 tokens/块 在大多数场景下是经验最优值。</p><p><strong>Q25：vLLM 的 Prefill 和 Decode 为什么效率不同？</strong></p><p>Prefill 是矩阵-矩阵乘法（Compute-bound），可以充分利用 GPU 算力；Decode 是矩阵-向量乘法（Memory-bound），瓶颈在显存带宽而非算力，GPU 利用率低。</p><p><strong>Q26：AWQ 和 GPTQ 量化的本质区别？</strong></p><table tabindex="0"><thead><tr><th></th><th>AWQ</th><th>GPTQ</th></tr></thead><tbody><tr><td>核心思想</td><td>识别重要权重（激活值大的通道）保持高精度</td><td>基于二阶 Hessian 信息最小化量化误差</td></tr><tr><td>校准数据</td><td>需要少量数据确定权重重要性</td><td>需要校准数据计算 Hessian</td></tr><tr><td>精度</td><td>通常略优</td><td>接近 AWQ</td></tr><tr><td>速度</td><td>量化过程较快</td><td>较慢</td></tr><tr><td>激活值精度</td><td>FP16（仅权重量化 W4A16）</td><td>FP16</td></tr></tbody></table><hr><h2 id="综合设计题-q27-q30" tabindex="-1">综合设计题（Q27-Q30） <a class="header-anchor" href="#综合设计题-q27-q30" aria-label="Permalink to &quot;综合设计题（Q27-Q30）&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q27-设计支持百万级用户的-llm-聊天服务" tabindex="-1">Q27：设计支持百万级用户的 LLM 聊天服务 <a class="header-anchor" href="#q27-设计支持百万级用户的-llm-聊天服务" aria-label="Permalink to &quot;Q27：设计支持百万级用户的 LLM 聊天服务&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>关键设计点</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 接入层：CDN + WAF + 全球负载均衡</span></span>
<span class="line"><span>2. 会话管理：Redis 存储对话历史，TTL 24h</span></span>
<span class="line"><span>3. 推理层：</span></span>
<span class="line"><span>   - 多区域部署（就近处理）</span></span>
<span class="line"><span>   - 每区域：3-5 个 vLLM 实例（A100×4 每实例）</span></span>
<span class="line"><span>   - 消息队列（Kafka）做异步解耦</span></span>
<span class="line"><span>4. 模型选择：</span></span>
<span class="line"><span>   - 简单问题 → 7B 模型（低延迟低成本）</span></span>
<span class="line"><span>   - 复杂问题 → 70B 模型（高质量）</span></span>
<span class="line"><span>   - 用分类器判断路由</span></span>
<span class="line"><span>5. 成本优化：</span></span>
<span class="line"><span>   - 夜间低峰期缩容</span></span>
<span class="line"><span>   - 使用 Spot 实例</span></span>
<span class="line"><span>   - AWQ 4-bit 量化降低 GPU 需求</span></span></code></pre></div><h3 id="q28-如何实现-vllm-的金丝雀发布-灰度升级" tabindex="-1">Q28：如何实现 vLLM 的金丝雀发布（灰度升级）？ <a class="header-anchor" href="#q28-如何实现-vllm-的金丝雀发布-灰度升级" aria-label="Permalink to &quot;Q28：如何实现 vLLM 的金丝雀发布（灰度升级）？&quot;">&amp;ZeroWidthSpace;</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 新版本 vLLM 在少量 Pod 上部署（10%）</span></span>
<span class="line"><span>2. 通过 Kubernetes Service 权重路由 10% 流量</span></span>
<span class="line"><span>3. 监控：错误率、延迟、内存使用对比新旧版本</span></span>
<span class="line"><span>4. 若指标正常，逐步扩大至 50% → 100%</span></span>
<span class="line"><span>5. 若异常，立即将权重调回 0%</span></span>
<span class="line"><span></span></span>
<span class="line"><span>关键监控指标对比：</span></span>
<span class="line"><span>  - TTFT P99</span></span>
<span class="line"><span>  - 错误率（5xx）</span></span>
<span class="line"><span>  - GPU OOM 频率</span></span>
<span class="line"><span>  - KV Cache 命中率（APC）</span></span></code></pre></div><h3 id="q29-如何排查-vllm-内存泄漏问题" tabindex="-1">Q29：如何排查 vLLM 内存泄漏问题？ <a class="header-anchor" href="#q29-如何排查-vllm-内存泄漏问题" aria-label="Permalink to &quot;Q29：如何排查 vLLM 内存泄漏问题？&quot;">&amp;ZeroWidthSpace;</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 监控 GPU 内存随时间的变化</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">watch</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -n</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 5</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nvidia-smi</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 检查 KV Cache 使用率是否持续增长</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Prometheus 查询：vllm:gpu_cache_usage_perc</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3. 检查是否有请求没有被正确清理</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 可能原因：异常请求未触发 abort_request()</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 4. 启用 vLLM 调试日志</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> VLLM_LOGGING_LEVEL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DEBUG</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 常见原因：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># - 客户端断开连接但服务端未检测到（缺少连接检测）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># - Beam Search 的 CoW 块未正确释放</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># - 解决：确保 request_id 唯一且 abort_request() 被调用</span></span></code></pre></div><h3 id="q30-如何在-vllm-上实现-rag-检索增强生成" tabindex="-1">Q30：如何在 vLLM 上实现 RAG（检索增强生成）？ <a class="header-anchor" href="#q30-如何在-vllm-上实现-rag-检索增强生成" aria-label="Permalink to &quot;Q30：如何在 vLLM 上实现 RAG（检索增强生成）？&quot;">&amp;ZeroWidthSpace;</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> vllm </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> LLM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, SamplingParams</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> faiss</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 向量检索（可以单独部署）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">def</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> retrieve</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(query: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, k: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 5</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) -&gt; list[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    query_vec </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> embed(query)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    _, indices </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> faiss_index.search(query_vec, k)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [documents[i] </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">for</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> i </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">in</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> indices[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 构建带上下文的 Prompt</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">def</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> build_rag_prompt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(query: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, context_docs: list[</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]) -&gt; </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">str</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    context </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\n\\n</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.join(context_docs)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&quot;&quot;Based on the following context, answer the question.</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Context:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">context</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Question: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">query</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Answer:&quot;&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3. 使用 vLLM 生成（启用前缀缓存）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">llm </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> LLM(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    model</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;meta-llama/Llama-2-7b-chat-hf&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">    enable_prefix_caching</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">True</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 系统 Prompt 共享缓存</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 4. 批量处理 RAG 请求（共享 System Prompt 的 KV Cache）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">queries </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;问题1&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;问题2&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;问题3&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">prompts </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [build_rag_prompt(q, retrieve(q)) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">for</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> q </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">in</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> queries]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">outputs </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> llm.generate(prompts, SamplingParams(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">max_tokens</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">512</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">))</span></span></code></pre></div><p><strong>APC 优化</strong>：若所有请求共享相同的 System Prompt，前缀缓存可以消除重复计算，大幅降低 TTFT。</p><h1 id="tensorrt-llm-常见面试题库" tabindex="-1">TensorRT-LLM 常见面试题库 <a class="header-anchor" href="#tensorrt-llm-常见面试题库" aria-label="Permalink to &quot;TensorRT-LLM 常见面试题库&quot;">&amp;ZeroWidthSpace;</a></h1><blockquote><p>涵盖 TensorRT 图优化、量化、in-flight batching、多 GPU 并行等核心考点，分初级 / 中级 / 高级三个层次。</p></blockquote><hr><h2 id="初级题" tabindex="-1">初级题 <a class="header-anchor" href="#初级题" aria-label="Permalink to &quot;初级题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q1-tensorrt-llm-与-vllm-的核心区别是什么" tabindex="-1">Q1. TensorRT-LLM 与 vLLM 的核心区别是什么？ <a class="header-anchor" href="#q1-tensorrt-llm-与-vllm-的核心区别是什么" aria-label="Permalink to &quot;Q1. TensorRT-LLM 与 vLLM 的核心区别是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>TensorRT-LLM</th><th>vLLM</th></tr></thead><tbody><tr><td>硬件绑定</td><td>NVIDIA GPU（CUDA/TensorRT）</td><td>多后端（CUDA/ROCm/CPU）</td></tr><tr><td>优化方式</td><td>编译期内核融合 + 量化</td><td>PagedAttention 运行时内存管理</td></tr><tr><td>量化支持</td><td>FP8/INT8/INT4（AWQ/GPTQ/SmoothQuant）</td><td>AWQ/GPTQ/INT8</td></tr><tr><td>部署集成</td><td>Triton Inference Server / NIM</td><td>OpenAI-compatible HTTP server</td></tr><tr><td>编译成本</td><td>高（需 build engine）</td><td>低（直接加载权重）</td></tr><tr><td>典型场景</td><td>NVIDIA 生产环境极致性能</td><td>通用多模型快速部署</td></tr></tbody></table><hr><h3 id="q2-tensorrt-的图优化流程分哪几步" tabindex="-1">Q2. TensorRT 的图优化流程分哪几步？ <a class="header-anchor" href="#q2-tensorrt-的图优化流程分哪几步" aria-label="Permalink to &quot;Q2. TensorRT 的图优化流程分哪几步？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li><strong>模型解析</strong>：读取 ONNX / TF / PyTorch 模型，建立 TensorRT 网络定义</li><li><strong>图优化</strong>（Graph Optimization）：消除冗余节点、常量折叠（Constant Folding）、层合并</li><li><strong>内核选择</strong>（Kernel Selection）：针对目标 GPU 选择最优 CUDA kernel（包含 cuBLAS、cuDNN）</li><li><strong>精度校准</strong>（Calibration）：INT8 量化时收集激活分布，确定 scale factor</li><li><strong>引擎序列化</strong>（Serialization）：将优化后的计划保存为 <code>.engine</code> 文件，下次直接加载</li></ol><hr><h3 id="q3-什么是内核融合-kernel-fusion-它带来哪些好处" tabindex="-1">Q3. 什么是内核融合（Kernel Fusion）？它带来哪些好处？ <a class="header-anchor" href="#q3-什么是内核融合-kernel-fusion-它带来哪些好处" aria-label="Permalink to &quot;Q3. 什么是内核融合（Kernel Fusion）？它带来哪些好处？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> 将多个独立的 CUDA kernel（如 LayerNorm → GEMM → GELU → GEMM）合并为一个 kernel 执行。</p><p><strong>好处：</strong></p><ul><li><strong>减少内存读写</strong>：中间结果留在寄存器/共享内存，避免写回 HBM</li><li><strong>降低 kernel launch 开销</strong>：每次 kernel 启动有固定延迟（~μs 级）</li><li><strong>提升 GPU 利用率</strong>：更大的计算密度</li></ul><p>典型融合：<code>QKV Projection</code> + <code>Softmax</code> + <code>Attention</code> 合并为 FlashAttention kernel。</p><hr><h3 id="q4-fp8-与-int8-量化有什么区别" tabindex="-1">Q4. FP8 与 INT8 量化有什么区别？ <a class="header-anchor" href="#q4-fp8-与-int8-量化有什么区别" aria-label="Permalink to &quot;Q4. FP8 与 INT8 量化有什么区别？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>FP8 (E4M3/E5M2)</th><th>INT8</th></tr></thead><tbody><tr><td>数据类型</td><td>浮点（含指数位）</td><td>整数</td></tr><tr><td>动态范围</td><td>较大（E4M3: ±448）</td><td>小（-128~127）</td></tr><tr><td>精度损失</td><td>更小</td><td>较大（尤其激活值分布不均匀时）</td></tr><tr><td>硬件支持</td><td>H100/H200 Tensor Core</td><td>A100 及以上</td></tr><tr><td>量化粒度</td><td>可 per-tensor/per-channel</td><td>同上</td></tr><tr><td>适用场景</td><td>H100 生产部署</td><td>A100 及更早 GPU</td></tr></tbody></table><hr><h3 id="q5-tensorrt-llm-的-in-flight-batching-是什么" tabindex="-1">Q5. TensorRT-LLM 的 in-flight batching 是什么？ <a class="header-anchor" href="#q5-tensorrt-llm-的-in-flight-batching-是什么" aria-label="Permalink to &quot;Q5. TensorRT-LLM 的 in-flight batching 是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> 也称 <strong>连续批处理</strong>（Continuous Batching）。传统静态批处理需要等一批请求全部完成才能接入新请求，而 in-flight batching 在每个解码步（iteration）后动态调整批次：</p><ul><li>已完成的序列立即移出批次</li><li>等待队列中的新请求立即加入</li><li>批次大小动态变化，GPU 持续满载</li></ul><p><strong>效果：</strong> 显著提升吞吐量，P99 延迟大幅降低，尤其对输出长度差异大的请求有效。</p><hr><h3 id="q6-什么是-smoothquant-解决了什么问题" tabindex="-1">Q6. 什么是 SmoothQuant？解决了什么问题？ <a class="header-anchor" href="#q6-什么是-smoothquant-解决了什么问题" aria-label="Permalink to &quot;Q6. 什么是 SmoothQuant？解决了什么问题？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br><strong>问题背景：</strong> LLM 激活值（activation）中存在离群值（outlier），直接 INT8 量化损失大。<br><strong>SmoothQuant 方法：</strong></p><ul><li>将激活中的困难（大值）迁移到权重中（权重更容易量化）</li><li>引入平滑因子 <code>s</code>：<code>Y = (X / s) × (W × s)</code></li><li>两边都变得更容易量化，精度损失小</li></ul><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 概念示意</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">smooth_factor </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> activation.abs().max(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">dim</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> alpha </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> weight.abs().max(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">dim</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> -</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> alpha)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">weight_smoothed </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> weight </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> smooth_factor</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">activation_smoothed </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> activation </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> smooth_factor</span></span></code></pre></div><hr><h2 id="中级题" tabindex="-1">中级题 <a class="header-anchor" href="#中级题" aria-label="Permalink to &quot;中级题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q7-tensorrt-llm-build-流程分几个阶段-每阶段做什么" tabindex="-1">Q7. TensorRT-LLM build 流程分几个阶段？每阶段做什么？ <a class="header-anchor" href="#q7-tensorrt-llm-build-流程分几个阶段-每阶段做什么" aria-label="Permalink to &quot;Q7. TensorRT-LLM build 流程分几个阶段？每阶段做什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>阶段 1: 权重转换</span></span>
<span class="line"><span>  HuggingFace 权重 → TRT-LLM 格式 (convert_checkpoint.py)</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>阶段 2: 引擎编译 (trtllm-build)</span></span>
<span class="line"><span>  定义网络结构 → 图优化 → 量化 → kernel 选择 → .engine 文件</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>阶段 3: 运行时推理 (tensorrt_llm.runtime)</span></span>
<span class="line"><span>  加载 .engine → 分配 KV cache → 处理请求 → 生成 token</span></span></code></pre></div><p>编译阶段耗时长（数分钟到数小时），但推理速度极快。</p><hr><h3 id="q8-tensorrt-llm-如何实现-tensor-parallel" tabindex="-1">Q8. TensorRT-LLM 如何实现 Tensor Parallel？ <a class="header-anchor" href="#q8-tensorrt-llm-如何实现-tensor-parallel" aria-label="Permalink to &quot;Q8. TensorRT-LLM 如何实现 Tensor Parallel？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ul><li><strong>列并行（Column Parallel）</strong>：将 GEMM 的权重矩阵按列切分，每个 GPU 计算部分输出，最后 All-Reduce</li><li><strong>行并行（Row Parallel）</strong>：权重按行切分，输入按列切分，局部 GEMM 后 All-Reduce</li><li>MHA 中 Q/K/V 投影用列并行，输出投影用行并行</li><li>FFN 中第一层列并行，第二层行并行</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TP=4 示意（4 GPU）:</span></span>
<span class="line"><span>  [W: 4096×4096] → 每 GPU 持有 [W: 4096×1024]</span></span>
<span class="line"><span>  前向：各 GPU 计算局部结果 → NCCL All-Reduce → 合并输出</span></span></code></pre></div><hr><h3 id="q9-kv-cache-分页-paged-kv-cache-在-trt-llm-中如何实现" tabindex="-1">Q9. KV Cache 分页（Paged KV Cache）在 TRT-LLM 中如何实现？ <a class="header-anchor" href="#q9-kv-cache-分页-paged-kv-cache-在-trt-llm-中如何实现" aria-label="Permalink to &quot;Q9. KV Cache 分页（Paged KV Cache）在 TRT-LLM 中如何实现？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> TensorRT-LLM 借鉴 vLLM 的 PagedAttention 思想：</p><ul><li>KV cache 不预分配固定大小，而是按 <strong>page/block</strong> 动态分配</li><li>每个 page 存储固定数量（如 16 或 32）token 的 KV 值</li><li>BlockManager 维护 logical→physical 块映射</li><li>支持 beam search 的写时复制（Copy-on-Write）</li></ul><p><strong>优势：</strong> 减少内存碎片，允许更大批次，提升 GPU 内存利用率。</p><hr><h3 id="q10-如何选择-int8-vs-int4-量化" tabindex="-1">Q10. 如何选择 INT8 vs INT4 量化？ <a class="header-anchor" href="#q10-如何选择-int8-vs-int4-量化" aria-label="Permalink to &quot;Q10. 如何选择 INT8 vs INT4 量化？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><table tabindex="0"><thead><tr><th>场景</th><th>推荐</th></tr></thead><tbody><tr><td>精度优先，VRAM 充足</td><td>FP16/BF16（不量化）</td></tr><tr><td>均衡场景，A100/H100</td><td>INT8（SmoothQuant/GPTQ）</td></tr><tr><td>VRAM 受限，可接受精度损失</td><td>INT4（AWQ/GPTQ W4A16）</td></tr><tr><td>H100，追求极致性能</td><td>FP8</td></tr></tbody></table><p><strong>实践原则：</strong></p><ul><li>先尝试 INT8，对比 perplexity / 任务指标</li><li>INT4 一般损失 &lt; 1% perplexity（AWQ 优于 GPTQ）</li><li>注意：权重 INT4 + 激活 FP16（W4A16）比 W4A4 精度好很多</li></ul><hr><h3 id="q11-triton-inference-server-与-tensorrt-llm-如何配合" tabindex="-1">Q11. Triton Inference Server 与 TensorRT-LLM 如何配合？ <a class="header-anchor" href="#q11-triton-inference-server-与-tensorrt-llm-如何配合" aria-label="Permalink to &quot;Q11. Triton Inference Server 与 TensorRT-LLM 如何配合？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>客户端 HTTP/gRPC</span></span>
<span class="line"><span>      ↓</span></span>
<span class="line"><span>Triton Inference Server</span></span>
<span class="line"><span>  ├── 请求路由 &amp; 并发管理</span></span>
<span class="line"><span>  ├── 动态批处理（Dynamic Batching）</span></span>
<span class="line"><span>  ├── 多模型多版本管理</span></span>
<span class="line"><span>  └── TensorRT-LLM Backend (libtensorrt_llm.so)</span></span>
<span class="line"><span>            ↓</span></span>
<span class="line"><span>      TRT Engine 执行</span></span>
<span class="line"><span>      (GPU 内核融合推理)</span></span></code></pre></div><p>配置文件 <code>config.pbtxt</code> 指定 backend、batch 策略、实例数等参数。</p><hr><h3 id="q12-什么是-gqa-grouped-query-attention-trt-llm-如何优化它" tabindex="-1">Q12. 什么是 GQA（Grouped Query Attention）？TRT-LLM 如何优化它？ <a class="header-anchor" href="#q12-什么是-gqa-grouped-query-attention-trt-llm-如何优化它" aria-label="Permalink to &quot;Q12. 什么是 GQA（Grouped Query Attention）？TRT-LLM 如何优化它？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> GQA 将 KV head 数量减少（如从 32 个 Q head 减到 8 个 KV head），多个 Q head 共享一组 KV，减少 KV cache 大小。</p><p>TRT-LLM 优化：</p><ul><li>专用 CUDA kernel 处理 GQA/MQA attention</li><li>KV cache 只存 KV head 数，而非 Q head 数</li><li>减少 KV cache 内存最高达 4×（如 LLaMA-3 70B: 8 KV heads vs 64 Q heads）</li></ul><hr><h2 id="高级题" tabindex="-1">高级题 <a class="header-anchor" href="#高级题" aria-label="Permalink to &quot;高级题&quot;">&amp;ZeroWidthSpace;</a></h2><h3 id="q13-设计一个基于-tensorrt-llm-的生产-llm-服务系统-支持-1000-qps" tabindex="-1">Q13. 设计一个基于 TensorRT-LLM 的生产 LLM 服务系统，支持 1000 QPS <a class="header-anchor" href="#q13-设计一个基于-tensorrt-llm-的生产-llm-服务系统-支持-1000-qps" aria-label="Permalink to &quot;Q13. 设计一个基于 TensorRT-LLM 的生产 LLM 服务系统，支持 1000 QPS&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>架构设计：</span></span>
<span class="line"><span>┌─────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│                    负载均衡层 (Nginx/Envoy)               │</span></span>
<span class="line"><span>└────────────────────┬────────────────────────────────────┘</span></span>
<span class="line"><span>                     │</span></span>
<span class="line"><span>         ┌───────────┴───────────┐</span></span>
<span class="line"><span>         ↓                       ↓</span></span>
<span class="line"><span>┌────────────────┐     ┌────────────────┐</span></span>
<span class="line"><span>│ Triton Node 1  │     │ Triton Node 2  │   (可水平扩展)</span></span>
<span class="line"><span>│ 4×H100 TP=4   │     │ 4×H100 TP=4   │</span></span>
<span class="line"><span>│ TRT-LLM Engine │     │ TRT-LLM Engine │</span></span>
<span class="line"><span>└────────────────┘     └────────────────┘</span></span>
<span class="line"><span>         ↓</span></span>
<span class="line"><span>┌────────────────────────────────────────┐</span></span>
<span class="line"><span>│         监控层 (Prometheus + Grafana)   │</span></span>
<span class="line"><span>│  指标: throughput, latency, GPU util   │</span></span>
<span class="line"><span>└────────────────────────────────────────┘</span></span></code></pre></div><p><strong>关键配置：</strong></p><ul><li>TP=4 部署 70B 模型（4×H100 80GB）</li><li>in-flight batching，max_batch_size=128</li><li>KV cache paging，内存利用率 &gt; 80%</li><li>FP8 量化（H100）</li><li>Triton Dynamic Batching，preferred_batch_size=[32,64]</li><li>请求队列 + 超时控制（max_queue_delay_microseconds）</li></ul><hr><h3 id="q14-tensorrt-llm-的-pipeline-parallel-如何处理-micro-batch" tabindex="-1">Q14. TensorRT-LLM 的 Pipeline Parallel 如何处理 micro-batch？ <a class="header-anchor" href="#q14-tensorrt-llm-的-pipeline-parallel-如何处理-micro-batch" aria-label="Permalink to &quot;Q14. TensorRT-LLM 的 Pipeline Parallel 如何处理 micro-batch？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ul><li>模型按层切分为 PP 个阶段（Stage），每个 Stage 在不同 GPU 上</li><li>使用 <strong>1F1B（One Forward One Backward）</strong> 调度减少 bubble</li><li>推理时无 backward，简化为流水线前向</li><li>Micro-batch 在流水线中重叠执行：Stage 1 处理 batch N+1 时，Stage 2 在处理 batch N</li></ul><p><strong>Pipeline Bubble 比例：</strong> <code>(PP-1) / (num_micro_batches + PP - 1)</code></p><hr><h3 id="q15-awq-activation-aware-weight-quantization-原理是什么" tabindex="-1">Q15. AWQ（Activation-aware Weight Quantization）原理是什么？ <a class="header-anchor" href="#q15-awq-activation-aware-weight-quantization-原理是什么" aria-label="Permalink to &quot;Q15. AWQ（Activation-aware Weight Quantization）原理是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> AWQ 的核心观察：<strong>权重中只有少部分（约 1%）对激活值影响大</strong>，对这部分权重保持高精度。</p><p><strong>算法：</strong></p><ol><li>统计激活值分布，找出 salient（重要）通道</li><li>对 salient 通道的权重放大（scale up），量化误差分散</li><li>其余通道正常 INT4 量化</li><li>推理时通过 scale factor 恢复</li></ol><p><strong>与 GPTQ 对比：</strong></p><ul><li>AWQ：免校准数据集（只需少量激活统计），速度更快</li><li>GPTQ：需要校准集，使用二阶海森矩阵信息，精度略高</li></ul><hr><h3 id="q16-如何-profile-tensorrt-llm-推理性能瓶颈" tabindex="-1">Q16. 如何 profile TensorRT-LLM 推理性能瓶颈？ <a class="header-anchor" href="#q16-如何-profile-tensorrt-llm-推理性能瓶颈" aria-label="Permalink to &quot;Q16. 如何 profile TensorRT-LLM 推理性能瓶颈？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 使用 nsys 采集 GPU timeline</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">nsys</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> profile</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --trace=cuda,nvtx</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  python</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run_trtllm.py</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --model</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> llama-7b</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 查看 GEMM vs attention 耗时比例</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">nsys</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> stats</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> report.nsys-rep</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3. 使用 ncu 详细分析单个 kernel</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ncu</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --metrics</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> sm__throughput.avg.pct_of_peak_sustained_elapsed</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  python</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run_trtllm.py</span></span></code></pre></div><p><strong>常见瓶颈：</strong></p><ul><li><code>prefill 阶段</code>：GEMM 密集，关注 SM utilization</li><li><code>decode 阶段</code>：访存密集（memory-bound），关注 HBM 带宽</li><li><strong>Roofline 模型</strong>：判断操作是 compute-bound 还是memory-bound</li></ul><hr><h3 id="q17-tensorrt-llm-中-speculative-decoding-如何加速推理" tabindex="-1">Q17. TensorRT-LLM 中 speculative decoding 如何加速推理？ <a class="header-anchor" href="#q17-tensorrt-llm-中-speculative-decoding-如何加速推理" aria-label="Permalink to &quot;Q17. TensorRT-LLM 中 speculative decoding 如何加速推理？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br><strong>原理：</strong> 用小模型（draft model）快速生成多个候选 token，大模型（target model）并行验证。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Draft Model (小):  [token1][token2][token3][token4]  → 4 个候选</span></span>
<span class="line"><span>Target Model (大): 并行验证 4 个 token</span></span>
<span class="line"><span>  - 接受前 k 个，拒绝第 k+1 个</span></span>
<span class="line"><span>  - 一次 target 前向 = 生成多个 token</span></span></code></pre></div><p><strong>加速比：</strong> 理想情况 3-4× token 速率提升（acceptance rate 越高，加速越明显）。</p><p>TRT-LLM 支持：</p><ul><li>Medusa（多个 draft heads）</li><li>Eagle（轻量级 draft 模型）</li><li>Draft model 选择（需与 target 同 tokenizer）</li></ul><hr><h3 id="q18-如何减少-trt-llm-引擎的编译时间" tabindex="-1">Q18. 如何减少 TRT-LLM 引擎的编译时间？ <a class="header-anchor" href="#q18-如何减少-trt-llm-引擎的编译时间" aria-label="Permalink to &quot;Q18. 如何减少 TRT-LLM 引擎的编译时间？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><ol><li><strong>使用预编译引擎（NIM）</strong>：NVIDIA 提供常见模型的预编译 .engine 文件</li><li><strong>指定精确 max_batch_size / max_seq_len</strong>：避免过大的优化空间</li><li><strong>禁用不需要的精度（strongly_typed=True）</strong>：减少 kernel profile 数量</li><li><strong>使用 weight_only_quant 而非 full INT8</strong>：编译更快</li><li><strong>分层编译缓存</strong>：相同层可复用编译结果</li></ol><hr><h3 id="q19-tensorrt-llm-的-moe-专家混合-并行策略是什么" tabindex="-1">Q19. TensorRT-LLM 的 MoE（专家混合）并行策略是什么？ <a class="header-anchor" href="#q19-tensorrt-llm-的-moe-专家混合-并行策略是什么" aria-label="Permalink to &quot;Q19. TensorRT-LLM 的 MoE（专家混合）并行策略是什么？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong><br> Mixture of Experts 模型（如 Mixtral 8×7B）在 TRT-LLM 中使用 <strong>Expert Parallel（EP）</strong>：</p><ul><li>将不同专家分配到不同 GPU 上</li><li>Token routing 后，跨 GPU 发送 token 到对应专家 GPU（All-to-All 通信）</li><li>可与 TP、DP 组合使用（TP×EP×DP = 总 GPU 数）</li></ul><p><strong>通信开销：</strong> MoE 的 All-to-All 是主要瓶颈，需要高速 NVLink 互联。</p><hr><h3 id="q20-如何验证量化后模型的精度" tabindex="-1">Q20. 如何验证量化后模型的精度？ <a class="header-anchor" href="#q20-如何验证量化后模型的精度" aria-label="Permalink to &quot;Q20. 如何验证量化后模型的精度？&quot;">&amp;ZeroWidthSpace;</a></h3><p><strong>答：</strong></p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1. 困惑度 (Perplexity) 对比</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> torch</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tensorrt_llm </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> LLM</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">fp16_ppl </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> compute_perplexity(fp16_model, wikitext2)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">int8_ppl  </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> compute_perplexity(int8_model,  wikitext2)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">print</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;精度损失: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">{</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">int8_ppl </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> fp16_ppl</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:.2f</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 通常 &lt; 0.5</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2. 任务指标 (MMLU, HellaSwag 等)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 使用 lm-evaluation-harness</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">python main.py </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">model trtllm </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">model_args model_dir</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">engine \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">  --</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">tasks mmlu </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">num_fewshot </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">5</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3. 端到端输出质量评估</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 抽样比较量化前后的生成文本</span></span></code></pre></div><p><strong>经验阈值：</strong></p><ul><li>PPL 增加 &lt; 0.5：优秀</li><li>PPL 增加 0.5-2.0：可接受</li><li>PPL 增加 &gt; 2.0：需重新调整量化参数</li></ul><hr><p><em>参考资料：<a href="https://github.com/NVIDIA/TensorRT-LLM" target="_blank" rel="noreferrer">TensorRT-LLM GitHub</a> | <a href="https://docs.nvidia.com/deeplearning/tensorrt/" target="_blank" rel="noreferrer">NVIDIA TensorRT 文档</a></em></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("02-面试指南/05-系统设计面试/01-LLM推理引擎面试题.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _01LLM_______ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _01LLM_______ as default
};
