# AGENTS.md — AI Agent 入口（精简目录）

> **Harness Engineering 规范**：本文件保持精简，作为 AI Agent 的快速导航入口。
> 详细操作规范请参阅 [`.agent-docs/operations.md`](./.agent-docs/operations.md)，避免上下文窗口拥塞。

## 关键资源（优先阅读）

| 资源 | 说明 |
|------|------|
| [`llms.txt`](./llms.txt) | 项目全局结构，AI 工具入口 |
| [`CLAUDE.md`](./CLAUDE.md) | 协作规范与技术分类（含记忆系统指南） |
| [`.agent-docs/operations.md`](./.agent-docs/operations.md) | 详细操作规范：命名、目录更新、日志格式 |
| [`README.md`](./README.md) | 项目背景与整体介绍 |

## 项目结构速览

> 本项目共 **9 大技术分类**（01-前端 ~ 09-AI-Agent工程化），`01-技术资料` 与 `02-面试指南` 一一对应。

```
max-effort/
├── AGENTS.md               # AI Agent 入口（本文件，精简 TOC）
├── README.md               # 项目概览
├── CLAUDE.md               # 协作规范（含记忆系统最佳实践）
├── llms.txt                # llms.txt 规范
├── .agent-docs/            # 详细规范文档（渐进式披露）
│   └── operations.md       # 操作规范详情
├── _WORKING_/              # 任务中间状态（任务完成后删除）
├── CHANGELOG/              # 操作记录
├── 01-技术资料/             # 技术学习文档（共 9 个分类）
│   ├── 01-前端/
│   │   ├── _index.md       # 内容索引与去重（构建器忽略）
│   │   ├── __blocks/       # 长文件分块（构建器忽略）
│   │   └── ...
│   ├── 02-后端/
│   ├── 03-数据库/
│   ├── 04-算法与数据结构/
│   ├── 05-系统设计/
│   ├── 06-DevOps与云计算/
│   ├── 07-AI-Agent全栈开发/
│   ├── 08-编码题/
│   └── 09-AI-Agent工程化/
└── 02-面试指南/             # 面试题目与解析（共 9 个分类）
    ├── 01-前端面试/
    ├── 02-后端面试/
    ├── 03-数据库面试/
    ├── 04-算法与数据结构面试/
    ├── 05-系统设计面试/
    ├── 06-DevOps与云计算面试/
    ├── 07-AI-Agent全栈开发面试/
    ├── 08-编码题面试/
    └── 09-AI-Agent工程化面试/
```

> 完整目录树见 [`.agent-docs/operations.md`](./.agent-docs/operations.md#目录结构总览)

## 核心约束（机械化强制执行）

每次修改文件后，Agent **必须**执行以下检查：

- [ ] 子目录 `README.md` 已更新
- [ ] 根 `llms.txt` 已更新
- [ ] `CHANGELOG/YYYY-MM.md` 已追加记录
- [ ] `01-技术资料` 与 `02-面试指南` 的对应分类保持同步

## 任务执行规范（Cloud Agent 必须遵守）

> Cloud Agent 有运行时间限制，以下机制确保任务可中断、可恢复、可并发。

### 0. 代码提交：使用 `report_progress` 工具（禁止 `git push`）

> ⚠️ **Cloud Agent 环境中 `git push` 命令没有推送权限，所有提交必须通过 `report_progress` 工具完成。**

- **提交方式**：调用 `report_progress` 工具，它会自动执行 `git add . && git commit && git push`
- **禁止操作**：不要使用 `git push`、`gh` CLI 等命令直接推送代码
- **提交频率**：每完成一个有意义的阶段性目标就调用一次 `report_progress`
- **PR 描述**：通过 `prDescription` 参数传入 Markdown 格式的进度清单，使用 `- [x]` / `- [ ]` 标记完成状态
- **Commit 信息**：通过 `commitMessage` 参数传入简短的提交信息

### 1. 中断恢复：`_WORKING_` 文件夹

- 每次编辑任务开始时，在项目根目录创建 `_WORKING_/` 文件夹
- 其中存放：用户原始指令、实施计划、当前进度、已获取的经验等中间过程内容
- 被终止后，下一个 Agent 可从 `_WORKING_/` 中恢复上下文，从中断处继续
- **任务完全完成时，必须删除 `_WORKING_/` 文件夹**
- 阶段性目标完成后，**必须调用 `report_progress` 提交代码**（避免丢失进度）

### 2. 长文件分块：`__blocks/` 文件夹

- 对于较长的文件，考虑拆分为多个块，放置到**同级目录**下的 `__blocks/同名文件夹/` 中
- `__blocks/` 文件夹内必须包含 `_manifest.md`，记录各块的索引、顺序和目录信息
- 编辑完成后，通过命令按 `_manifest.md` 中的顺序拼接为完整文件
- **主要目的**：支持多个 subagent 并发编辑不同块，大幅降低任务耗时
- `__blocks/` 已被 VitePress 构建器忽略，最终站点只显示拼接后的完整文件

### 3. 内容索引与去重：`_index.md`

- 编辑任务过程中，更新当前文件夹下的 `_index.md` 文件（不存在则创建）
- `_index.md` 包含当前目录下所有文档的各部分内容索引，**精确到二级标题**
- 核心职责：
  - 为后续变更提供快速定位索引
  - **内容去重**：多 subagent 并行编写时可能产生重复内容，编写完成后应回顾 `_index.md` 确保同一主题不会在不同文档中完全重复（同主题的不同层次/场景内容可保留）
- `_index.md` 已被 VitePress 构建器忽略，不会出现在最终站点中

> 详细操作流程见 [`.agent-docs/operations.md`](./.agent-docs/operations.md#任务执行流程)

## 写作规范（必须遵守）

- **教程式写作**：技术文档按「为什么 → 是什么 → 怎么做 → 实践案例」的递进逻辑组织
- **类比优先**：每个核心概念必须提供至少一个生活化类比
- **完整上下文**：不假设读者已知前置知识，关键概念从动机开始解释
- **深度优于广度**：宁可一个知识点讲透，不可泛泛而谈仅指出"有这么一回事"
- **常见陷阱必备**：每篇文档必须包含「常见陷阱与最佳实践」章节，用 ❌ vs ✅ 对照格式
- **TL;DR 速览**：每篇文档开头应包含 5-8 条核心要点速览，方便快速回忆
- 详见 [`CLAUDE.md` 文档写作理念](./CLAUDE.md#文档写作理念)

## 架构边界（不可违反）

1. **序号连续**：文件夹和文档均以 `NN-` 开头，编号不可跳跃
2. **分类对应**：两大目录的 `NN` 编号一一对应，不可单独新增
3. **相对路径**：所有文档内链接必须使用相对路径
4. **渐进披露**：`AGENTS.md` 保持精简；大量细节放入 `.agent-docs/` 子目录

## 记忆与上下文管理

本项目遵循以下上下文策略（参考 Claude Memory 最佳实践）：

- **上下文内记忆**：优先使用 `llms.txt` + `AGENTS.md` 快速定位，避免一次性加载全量文档
- **外部检索**：通过分类目录的 `README.md` 渐进式获取所需技术资料或面试题
- **Skills 模式**：各分类目录下的 `README.md` 充当"技能模块"，按需加载，不全量注入上下文
